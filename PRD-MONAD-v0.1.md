# PRD — MONAD v0.2

**단일 기질 로컬 지능 엔진 (Single-Substrate Local Intelligence Engine)**

문서 버전 0.2 · 2026-08-11 · 근거 문서: [Non-LLM-AGI-Research-2026-08.md](Non-LLM-AGI-Research-2026-08.md)

> **v0.2 변경** (외부 비평 반영): ① 자료형을 "인지 원자"로 정교화 — 연속값 강제 이진화 제거(§4.1) ② "특수 사례 도출" 단정을 가설 검증으로 완화(§4.8) ③ M0 통과 기준을 Bounce Test(추상·전이 증명) 중심으로 교체, 8→10주(§6). 실행 분해: [DEV-PLAN-MONAD.md](DEV-PLAN-MONAD.md)

---

## 0. 한 줄 정의

> **MONAD는 문장을 생성하지 않는다. 세계를 배운다.**
> GPU 없는 노트북에서 상시 구동되며, 사용 중에 계속 배우고(온라인 학습), 배운 것을 잊지 않고(지속 학습), 스스로 목표를 향해 행동하는(에이전시) **단일 기질(single-substrate) 에이전트 엔진**.

이름의 뜻: 라이프니츠의 모나드 — *세계 전체를 비추는 더 이상 쪼갤 수 없는 하나의 단위*. 이 제품의 설계 철학("단 하나")과 정확히 일치한다.

---

## 1. 왜 만드는가 (Problem / Opportunity)

### 1.1 문제
- LLM은 지식을 파라미터에 저장하고 GPU로 조회한다 → 서버 종속, 추론 비용, **배포 후 학습 불능**(가소성 상실은 Nature 2024로 실증됨).
- 비-LLM 진영은 6대 지능 속성(세계모델·지속학습·에이전시·추론·샘플효율·범용성)을 **전부 따로따로만** 실증했다. 이를 한 시스템에 담은 사례는 2026년 8월 현재 세계에 없다.
- 기존 통합 시도는 전부 **파이프라인 접합**(지각 모듈 + 추론 모듈 + 메모리 모듈…)이었고, 접합부에서 불확실성·학습 신호가 끊겨 실패했다. 인지 아키텍처 40년사가 그 증거다.

### 1.2 기회
- 부품 원리는 전부 논문·코드로 공개돼 있다(AXIOM의 무경사 구조 학습, CSCG의 클론 그래프, VSA의 심볼 대수, EFE 계획, NARS의 자원 제한 제어, DreamCoder의 라이브러리 성장).
- 자본은 대형 오프라인 세계모델로 몰렸고, **"저사양 로컬 + 온라인 학습 + 세계모델 에이전트" 조합은 완전한 공백**이다.
- 이 공백은 우연이 아니라 방법론의 공백이다: 모두가 "모듈을 잇는" 방식으로 접근했다. MONAD는 **모듈이 없다**.

---

## 2. 제품 원칙 — "단 하나"의 교리 (Five Ones)

기존 방식(모듈 파이프라인, 사전학습→배포 2단계, 백프롭 + 리플레이 노가다)을 따르지 않는다. 시스템 전체가 다음 다섯 개의 "하나"로만 구성된다.

| # | 단 하나의 | 정의 | 대체하는 기존 방식 |
|---|---|---|---|
| 1 | **자료형** | D차원 희소 블록 하이퍼벡터(SBV) — 지각·개념·관계·상태·목표·스키마·행동 전부 이 타입 하나 | 텐서/토큰/심볼/포인터가 뒤섞인 이종 표현 |
| 2 | **자료구조** | 성장하는 클론 전이 그래프(Clone-Structured World Graph) — 기억이 곧 세계모델 | 가중치 행렬 + 별도 벡터DB + 별도 KV캐시 |
| 3 | **목적함수** | 자유에너지 F (지각·학습) / 기대자유에너지 G (행동·탐색) | 태스크별 손실함수 + 보상 셰이핑 + RLHF |
| 4 | **학습 규칙** | 국소 카운트 갱신(1-shot 쓰기) + 수면기 베이지안 모델 축약(BMR) | 백프롭 + 리플레이 버퍼 + 재학습 파이프라인 |
| 5 | **루프** | 깨어서 한 틱(지각→정착→행동→기록), 한가할 때 수면 패스(압축→스키마 추출) | 학습 단계 / 배포 단계의 분리 |

**통합의 정리(thesis)**: 지각 = 현재에 대한 추론, 계획 = 미래에 대한 추론, 추상 추론 = 스키마 공간에 대한 추론, 기억 = 추론이 설명 못 한 것의 기록, 학습 = 그 기록의 압축. **다섯 능력이 다른 모듈이 아니라 같은 연산의 다른 방향이다.** 이것이 "이어붙이기"와의 근본적 차이다.

### 금지 목록 (Anti-requirements)
- ❌ 백프롭·경사하강 (코어 루프에서 전면 금지)
- ❌ 리플레이 버퍼, 타깃 네트워크, 학습/배포 단계 분리
- ❌ 사전학습 모델 이어붙이기, 프롬프트 접착
- ❌ float 행렬곱 의존 (코어는 비트 연산 + 정수 카운트)
- ❌ 자기회귀 다음-토큰 목적함수

---

## 3. 성능 계약 — "프론티어급"의 정직한 정의

프론티어 LLM과 **같은 종목으로 겨루지 않는다. 이길 수 있는 종목을 명시하고, 그 종목에서 프론티어를 이긴다.** 조사(§9)가 보여줬듯 텍스트 박식함은 코퍼스 스케일의 함수라 이 체급에서 경쟁 불가하며, 그것을 좇는 순간 이 제품은 열화 LLM이 된다.

### 3.1 이기는 축 (Beat Frontier) — v1의 존재 이유
| 능력 | MONAD 목표 | 프론티어 LLM 현주소 |
|---|---|---|
| **온라인 학습** | 새 환경을 상호작용 중에 습득, 배포 후 영구 학습 | 구조적으로 불가(컨텍스트=휘발, 파인튜닝=별도 파이프라인+망각) |
| **지속 학습** | 10개 환경 순차 학습 후 유지율 ≥ 90% | 파국적 망각, 가소성 상실(Nature 2024) |
| **샘플 효율** | 아케이드급 환경 유능 플레이 ≤ 10^4 스텝 (AXIOM 3,175 스텝 동급~상회) | 해당 능력 자체가 없음 (RL 계열 SOTA: DreamerV3 24,207) |
| **상시 구동 비용** | 노트북 CPU 4코어, RAM ≤ 4GB, ≥ 20Hz, 네트워크 0 | GPU 서버, 토큰당 과금, 상시 구동 비현실 |
| **투명성** | 모든 믿음·예측·계획이 검사 가능한 이산 구조 | 블랙박스 |

### 3.2 대등을 노리는 축 (Match, cost-adjusted)
| 능력 | MONAD 목표 (M2) | 비교 기준 |
|---|---|---|
| 추상·조합 추론 | ARC-AGI-1 ≥ 30% **사전학습 0·귀납적**(퍼즐 ID 금지)·과제당 ≤ 5분/CPU | 무사전학습 SOTA인 CompressARC 20%(GPU 20분)를 상회. TRM 44.6%는 transductive라 비교 대상 아님 |
| 계획 깊이 | 학습된 그래프 위 anytime 계획 — 시간을 더 주면 더 좋은 답 | MCTS류(좁은 도메인)와 동일 원리, LLM CoT보다 검증 가능 |

### 3.3 겨루지 않는 축 (Non-goals, v1)
- 개방 도메인 잡담·백과사전 지식·코드 생성. **v1의 언어는 "세계에 접지된 명령·질의 이해"까지만**(M3). 채팅 제품이 아니다.
- 사진급 이미지 인식 일반성 (지각은 과제 도메인에 한정해 출발)

> 이 3축 계약이 마케팅 거품 체크리스트(리서치 §8)를 스스로에게 적용한 결과다. 우리는 이길 종목을 공개적으로 못박고, 못 하는 종목을 숨기지 않는다.

---

## 4. 아키텍처 설계

### 4.1 표현층 — 인지 원자 (Cognitive Atom, SBV 코어)

모든 것의 자료형은 **인지 원자** 하나다:

```
Atom { id: SBV, value: 선택적 연속 벡터(소형), evidence: 카운트, t: 타임스탬프 }
```

정체성·바인딩·연상·유사성은 id(SBV)가 담당하고, 위치·속도·확률 같은 연속량은 value 필드가 담당한다 — **연속값을 억지로 이진화하지 않는다**(v0.2). 기질은 여전히 하나다: 그래프의 모든 노드가 같은 원자 타입이고, 대수 연산은 id 위에서만 정의된다.

id의 구현: **D = 16,384비트 = 128블록 × 128비트, 블록당 활성 1비트**(희소 블록 코드).

- 연산 4개가 전부: **bind**(⊗, 블록별 순환 시프트 — 역연산 존재), **bundle**(⊕, 블록별 다수결 — 중첩), **permute**(ρ, 순서 인코딩), **cleanup**(연상 메모리 최근접 인출).
- 왜 이것인가: ① 비트 연산 = CPU 네이티브(AVX2/NEON), 벡터당 2KB ② bundle이 곧 1-shot 학습 ③ bind가 곧 변수 바인딩 = 체계적 조합성(NVSA가 RPM 88%로 실증한 그 대수) ④ 노이즈에 강건(홀로그래픽) ⑤ 블록 코드는 랜덤 이진 대비 바인딩 용량·복원력 우위.
- 객체 = bind(무엇, 어디, 상태), 사건 = bind(객체, 행동, 결과), 스키마 = bundle(사건들의 공통 구조). **AXIOM의 슬롯 = bind의 특수 사례, Hopfield 인출 = cleanup의 특수 사례.**

### 4.2 구조층 — 클론 세계 그래프 (Clone-Structured World Graph)

기억과 세계모델이 같은 것. 노드 = 잠재 상태(SBV), 간선 = 카운트 붙은 전이 `N(s, a → s')`.

- **같은 관측, 다른 문맥 → 다른 클론.** 예측이 계속 틀리는 자리에서 새 클론 노드를 즉석 할당한다(1-shot 구조 성장). 이것이 CSCG의 클론 분화이자 AXIOM의 혼합성분 성장 — **같은 수(move)의 두 표기법**임을 조사에서 확인했다.
- 전이 저장 = 연상 메모리: key = bind(s, a), value = 후속 상태 분포(bundle + 카운트). 조회는 역색인 + 해밍 랭킹.
- 보상·선호 = 상태 위 선호 사전분포 C (능동추론의 C 벡터). 별도 보상 모델 없음.

### 4.3 목적층 — 자유에너지 하나

- **지각**: 관측을 받으면 그래프 위 믿음을 정착(settle)시켜 F 최소화 — 몇 회의 국소 메시지 패싱. 놀라움(설명 실패)의 잔여가 곧 학습 신호.
- **학습**: F가 임계 초과로 남으면 → 클론 할당/카운트 갱신(쓰기). **학습률 없음, 에폭 없음.**
- **행동**: 후보 정책의 기대자유에너지 G = 실용 가치(선호 도달) + **정보 이득(불확실한 곳 탐색)**. 호기심이 별도 모듈이 아니라 목적함수의 한 항이다.
- **주의/예산**: NARS의 AIKR 원칙을 정착·계획의 반복 예산으로 구현 — 시간이 없으면 얕게, 있으면 깊게. **anytime 시스템.**

### 4.4 시간층 — 생각 = 반복

추론 깊이는 파라미터가 아니라 시간에서 산다(리서치 §7 원리 6, TRM/HRM 분석의 정직한 교훈: 점수의 실체는 테스트 시점 정제 루프였다 — 우리는 그 루프를 트릭이 아니라 1급 설계로 채택하되, 퍼즐 ID 같은 transductive 편법은 금지한다).

- 같은 정착 루프가 짧게 돌면 반사, 길게 돌면 숙고. System 1/2가 두 모듈이 아니라 **한 루프의 반복 횟수 차이**.
- 계획 = 그래프 위 연상 체이닝 롤아웃(이산·초고속), G로 가지치기.

### 4.5 추상층 — 수면과 라이브러리

한가할 때(수면 패스) 그래프를 압축한다. **압축이 곧 추상화다.**

- **BMR(베이지안 모델 축약)**: 증거 적은 클론 병합·간선 가지치기 → 망각 없는 용량 관리.
- **스키마 추출**: 반복 등장하는 부분그래프를 하나의 SBV 스키마로 청킹 → 라이브러리에 등재 → 이후 새 상황에서 bind/unbind로 즉시 재사용(유추). 이것이 DreamCoder의 라이브러리 성장 + OaK의 옵션 — 조사에서 "누적 학습의 유일한 실증"으로 판정된 그 메커니즘을 기질 내장으로 만든 것.
- ARC류 과제 = "예제들을 최소 서술로 압축하는 스키마 합성" — CompressARC의 압축=지능 테제를 라이브러리 탐색으로 수행.

### 4.6 지각 경계 (정직한 최약점)

픽셀→SBV 인코더가 유일하게 "신경망스러운" 부분이다. v1 방침:
- 이벤트 구동 입력(변화만 처리) + 랜덤 투영 + k-WTA 희소화로 시작(학습 없는 고정 인코더).
- 부족하면 국소 예측부호화 인코더(층 국소 학습, 전역 백프롭 없음)까지 허용. **코어 루프의 무경사 원칙은 불변.**
- 도메인 확장은 인코더 교체가 아니라 인코더 추가로(같은 SBV로 합류하므로 코어 불변).

### 4.7 단일 루프 (전체 시스템 의사코드)

```
loop (매 틱, 목표 ≥ 20Hz):
  o   ← sense()                        # 이벤트 구동: 바뀐 것만
  x   ← encode(o)                      # SBV로
  s   ← settle(x, s_prev, budget=τ₁)   # F 최소화: 지각 = 추론
  if residual_F(s) > θ:                # 설명 실패 = 배울 것
      s ← clone_and_write(x, context)  # 1-shot 구조 성장
  π   ← plan(s, C, budget=τ₂)          # G 최소화: 롤아웃 = 연상 체이닝
  act(π.first)
  write_counts(s_prev, a_prev, s)      # 국소 카운트 갱신 — 이게 학습의 전부

when idle (수면 패스):
  merge_clones_by_BMR()                # 압축 = 일반화
  extract_recurring_subgraphs()        # 스키마 → 라이브러리
  decay_unused()                       # 예산 내 용량 유지
```

훈련 스크립트가 없다. 위 루프가 첫 부팅부터 폐기까지 도는 전부다.

### 4.8 기존 시스템과의 관계 — 조합이 아니라 특수화

| 기존 시스템 | MONAD에서의 위치 |
|---|---|
| AXIOM | 연속 슬롯 도메인에서의 특수 사례 (혼합성분 성장 = 클론 성장) |
| CSCG | 순수 시퀀스 도메인에서의 특수 사례 |
| NARS/ONA | 예산 제어(AIKR)의 특수 사례 — 우리 τ 예산 정책 |
| Hopfield/VSA | 표현층 연산 그 자체 |
| DreamCoder | 수면 패스의 특수 사례 (라이브러리 성장) |
| TRM/CTM | 시간층의 특수 사례 (반복=깊이), 단 transductive 편법 제거 |
| Episodic Control | 클론 그래프의 퇴화 사례 (전이 없는 쓰기 전용) |

부품을 가져다 잇는 것이 아니다. 정확히 말하면(v0.2) MONAD는 **"이 인지 현상들이 단일 기질의 그래프 추론·구조 갱신·압축만으로 구현될 수 있다"는 가설을 검증하는 프로젝트**이고, 위 표는 가설이 성립할 때 각 기존 시스템이 차지하는 자리다 — 아직 정리(theorem)가 아니라 검증 대상이다. 이것이 "노가다 조합 금지" 요구의 설계적 답이다.

---

## 5. 시스템 사양 (v1 타깃)

| 항목 | 사양 |
|---|---|
| 하드웨어 최소 | x86-64/ARM 4코어, RAM 8GB(엔진 점유 ≤ 4GB), GPU 불필요, 오프라인 완전 동작 |
| 원자/벡터 | `Atom{id, value, evidence, t}` — id: SBV D=16,384bit(2KB, 블록 128×128, 활성 128bit), value: 선택적 연속 벡터 |
| 그래프 용량 | 클론 ≤ 1M(≈2GB) + 희소 카운트, 수면 압축으로 상한 유지 |
| 틱 지연 | ≤ 50ms (지각 정착 ≤ 10ms, 계획 32롤아웃×깊이8 ≤ 30ms) — anytime이라 초과 시 품질만 저하 |
| 코어 구현 | Rust + SIMD(AVX2/NEON) 비트 커널, Python 바인딩(연구·통합용) |
| 저장 | 단일 파일 스냅숏(그래프+라이브러리), mmap 로드, 종료-재시작 후 기억 연속 |
| GPU(선택) | 있으면 계획 롤아웃 병렬화에만 사용 — 기능 아닌 속도 옵션 |
| 라이선스 | 코어 MIT. AXIOM 코드는 비상업 라이선스이므로 **논문 기반 클린룸 구현**(코드 미참조). VERSES 특허 출원 범위 법률 검토 1회 필수 |

---

## 6. 마일스톤 — 각 단계에 킬 기준 포함

| 단계 | 기간 | 목표 | 통과 기준 | 킬/피벗 기준 |
|---|---|---|---|---|
| **M0 기질** | ~10주 | 인지 원자 커널 + 클론 그래프 + **Bounce Test** | **Bounce Test Phase 1~3 통과**(신규 객체·외관 변화로 스키마 전이, 내부 스키마를 사람이 읽는 형식으로 덤프) + Pong급 ≤ 5×10³ 스텝(부차) + 재시작 후 기억 유지 | Phase 2(신규 객체 전이) 실패, 또는 스텝이 AXIOM의 3배 초과 시 표현·추상층 재설계 |
| **M1 지속** | +10주 | Gameworld급 10개 환경 순차 학습 | 유지율 ≥ 90%(재학습 0), 평균 습득 ≤ 10⁴ 스텝, RAM ≤ 4GB 유지 | 유지율 < 70%면 BMR 정책 결함 — 수면층 재설계 |
| **M2 추상** | +12주 | 수면 스키마 라이브러리 + ARC-AGI-1 | **사전학습 0·귀납**으로 ≥ 30%, 과제당 ≤ 5분/CPU. 스키마 재사용률 측정 공개 | < 15%면 라이브러리 탐색 가설 기각 — 열역학적 탐색(EBM식)으로 피벗 |
| **M3 접지 언어** | +12주 | 언어 = 세계 그래프 위 시퀀스 스키마 | 환경 내 자연어 명령·질의 수행(예: "빨간 것 왼쪽으로") — 채팅 아님 | 접지 실패 시 언어를 v2로 이월(제품 가치는 M1·M2로 성립) |
| **M4 개방 환경** | +12주 | Crafter/Craftax급 개방 과제를 CPU 온라인 학습 | DreamerV3 대비 1/10 샘플로 동급 점수대 진입 | — (v2 범위 협상) |

각 마일스톤 산출물에는 **어블레이션 리포트 의무**(클론 성장 끄면? 수면 끄면? 정보 이득 항 끄면?) — HRM의 교훈: 서사가 아니라 어블레이션이 성능의 실체를 말한다.

**Bounce Test**(v0.2, M0 관문): 반사 규칙 관찰(5회) → ① 처음 보는 객체 ② 외관이 바뀐 벽 ③ 중력 추가에 대한 예측 전이·스키마 개정을 검사하는 MONAD-네이티브 지능 시험. "경험을 저장하는 시스템이 경험을 이해하는 시스템이 되는가"를 최초로 증명하는 관문이다. 정의: 개발계획 D1.

### 스케일링 가설 (조사 §9-④에 대한 응답)
LLM의 스케일 축은 FLOPs×데이터다. MONAD의 스케일 축 가설: **능력 ∝ 저장 구조 수 × 스키마 재사용률** (연산이 아니라 기억이 병목). M1부터 "클론 수 대 습득 속도" 곡선을 상시 계측해 이 가설 자체를 공개 검증한다. 성립하면 이 체급 최초의 스케일링 법칙이고, 기각되면 그 데이터가 다음 설계의 근거다.

---

## 7. 제품 형태와 사용자

| 단계 | 형태 | 사용자 | 가치 |
|---|---|---|---|
| v1 | Rust 코어 + Python SDK + 로컬 데몬(`monadd`) | 연구자·개발자 | "노트북에서 배우는 에이전트"를 pip 한 줄로 |
| v1.5 | 게임/시뮬레이션 어댑터(Gym 호환), 관측 대시보드(믿음·계획·스키마 실시간 시각화) | 동일 + 교육 | 모든 내부가 보이는 유리상자 지능 |
| v2 | 온디바이스 상주 에이전트(개인 루틴·환경을 학습하는 로컬 비서), 로봇/IoT 임베딩 | 일반 사용자·엣지 | 클라우드 0, 개인정보 이탈 0, 쓸수록 그 사용자에게 적응 |

차별점 한 줄: **"클라우드의 남의 지능을 빌리는 게 아니라, 내 기기에서 나를 배우는 내 지능."**

---

## 8. 리스크 대장

| 리스크 | 등급 | 대응 |
|---|---|---|
| 지각 경계(픽셀→SBV)가 병목 | **높음** | §4.6 단계적 방침 + M0를 저차원 지각 환경으로 설계해 코어 검증과 분리 |
| 바인딩 용량 한계(중첩 노이즈) | 중 | 블록 코드 채택 + cleanup 메모리 + 용량 상시 계측 |
| 구조 폭발(클론 무한 성장) | 중 | 수면 BMR + 예산 상한 + decay. M1 킬 기준으로 조기 검출 |
| 이산 세계모델의 정밀도 상한 | 중 | 연속 잔차를 클론 내 로컬 선형모델로 보완(AXIOM sLDS 동형) |
| "동급 성능" 미달 리스크 | 중 | §3 성능 계약이 방어선 — 이기는 축을 명시했고 각 단계 킬 기준 보유 |
| VERSES 특허 저촉 | 낮~중 | 클린룸 + 착수 전 법률 검토 1회 |
| 언어 접지 실패 | 중 | M3 이월 옵션 — 제품 성립은 M1·M2에 둔다 |

---

## 9. v0.1 승인 요청 사항

1. **M0 착수 승인** — Rust SBV 커널 + 클론 그래프 + Pong급 환경 (8주)
2. 성능 계약(§3)의 3축 정의 합의 — 특히 "언어는 v1 비목표" 확정
3. VERSES 특허 검토 진행 여부

---

*이 PRD는 2026-08-11의 40여 시스템 전수조사에 근거하며, 모든 수치 목표의 출처는 근거 문서에 있다. 설계의 신조: 부품을 잇지 말고, 부품들이 도출되는 하나의 수학을 구현하라.*
