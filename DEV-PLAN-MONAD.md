# DEV PLAN — MONAD

**개발계획 v1.0 · 2026-08-11 · 기준 문서: [PRD-MONAD-v0.1.md](PRD-MONAD-v0.1.md) (v0.2)**

> **진행 현황 (2026-08-11 실행)**: **M0 엑싯 게이트 5/5 통과** — [M0-REPORT.md](M0-REPORT.md) ·
> [LAB-NOTEBOOK.md](LAB-NOTEBOOK.md) 참조.
> 완료: E1 ✅ · A1 ✅(초과: 128B 표현, 131M쌍/s) · A2 ✅(8.6µs@1M) · A3 ✅ · B1~B5 ✅
> (믿음 빔·문맥 클론·재정위 인내·출발귀속 분화·타부) · C1 ✅(+꿈 EM 추가 — 계획 편차,
> 보고서 §3) · C2-S ✅(복구율 100%, 8/8) · C2 ✅(Rust 이식) · D1 ✅(Bounce 100%) ·
> D2 ✅(미로 3원·바운스 어블레이션) · M0 게이트2 ✅(Pong 5/5).
> M1 선행: C4 ✅(규칙 개정, 회복 8회≤20) · 연속학습 예비 ✅(유지율 100%) · 10환경 본게이트 실행 중.

---

## 0. 비평 반영 내역 (GPT 총평 → 설계·계획 변경 5건)

| # | 지적 (GPT 총평) | 반영 |
|---|---|---|
| 1 | "모든 것이 SBV"는 위험 — 연속값 강제 이진화는 역효과 (§12) | 자료형을 **인지 원자** `Atom{id: SBV, value: 연속벡터?, evidence, t}`로 정교화. PRD §4.1 개정 완료 |
| 2 | 진짜 AGI 알고리즘은 **구조 추상화**(무엇을 변수화할 것인가)일 가능성 (§13) | C2(스키마 추출)를 크리티컬 패스로 승격, **W1부터 선행 스파이크**. 실패 기준 명시 |
| 3 | 그래프 추론의 조합 폭발 — "무엇을 생각하지 않을지"가 필수 (§14) | B5(주의/예산)를 독립 워크패키지로 승격. 지능 = 기억+추상화+주의 3축을 계측 대상으로 |
| 4 | "특수 사례 도출"은 아직 정리가 아님 — 가설로 표현하라 (§18) | PRD §4.8을 "단일 기질 가설 검증"으로 개정 완료 |
| 5 | M0는 Pong 점수가 아니라 **전이 시험**으로 증명하라 (§19, §21) | **Bounce Test**를 M0 관문으로 채택(D1). Pong은 부차 기준으로 강등. M0 8→10주 |

**이 계획의 단 하나의 증명 목표** — *"경험을 저장하는 시스템이 경험을 이해하는 시스템으로 발전할 수 있는가."* 모든 패키지는 `경험 → 구조 → 압축 → 추상화 → 전이` 사슬의 어느 고리인지가 명시된다.

---

## 1. 실행 원칙

1. **크리티컬 패스 우선**: C2(구조 추상화)가 전체 프로젝트의 성패다. 가장 먼저 위험을 소진한다(W1 스파이크). 나머지 트랙은 C2가 올라설 무대다.
2. **유리상자 의무**: 모든 패키지의 산출물은 내부 상태(믿음·클론·스키마·증거 카운트)를 사람이 읽는 형식으로 덤프할 수 있어야 완료로 친다.
3. **어블레이션 없이 주장 없음**: 기능 X를 넣었으면 "X 끄면 얼마나 나빠지는가"가 리포트에 있어야 한다.
4. **매 패키지에 DoD(완료 정의)와 킬/피벗 기준**: 통과 못 하면 다음으로 넘어가지 않는다.
5. **코어 무경사 원칙 불변**: 어떤 패키지도 코어 루프에 백프롭·리플레이 버퍼를 들이지 않는다.

---

## 2. 워크패키지

### A-트랙 · 기질 (Substrate) — 사슬의 "구조" 저장소

**A1. 인지 원자 & SBV 커널** `Rust`
- 목표: `Atom{id, value, evidence, t}` 타입 + SBV 4연산(bind/bundle/permute/hamming) SIMD 구현(AVX2/NEON).
- 산출물: `monad-core` 크레이트, 벤치 스위트, property 테스트(bind 역연산 복원, bundle 교환성).
- DoD: ① 해밍 거리 1M쌍/초/코어 이상(AVX2 기준 목표치, 미달 시 D·블록 구조 재조정) ② 중첩 용량 곡선(bundle K개→복원율) 문서화 ③ value 필드 유무가 id 대수에 영향 없음 검증.
- 의존: 없음. 기간: 2주.

**A2. 연상 메모리 (Associative Store)**
- 목표: 활성비트 역색인 + top-k 해밍 랭킹. cleanup(최근접 인출)의 물리적 구현.
- DoD: 1M 원자에서 top-8 조회 < 1ms(4코어), 리콜@8 ≥ 99%(합성 노이즈 20% 조건).
- 의존: A1. 기간: 1.5주.

**A3. 클론 그래프 저장소 (World Graph Store)**
- 목표: 노드=원자, 간선=`N(s,a→s')` 희소 카운트. append 로그 + mmap 스냅숏, 종료-재시작 연속성.
- DoD: kill -9 후 재기동 시 그래프 무손실 복구, 스냅숏 로드 < 2초(클론 10만 기준), RAM 계측 훅 내장.
- 의존: A1, A2. 기간: 1.5주.

### B-트랙 · 각성 루프 (Wake) — 사슬의 "경험" 수집기

**B1. 인코더 v0 (지각 경계)**
- 목표: 저차원 게임 환경(그리드/스프라이트) 전용 — 이벤트 구동(변화 픽셀만) + 랜덤 투영 + k-WTA 희소화 → Atom. 학습 없는 고정 인코더.
- DoD: 동일 장면 반복 시 코드 안정성(해밍 자기거리 ≤ 5%), 다른 객체 간 분리도(거리 분포 이봉성) 리포트.
- 의존: A1. 기간: 1주. *주의: v0는 의도적으로 저차원. 픽셀 일반화는 M2 이후(PRD §4.6).*

**B2. 정착 (Settle = 지각 추론)**
- 목표: 관측 원자 + 직전 상태 → 그래프 위 국소 메시지 패싱으로 현재 상태 믿음 정착(F 최소화), 예산 τ₁.
- DoD: 노이즈 30% 관측에서 참상태 복원율 ≥ 95%(합성 HMM 세계), 반복 횟수↑ → F 단조 감소 곡선 확인.
- 의존: A2, A3, B1. 기간: 2주.

**B3. 클론 성장 & 쓰기 (구조 학습)**
- 목표: 잔여 F > θ → 새 클론 할당(문맥 분화) + 전이 카운트 1-shot 갱신.
- DoD: **CSCG 논문의 앨리어싱 미로 재현** — 같은 관측·다른 문맥이 다른 클론으로 분화되고 최단경로 계획이 가능함. 클론 수 폭주 없음(θ 민감도 스윕 문서화).
- 의존: B2. 기간: 2주. *이 패키지가 "경험→구조"의 최초 증명이다.*

**B4. 계획 (EFE 롤아웃)**
- 목표: 연상 체이닝 롤아웃 + G 평가(실용 가치 + 정보 이득), anytime.
- DoD: ① 선호 상태 도달 과제 통과 ② **정보 이득 항 어블레이션**: 끄면 미탐험 영역 방문율이 유의하게 하락함을 증명 ③ 롤아웃 예산 2배→성능 향상 곡선(anytime성).
- 의존: B3. 기간: 2주.

**B5. 주의/예산 (AIKR — 무엇을 생각하지 않을 것인가)**
- 목표: ① 활성 부분그래프 제한(현재 문맥 반경 r 이내만 정착 대상) ② 롤아웃 조기 가지치기(G 하위 후보 폐기) ③ 틱 예산 초과 시 품질 강등 정책.
- DoD: 예산 50% 절감 시 과제 성능 열화 ≤ 10%(우아한 저하), 조합 폭발 스트레스 테스트(분기 계수 20 환경)에서 틱 ≤ 50ms 유지.
- 의존: B4. 기간: 1.5주(M0에서는 최소 구현, M1에서 고도화).

### C-트랙 · 수면/추상 (Sleep) — 사슬의 "압축→추상화" 엔진 · **크리티컬 패스**

**C2-S. 구조 추상화 스파이크** ⚠️ **W1 즉시 착수, 전 트랙 중 최우선**
- 목표: "어떤 슬롯을 변수화할 것인가"의 알고리즘 후보 검증. 접근: 반복 부분그래프 마이닝(모티프 해싱) → **anti-unification**(사건들의 최소 일반화) → 변수화 채택 기준 = **MDL 압축 이득**(스키마 도입으로 그래프 서술 길이가 줄면 채택, 반례는 카운트로 벌점).
- 환경: 합성 규칙 세계(색·모양·속도 무관 반사 규칙, 색 의존 규칙 등 정답 스키마를 아는 생성기).
- DoD: 정답 스키마 복구율 ≥ 70%(스파이크 기준), 과변수화/과특수화 비율 리포트.
- **킬 기준: 2주 내 복구율 < 50%면 접근 변경**(후보: 확률적 스키마 — 변수화를 이산 결정이 아니라 슬롯별 일반화 분포로; 또는 EBM식 열역학 탐색).
- 의존: 없음(그래프는 목업으로 대체 가능 — A트랙과 병행). 기간: 2주.

**C1. BMR 병합·가지치기**
- 목표: 증거 적은 클론 병합, 저사용 간선 감쇠 — 망각 없는 용량 관리.
- DoD: 병합 후 과제 성능 유지(±3%), 그래프 크기 ≥ 30% 축소(포화 환경 기준). M1 유지율 시험의 전제.
- 의존: B3. 기간: 1.5주.

**C2. 스키마 추출 v1 (스파이크 결과의 제품화)**
- 목표: 수면 패스에서 C2-S 알고리즘을 실그래프에 적용, 스키마를 라이브러리에 등재. 사람이 읽는 덤프 형식 확정:
  ```
  Schema #17
  IF   moving_entity(X) ∧ approaching(X, rigid_boundary(Y))
  THEN velocity_component(X, normal(Y)) reverses
  evidence=8  counterexamples=1  confidence=0.84
  ```
- DoD: Bounce Test 환경에서 위 형식의 반사 스키마가 **자동으로** 생성됨.
- 의존: C2-S, C1, B3. 기간: 2주.

**C3. 라이브러리 & 전이 (재사용)**
- 목표: 새 상황에서 스키마 매칭(bind/unbind로 변수 채우기) → 정착·계획이 스키마를 우선 사용.
- DoD: **Bounce Test Phase 2·3 통과**(아래 D1). 스키마 재사용률 계측기 내장.
- 의존: C2. 기간: 1.5주.

**C4. 스키마 개정 (반증 학습)** — M1 범위
- 목표: 반례 누적 → 스키마 신뢰도 하향 → 조건 세분화(중력 추가 시 "반사"가 "수직 성분 반사 + 가속"으로 분화).
- DoD: **Bounce Test Phase 4 통과** — 규칙 변경 후 N=20 사건 이내 스키마 개정.
- 의존: C3. 기간: 2주.

### D-트랙 · 증명 (Proof) — 사슬의 "전이" 검증기

**D1. Bounce Test 하네스** — **M0의 관문** (GPT §19 설계 채택)
- 환경: 2D 박스, 객체(모양·색·크기·속도 파라미터), 벽(외관 파라미터), 중력 토글.
- 절차와 통과 기준:
  - **Phase 1 (관찰)**: 공-벽 충돌 5회 시연 → 반사 스키마가 라이브러리에 생성되는가.
  - **Phase 2 (신규 객체 전이)**: 처음 보는 삼각형 투척 → 충돌 후 속도 예측 오차 ≤ 허용치. **여기 실패 = M0 킬.**
  - **Phase 3 (외관 불변성)**: 벽 색·질감 변경 → 예측 유지되는가(스키마가 외관이 아니라 역할에 걸려 있는가).
  - **Phase 4 (규칙 개정, M1)**: 중력 추가 → 20 사건 내 스키마 개정.
  - 전 Phase에서 내부 스키마 덤프를 리포트에 첨부(유리상자).
- DoD: 자동 실행 + JSON/사람용 리포트 이중 출력, 어블레이션 조합(수면 off/정보이득 off/클론 off) 자동 비교 포함.
- 의존: 환경은 독립 구현 가능 — **W3부터 병행 제작**. 기간: 2주.

**D2. 어블레이션 하네스**
- 목표: 플래그 하나로 구성요소 on/off 매트릭스 실행, 표 자동 생성. 모든 마일스톤 리포트의 공통 기반.
- 의존: B4. 기간: 1주(D1과 공유 코드).

**D3. 외부 벤치마크**
- M0: Pong급(부차 기준 ≤ 5×10³ 스텝). M1: Gameworld류 10종 순차 — 유지율 ≥ 90%, 평균 습득 ≤ 10⁴ 스텝, RAM ≤ 4GB. 비교선: AXIOM 공표치·DreamerV3 공표치(재실행 아닌 문헌 대조로 시작).
- 의존: B5, C1. 기간: M1 기간 내 상시.

**D4. 스케일링 계측 (상시)**
- 목표: PRD의 스케일링 가설(능력 ∝ 구조 수 × 스키마 재사용률) 검증 데이터 — 클론 수·스키마 수·재사용률 vs 신규 환경 습득 속도 곡선을 모든 실험에서 자동 기록.
- 의존: A3, C3. 기간: 0.5주(계측 훅) + 상시.

### E-트랙 · 제품/인프라

**E1. 빌드·CI** — Rust 워크스페이스, Win/Linux/mac 크로스빌드, AVX2/NEON 런타임 디스패치, 벤치 회귀 게이트. (1주, W1 병행)
**E2. Python SDK** — `pip install monad-core`(pyo3), Gym 스타일 API: `agent.step(obs) → action`, `agent.dump_schemas()`. (1주, M0 말)
**E3. 유리상자 대시보드** — 믿음·계획 롤아웃·스키마 라이브러리 실시간 뷰(로컬 웹). M0에서는 텍스트 덤프로 대체, v1.5에서 본격화. (M1)
**E4. 환경 어댑터** — Bounce Test 환경 + Pong급 + Gameworld류 재구현(라이선스 회피 자체 제작). (M0~M1 분산)

---

## 3. 일정 — M0 (10주)

| 주차 | A (기질) | B (각성) | C (수면/추상) ⚠️ | D/E (증명/인프라) |
|---|---|---|---|---|
| W1–2 | A1 원자·SBV 커널 | — | **C2-S 추상화 스파이크** (최우선) | E1 CI |
| W3–4 | A2 연상 메모리 → A3 그래프 저장소 | B1 인코더 v0 | C2-S 결과 판정 → 접근 확정 | D1 환경 제작 시작 |
| W5–6 | — | B2 정착 → B3 클론 성장 (CSCG 미로 재현) | C1 BMR 설계 | D1 하네스 |
| W7–8 | — | B4 계획(EFE) + B5 최소 | C1 구현, C2 v1 착수 | D2 어블레이션 |
| W9–10 | 통합·성능 튜닝 | 통합 | **C2 v1 + C3 전이** | **D1 Bounce Test 시행** + 리포트, E2 SDK |

**M0 엑싯 게이트** (전부 충족해야 M1 진입):
1. Bounce Test Phase 1–3 통과 + 스키마 덤프 공개
2. Pong급 온라인 학습 ≤ 5×10³ 스텝 (CPU 실시간)
3. CSCG 앨리어싱 미로 재현 (B3 DoD)
4. 어블레이션 리포트 (수면/정보이득/클론 각각 off)
5. kill -9 → 재기동 기억 연속성

**M0 킬 트리거**: C2-S 2주 실패(→접근 변경 후 1회 재시도, 재실패 시 아키텍처 재설계) · Phase 2 실패 · 습득 스텝 AXIOM 3배 초과.

**M1 (+10주)**: C4 스키마 개정, B5 고도화, C1 대규모화, D3 10환경 연속 학습(유지율 ≥ 90%), E3 대시보드, D4 스케일링 곡선 첫 공개. 이후 M2~M4는 PRD §6 유지.

---

## 4. 실행 체제 (1인 + 에이전트 팀 기준)

| 스트림 | 방식 |
|---|---|
| A·E (기질·인프라) | 사양이 명확 → 코딩 에이전트 병렬 위임 적합. DoD가 자동 판정 가능하도록 벤치를 먼저 작성 |
| B (각성 루프) | 수학이 얽힘 → 순차 개발, 패키지당 합성 환경 테스트 우선 |
| C (추상화) | **연구 성격 — 운영자 직접 관장 권장.** 에이전트에게는 실험 실행·스윕만 위임, 알고리즘 판단은 단일 스레드로 |
| D (증명) | 하네스 코드는 위임, 리포트 해석·킬 판정은 운영자 |

주간 리듬: 월 — 주간 목표·킬 기준 확인 / 금 — DoD 판정·어블레이션 리뷰. 모든 실험은 D4 계측 훅을 지나야 하며, 계측 없는 실험 결과는 무효.

---

## 5. 리스크 워치리스트 (계획 관점 재정렬)

| 순위 | 리스크 | 조기 신호 | 예방선 |
|---|---|---|---|
| 1 | **C2 추상화 실패** (변수화 결정 불능) | 스파이크 복구율 < 50% | W1 선행 스파이크 + 대체 접근 2종 예약 |
| 2 | 조합 폭발 | 분기 20 환경에서 틱 > 50ms | B5 조기 최소 구현, 스트레스 테스트 상시 |
| 3 | 클론 폭주 (θ 민감) | 클론 수가 사건 수에 선형 비례 지속 | B3 DoD에 θ 스윕 포함, C1 조기 투입 |
| 4 | SBV 용량 병목 | bundle 복원율 조기 붕괴 | A1 용량 곡선을 첫 산출물로 |
| 5 | 지각 경계에서 시간 유실 | B1이 1주 초과 | v0 범위 고정(저차원 전용), 픽셀 야심 금지 |
| 6 | "Pong 점수 유혹" — 벤치 최적화로 회귀 | 스키마 재사용률 정체인데 점수만 상승 | 엑싯 게이트 1번(Bounce Test)이 항상 우선 |

---

## 6. 이번 주 착수 목록 (W1)

1. `monad/` Rust 워크스페이스 생성, `monad-core` 스캐폴드 + CI (E1)
2. A1: SBV 타입·bind/bundle/permute/hamming + property 테스트 → 용량 곡선 벤치
3. **C2-S: 합성 규칙 세계 생성기 + anti-unification/MDL 프로토타입** (Python으로 빠르게 — 스파이크는 속도가 생명)
4. D1: Bounce Test 환경 사양서 1페이지 (파라미터 공간·허용 오차 정의)
5. 판정 캘린더 고정: W2 말 = C2-S 1차 판정일

---

*이 계획의 신조: 가장 어려운 것(추상화)을 가장 먼저, 가장 정직한 시험(전이)을 관문으로. 점수는 부산물이다.*
