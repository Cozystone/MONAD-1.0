# MONAD 실험 노트

측정된 사실만 기록한다. 각 항목은 재현 명령을 포함한다.
기준 문서: [PRD](PRD-MONAD-v0.1.md) · [개발계획](DEV-PLAN-MONAD.md)

**전체 사슬**: `경험 → 구조 → 압축 → 추상화 → 전이`
각 워크패키지가 어느 고리인지 표시한다. 사슬이 끊기면 그 지점이 다음 작업이다.

---

## W1 — 2026-08-11

### E1 · 워크스페이스 ✅
- `monad/` Cargo 워크스페이스 + `monad-core` 크레이트. **외부 의존성 0**(std만).
- 테스트 19개 통과. AVX2 런타임 디스패치 + 스칼라 폴백, 둘의 결과 일치를 테스트로 고정.
- 재현: `cargo test --release`

### A1 · 인지 원자 + SBV 커널 ✅ (사슬: 기질)

**설계 개선 — PRD 대비 16배 압축.** PRD는 SBV를 밀집 비트맵 2KB로 잡았으나,
희소 블록 코드는 블록별 활성 인덱스만 저장하면 `[u8; 128]` = **128바이트**로 충분하다.
부수 효과가 둘 다 유리하다:
- `bind`가 **블록별 모듈러 덧셈** → 근사 없는 완전 가역(테스트 `unbind_is_exact_inverse`)
- `dist`가 128바이트 비교 → AVX2 4회 (2KB popcount 64회 대비 16배 적은 작업)
- 원자 100만 개 = **128MB** (PRD RAM 예산 4GB 안에 여유)

**처리량** (단일 코어, AMD Ryzen 9 9950X3D, AVX2):

| 연산 | 처리량 | DoD | 판정 |
|---|---|---|---|
| `dist` | **131.5 M쌍/초** | ≥ 1 M/s | 통과 (131×) |
| `bind` | 154.5 M회/초 | — | — |
| `unbind` | 165.7 M회/초 | — | — |
| `bundle.add` | 34.5 M회/초 | — | — |

> 주: 개발기는 고사양이나 SBV 연산은 SIMD 바운드라 저사양 CPU에서도 수십 M/s대가
> 예상된다. 타깃 기기 실측은 M0 말 통합 시 수행한다(미결 항목).

**중첩 용량 곡선** (K개 번들 후 구성원을 무작위 1000개 중 식별한 비율):

| K | 구성원 sim | 잡음 sim | 복원율 |
|---|---|---|---|
| 2 | 0.504 | 0.008 | 100.0% |
| 4 | 0.261 | 0.008 | 100.0% |
| 8 | 0.151 | 0.008 | 100.0% |
| 16 | 0.104 | 0.008 | **98.7%** |
| 32 | 0.070 | 0.008 | 84.2% |
| 64 | 0.048 | 0.008 | 51.2% |
| 128 | 0.034 | 0.008 | 22.9% |

→ **설계 예산 확정**: 한 노드/스키마에 안전하게 중첩할 수 있는 항목은 **16개**(98.7%),
허용 한계 32개. A3의 노드당 중첩 예산과 C2의 스키마 슬롯 수가 이 값에 묶인다.

**연상 인출 잡음 내성** (원본의 일부 블록을 교란한 질의로 DB에서 원본 찾기):

| 교란 블록 | N=10k | N=100k |
|---|---|---|
| 0–112 (≤87.5%) | 100% | **100%** |
| 120 (93.8%) | 94.5% | 60.0% |
| ≥124 | 0% | 0% |

→ **온전한 블록 16개면 10만 개 중에서 원본을 식별한다.** 지각 인코더(B1)가
거칠어도 상태 재인식이 무너지지 않는다는 뜻 — B1의 요구 정밀도를 크게 낮춘다.

**측정 버그 1건 기록**: 최초 구현은 교란할 블록을 중복 허용으로 뽑아, 128개를
교란해도 약 37%가 손대지 않은 채 남았다(coupon collector). 그래서 "128블록 교란에도
100% 복원"이라는 거짓 결과가 나왔다. 서로 다른 블록을 뽑고 값이 반드시 달라지도록
수정한 뒤에야 위의 진짜 절벽이 드러났다. — *유리한 결과가 나오면 먼저 측정을 의심할 것.*

**DoD 3번**: `value` 필드 유무가 `id` 대수에 영향 없음 — 테스트
`value_does_not_affect_id_algebra`로 고정(200회 무작위 검증).

재현: `cargo run --release --bin bench-sbv`

---

### C2-S · 구조 추상화 스파이크 ✅ **통과** (사슬: 압축 → 추상화)

> 프로젝트 최대 리스크. "어떤 슬롯을 변수화할 것인가"를 자동으로 결정할 수 있는가.
> **DoD 복구율 ≥ 70% · 킬 < 50% → 결과 100%.**

**시험대**: 정답 스키마를 아는 합성 규칙 세계 8종. 각 세계가 서로 다른 실패 모드를 겨눈다.

| 세계 | 겨누는 난제 |
|---|---|
| R1 | 무관 슬롯 8개를 전부 변수화할 수 있는가 |
| R2 | 논리곱(각 조건 단독으로는 이득 0)을 발견하는가 |
| R3 | 결과와 85% 상관된 **가짜** 슬롯의 유혹을 견디는가 |
| R4 | 값이 아니라 **관계**(두 슬롯이 같음)를 발견하는가 |
| R5 | 연속량을 임계 술어로 이산화하는가 |
| R6 | 규칙 하나가 아니라 규칙 **집합**을 찾는가 |
| R7 | 일반 규칙과 **예외**를 동시에 유지하는가 |
| R8 | 라벨 잡음 10%에 과적합하지 않는가 |

**판정 기준**(둘의 논리곱): ① 학습에 쓰지 않은 새 사건 전이 정확도 ≥ 98%
② 제약한 슬롯 집합이 정답 인과 슬롯과 **정확히** 일치. 정확도만 높고 엉뚱한
슬롯을 붙들면 그것은 상관이지 이해가 아니므로 실패로 친다.

**결과 — 8/8 복구, 전이 정확도 100%, 시드 5개 반복 40/40.**

**채택 알고리즘**: MDL 압축 이득 + 전방탐색 빔서치 + 예외 정제
- *판정 기준은 MDL 하나*: 스키마는 결과를 기술하는 비트를 줄일 때만 채택. 이 한 기준이
  과일반화(덮은 집합이 불순해짐)와 과특수화(스키마 비용만 늘고 이득 없음)를 동시에 막는다.
- *비트마스크 표현*: 사건 집합을 정수로 표현해 패턴 피복을 AND 한 번으로 계산. 이것이
  수만 회 평가를 Python에서 초 단위로 가능하게 한 실행 가능성의 열쇠.
- *예외 정제*: 규칙이 덮은 범위 **안에서** 다시 압축을 시도해 체계적 반례를 더 구체적인
  스키마로 분화시킨다. 이것이 곧 C4(스키마 개정)의 메커니즘.

**어블레이션 — 세 구성요소 모두 필요함이 증명됨** (구성요소를 빼면 무너지는 세계):

| 구성 | 복구율 | 무너지는 세계 |
|---|---|---|
| 전체 | **100%** | — |
| − 전방탐색 | 79.2% | R7, R8 (단일 시드에서는 R3도: 가짜 상관에 먼저 걸림) |
| − 예외정제 | 87.5% | R7 |
| − MDL (순수 anti-unification) | 75.0% | R5(임계 표현 불가), R8(잡음 1건에 일반화 붕괴) |

**표본 효율 — 규칙 복구에 필요한 최소 사건 수** (시드 10개 중 8개 이상 통과):

| 세계 | R1 | R2 | R3 | R4 | R5 | R6 | R7 | R8 |
|---|---|---|---|---|---|---|---|---|
| 최소 사건 | **16** | 30 | 40 | 25 | 30 | 60 | 200 | 60 |

→ 단일 조건 규칙은 **사건 16개**, 논리곱은 30개, 가짜 상관을 이겨내는 데 40개.
가장 어려운 예외 계층도 200개. 이것이 MONAD의 샘플 효율 주장이 추상화 층에서도
성립한다는 최초의 정량 근거다.

**MONAD 기질로의 이식 경로 확인**: 발견된 제약 집합은 그대로 스키마 하이퍼벡터가 된다 —
`schema = bundle([bind(role, value) for (slot, value) in 제약])`, 변수화된 슬롯은 번들에
넣지 않는다(= '열린 슬롯'은 물리적으로 없음). MDL 채점은 표현과 무관하게 사건 집합 위에서
정의되므로 이식에 영향받지 않고, 제약 검사만 딕셔너리 조회 → `unbind` 후 유사도 비교로
바뀐다. A1의 잡음 내성(87.5% 손상에도 100% 식별)이 이 검사의 신뢰성을 보장한다.
A1 용량 곡선(K=16에서 98.7%)이 **스키마당 제약 수 상한 16**을 준다 — R7의 3제약도 여유.

재현: `python run_spike.py --n 1500` · `python sweep.py`

---

### A2·A3·B1·B2·B3·C1 · 기질~각성 루프~수면 ✅ (사슬: 경험 → 구조 → 압축)

**A2 연상 메모리** — 밴딩 LSH(블록 4개=밴드, 16밴드) + 정확 해밍 랭킹.
100만 원자 top-8 조회 **8.6µs**(DoD 1ms의 1/116), 선형 스캔 대비 2,635배. 20% 잡음 리콜@8 100%.

**A3 클론 세계 그래프** — 노드=원자, 간선=카운트 전이. append 저널 + 원자적 스냅숏.
`kill -9` 상황(플러시 후 즉사) 복구 무손실을 테스트로 고정. 상태 벡터 = bind(지각, 클론태그)라
`unbind`로 언제든 "무엇을 보고 있는 상태인지" 역산 가능(유리상자).

**B1 인코더** — 역할⊗값 중첩, 이벤트 구동(바뀐 항만 재계산, 계측 내장), 연속값은 앵커 보간
벡터 + value 필드 원본 보존. `probe_cat`으로 코드에서 역할값 역질의 가능.

**B2·B3 정착과 성장** — 최종 설계는 4개 부품의 합:
1. **믿음 빔**: 상태를 하나로 단정하지 않고 (상태, 로그확률) 분포를 유지 — 별칭 세계에서
   한 번의 애매함이 치명상이 되지 않는다. `belief_entropy`로 "내가 어디인지 모른다"를 스스로 안다.
2. **문맥 주소 클론**: 설명 실패 시 ctx = 지각ₜ ⊕ ρ(행동⊗지각)ₜ₋₁ ⊕ ρ²(…)ₜ₋₂ 벡터로 연상
   조회 → 재발 문맥은 같은 클론을 되찾는다. 기질 연산 3개만으로 "여기가 어디인가"를 주소화.
3. **재정위 인내(lost_patience)**: 실패 1회는 재정위(믿음을 그 지각의 전 클론으로 확산),
   **지각별** 실패 카운터가 임계를 넘을 때만 구조 성장. 에피소드 경계는 카운터를 지운다.
   이것이 수면이 세운 깨끗한 지도를 각성의 혼란에서 지키는 핵심.
4. **타부**: 목표 추구 중 방금 밟은 (상태,행동)에 감쇠 벌점 — 중복 클론 간선 분산이 만드는
   두 칸 왕복 함정을 스스로 끊는다.

**C1 수면** — 2단 구성:
- **꿈(dream.rs)**: 에피소드 기억 전체에 클론-HMM 전방-후방 추론(EM) + Viterbi 재라벨링
  → 그래프 재건. CSCG(Nature Comm 2021)가 별칭 미로를 푼 그 절차. 경사하강 아님(닫힌형
  베이지안 갱신), 리플레이 버퍼 아님(일화 기억의 수면 재생). 한 밤에 두 번 꾼다(꿈→걷기→꿈).
- **압축(sleep.rs)**: 분할 정련(Moore식 bisimulation — 거친 데서 시작해 행동으로 구별될 때만
  쪼갬) + 와일드카드 병합(안 해본 행동은 반증이 아님) + 증거 기반 가지치기.

**B3 DoD — 별칭 미로 최종 결과** (`cargo run --release --bin maze-test`):

| 구성 | 참칸수 | 각성후 | 수면후 | 순도 | 위치식별 | 길찾기 | 경로비 |
|---|---|---|---|---|---|---|---|
| 4×4·기호4 | 16 | 127 | 57 | 1.000 | 99.9% | **100%** | 1.02 |
| 5×5·기호5 | 25 | 253 | 56 | 1.000 | 100% | **100%** | 1.02 |
| 6×6·기호6 | 36 | 329 | 44 | 1.000 | 100% | **97.5%** | 1.02 |
| 8×8·기호8 | 64 | 564 | 65 | 1.000 | 100% | **92.5%** | 1.07 |
| 6×6·기호3(별칭도 12) | 36 | 136 | **36** | 1.000 | 100% | **92.5%** | 1.33 |

- **시드 5개 안정성: 위치식별 평균 99.9%, 길찾기 5/5 전부 100%**
- 어블레이션(6×6): 전체 = **36노드(참값과 일치)**·100%/100% / 수면 제거 = 354노드·51%/53% /
  분화 제거 = 12노드·37%/8% → 세 부품 모두 필요함이 증명됨
- 학습 곡선: 5,000걸음에 100%/95% 도달
- **B3 DoD(식별≥90% ∧ 길찾기≥90%) 통과. M0 엑싯 게이트 3번(CSCG 재현) 달성.**

**디버깅 기록** (측정이 설계를 몰았다):
- 클론 폭발 1차: "새 노드는 예측이 없어 영원히 놀람" → 예측 없음 ≠ 놀람 구분으로 해결
- 클론 붕괴: 만든 클론을 되찾을 방법이 없어 최다증거 하나로 수렴 → 문맥 주소로 해결
- 지도 오염: 꿈 직후 각성이 실패 1회마다 클론 생성 → 재정위 인내로 해결
- 전역 연속 실패 카운터의 함정: 별칭 실패는 반복되지만 연속되지 않는다 → 지각별 카운터
- 길찾기 왕복 함정: 목표 추구 중 정보이득 항이 탐색 환상 경로를 만듦(진단 로그로 확인)
  → 선호 설정 시 정보이득 항 차단(EFE 정밀도 가중과 동치) + 타부
- **유리한 결과는 measurement를 먼저 의심**: 잡음 주입 중복 추출 버그(A1 §참조)와 동일 교훈 반복

---

### C2·D1 · 스키마 추출 Rust 이식 + Bounce Test ✅ **M0 관문 통과** (사슬: 추상화 → 전이)

C2-S 스파이크의 알고리즘(MDL 압축 이득 + 전방탐색 빔 + 예외 정제)을 `schema.rs`로 이식.
비트셋 기반, 외부 의존성 0. 단위테스트 3종(무관 슬롯 일반화 / 예외 발견 / 수치 임계) 통과.

**Bounce Test 결과** (`cargo run --release --bin bounce-test`):

빨간 원의 충돌 49회 관찰 → 수면 → 시스템이 자동 생성한 스키마(실제 덤프):
```
Schema #0: IF contact=0    THEN none        [evidence=1151, confidence 1.00]
Schema #1: IF wall_side=2  THEN reverse_vx  [evidence=12,   confidence 0.92]
Schema #2: IF wall_side=1  THEN reverse_vx  [evidence=10,   confidence 0.91]
(그 외) → reverse_vy
```
모양·색·크기·속도·벽무늬가 **전부 스스로 변수화**되고 {접촉, 벽면}만 남았다.

| Phase | 시험 | 결과 |
|---|---|---|
| 1 관찰 | 반사 스키마 생성 + 인과 슬롯만 제약 | ✅ |
| 2 신규 객체 | 처음 보는 파란 삼각형의 충돌 직전 속도 예측 | **40/40 = 100%** |
| 3 외관 불변 | 벽무늬·물체 교체(노란 별) | **40/40 = 100%** |
| 4 (M1 참고) | 중력 추가 | 100% (반사 규칙 자체는 불변이므로 강건성 확인 — 진짜 규칙 *개정* 시험은 M1 C4) |
| 어블레이션 | `--no-schema` (추상화 층 제거) | **100% → 0%** — 전이 성능의 실체가 스키마 층임을 증명 |

### 게이트 2 · Catch-Pong 온라인 학습 ✅

격자 8×6, 패들 폭 3, 사전학습·보상 셰이핑·데모 없음. 보상 배선은 단 한 줄:
잡은 순간의 지각을 선호(prefer_percept). EFE 계획이 나머지를 한다.

| 시드 | 80% 도달 스텝 | 최종 잡기율(마지막 100) |
|---|---|---|
| 1~5 | **1,584 ~ 2,700** (전부 ≤5,000 DoD) | 73~79% |

무작위 기준선: 32~41% (도달 없음). 주기적 꿈(1,200스텝마다)이 지도를 다듬는다.
정직한 기록: 도달 후 정상상태가 ~76%로 내려앉는다(ε=0.02 잔여 탐색 + 잔여 지도 잡음).
M1 안정화 항목으로 이월.

### 각성 루프 최종 메커니즘 (이 과정에서 확정된 설계 발견 3건)

1. **확신-기록**: 미설명 전이도 직전 믿음이 확신 상태(H≤0.5)면 기록한다. 안 하면
   "안 해본 행동의 결과"가 영원히 지도에 안 적혀 탐색이 헛돈다(퐁이 무작위 수준이던 원인).
2. **품질 감시**: 설명이 존재해도 최선 가설의 전이 확률이 지속적으로 낮으면(잔여 F−관측항
   > 0.9비트) 그것은 잡음이 아니라 숨은 문맥의 신호다.
3. **출발 귀속 분화**: 별칭의 증상은 도착 지각의 나쁜 설명이지만 **범인은 출발 상태다**.
   불량 예측을 출발 지각에 적립하고, 넘친 지각을 다음 방문에서 강제 분화한다.
   도착을 갈라봐야 엉뚱한 곳만 쪼개진다(직접 확인).

세 배터리(미로 99.3%/99.0% · 바운스 100% · 퐁 5/5) 가 **하나의 코어, 하나의 기본 설정**으로
동시 성립. 테스트 46개 전부 통과.

---

## M1 첫 시험 — 같은 날 (사슬: 개정 · 누적)

### C4 규칙 개정 ✅ (`cargo run --release --bin revise-test`)

세계의 규칙 자체를 교체했다: 바닥이 접착제로 변함(reverse_vy → stick).

- 교체 직후 3회는 옛 규칙으로 예측(당연) → **4회째부터 stick 예측 성공**, 트레일링 기준
  공식 회복 8회 (DoD ≤20의 절반 이하)
- 최종 라이브러리가 stick을 **기본 결과**로 최대 압축: "접촉 없음→none, 그 외→stick" —
  퇴화한 후기 물리(수직 반동만 남음)의 올바른 최소 서술. 판정 코드가 명시적 스키마만
  세다가 이 압축을 놓친 버그 1건 수정.
- 메커니즘: 최근 사건 창(400틱)에 대한 주기 재유도 — 작업 기억 위의 수면.

### 연속 학습 유지율 ✅ — **파국적 망각이 구조적으로 없다**

같은 기호 어휘를 쓰는 미로 A → B 순차 학습(간섭 유발 설계):

| 시점 | A 식별 | B 식별 | 노드 |
|---|---|---|---|
| A 학습 직후 | 100% | — | 39 |
| B 학습 직후 | — | 100% | **72** (A 위에 B가 자람) |
| A 복귀(재학습 0) | **100%** | — | — |

**유지율 100%.** 가중치를 덮어쓰는 대신 구조가 자라기 때문에(클론+카운트) 새 학습이
옛 지식을 파괴할 물리적 경로가 없다. 가소성 상실(Nature 2024)이 LLM 스케일링의 한계로
지목한 바로 그 속성의 반례를 아키텍처가 기본값으로 갖는다 — M1 본게이트(10환경)의 강한 예고.

### 10환경 연속 학습 1차 시도 ❌ → 설계 발견: **창 없는 꿈은 스케일하지 않는다**

전 생애 에피소드를 매번 전면 재건하는 꿈으로 10환경(같은 기호 어휘 = 최대 간섭)을 돌린 결과:
- 총 3시간 4분(비용 ∝ 생애², 환경 10의 꿈이 30분+)
- 습득이 붕괴: 환경1 99.4% → 환경3부터 40~57%대 — 전역 EM이 방금 배운 환경을
  옛 환경들과 함께 재라벨링하며 뒤섞음
- 측정 설계 결함도 발견: 복귀 측정 중 온라인 학습이 계속되어 "유지율 200%" 같은
  무의미한 수치(빠른 재학습과 유지가 뒤섞임) → 습득·복귀 측정 길이를 동일화로 수정

**해법(생물학이 아는 그것): 응고화 꿈** — `DreamConfig.consume: true`
- 미소화 에피소드(해마 버퍼)만 꿈꾼다 → 비용이 생애가 아니라 최근 경험에 비례
- 산출물을 기존 그래프(피질)에 **비파괴 병합** — 옛 환경 구조를 건드리지 않음
- 기존 구조와의 통합(중복 병합)은 bisim 수면이 맡고, 버퍼는 비운다
- 전면 재건 모드는 단일 환경용으로 유지(미로·퐁은 그 모드가 적합)

수정 후 기존 배터리 전부 무손상 확인(테스트 46 · revise 2/2 · maze 100%/100%).

### 10환경 게이트 반복 4회의 수확과 미해결 지점 (정직 기록)

| 시도 | 변경 | 결과 | 배운 것 |
|---|---|---|---|
| 1 | 전 생애 재건 꿈 | 3시간, 습득 붕괴 | 창 없는 꿈은 비용·간섭 양면에서 스케일 불가 |
| 2 | 응고화 꿈(consume) | 10초(!), 그러나 환경1부터 33% | EM은 초기값 민감 — 재건 모드는 각성 그래프를 초기값으로 쓰고 있었다 |
| 3 | + EM 다중 재시작(우도 선택) | 환경1 100%, 환경2+ 28% | 응고화 후 남은 각성 흔적이 깨끗한 구조와 경쟁하며 정착 납치 |
| 4 | + 해마 흔적 은퇴 + 최근성 사전분포 | 환경1 99.4%·43노드, 환경2+ ~49% | 단일 환경 동등성 확보. 다환경 잔여 원인 특정(아래) |

**미해결 잔여 원인(정확히 특정됨)**: 같은 지각 어휘의 새 환경에 들어서면, 분화가 따라잡기
전까지 각성이 **옛 환경의 피질 클론에 새 환경의 전이를 기록**한다(피질 오염). 지각별 분화는
이를 점진적으로만 교정한다. 근본 해법은 **"내가 어느 지도에 있는가"라는 상위 잠재변수**
(지도 수준 문맥 추론·계층) — 개발계획이 M1 10주(B5 고도화)에 배정한 바로 그 작업이며,
2환경(A→B→A) 유지율 100%는 현 메커니즘으로도 성립함을 확인했다.

이번 반복으로 **영구 확보된 메커니즘 3종**(전부 기존 배터리 무회귀 확인):
① 응고화 꿈 — 비용이 생애가 아니라 최근 경험에 비례, 해마→피질 병합
② 해마 흔적 은퇴 — 각성의 임시 클론은 응고화 후 제거(fresh_clones)
③ 재정위 최근성 사전분포 — "방금 전까지 있던 세계" 가중

### 지도 수준 문맥 추론(계층) 구현 — 반복 7회의 전 기록 (M1 본작업, 진행 중 동결)

"내가 어느 세계에 있는가"를 1급 잠재변수로 구현했다: 노드→지도 배열, 지도별 응고화 꿈
분리, 그룹 제한 bisim, 궤적 생존율 기반 재정위(`map_score`), 신생 지도 보호, 이중 확인
개설. **계측이 이론을 다섯 번 뒤집었다** — 전 과정을 남긴다:

| # | 가설/변경 | 계측 결과 | 발견 |
|---|---|---|---|
| 5 | 지각 수준 정합성으로 지도 판정 | 전환 0회 | 같은 어휘 세계의 지각 수준 간선 커버리지는 짙다(우연 일치 60~80%) — 판별력 없음 |
| 6 | 불량률 트리거(임계 0.55→0.30) | 전환 0회 | **빔 추적은 오답 지도에서도 건강해 보인다**(8가설 중 하나는 늘 살아남음). 그리고 꿈 전의 각성 그래프는 옳은 세계에서도 불량률 ~1.0 — 틱 품질은 세계 전환이 아니라 그래프 파편화를 측정한다 |
| 7 | 궤적 생존율 트리거 + 검사 폭 3 | 지도 생성 시작(환경당 1개!) | 폭 W 사슬의 오답 지도 우연 생존율 ≈ 1-(5/6)^W: W=16이면 0.95(무용), W=3이면 0.42 — 판별력은 폭이 결정한다 |
| 7' | 동일 구성의 부작용 | 환경1이 t=3000에 자가 분열 | **생존율 검사는 응고된 구조를 전제한다** — 꿈 전의 파편 그래프는 자기 세계도 못 알아본다 |
| 8 | 주기 응고(1500틱)로 전제 충족 | 지도 3개에서 응집 정지, 지도0 비대(930노드) | 잦은 응고×감지기×병합의 상호작용이 미해결 — EM 사본 중복이 bisim 병합을 빠져나감(원인 미규명) |

**동결 상태**: `Config.map_inference`(기본 꺼짐) 뒤로 격리 — 기본 흐름은 M0 검증 구성과
행동 동일(전 배터리 복원 확인). continual-test만 켠 채 실험 계속 가능.

### M1 잔여 항목 3건 처리 (같은 세션 연장)

**① Pong 정상상태** — 유능선 도달 후 ε=0 잠금을 시험: 정상상태 72~83%로 **변화 없음**.
→ 침하 원인은 탐색이 아니라 계획 기반 제어의 잔여 잡음(중복 클론·선호 경로의 비최적성).
가치 반복 없는 EFE 롤아웃의 한계로 특정 — M1 안정화의 진짜 과제로 재정의.

**② 동일 하네스 기준선: 표 형태 Q-러닝** (`pong-test --qlearn`) — 정직한 결과:

| | 도달 스텝(5시드) | 정상상태 |
|---|---|---|
| MONAD | 1,656~2,658 | 72~83% |
| Q-러닝(α=0.2, γ=0.95) | 1,668~2,094 | **93~99%** |

완전관측 소형 MDP는 표 기반 가치법의 홈그라운드 — 이기는 게 정상이다. 이 대조가
MONAD의 가치 위치를 정밀하게 만든다: **Q-러닝은 별칭 미로(상태 추정)·신규 객체 전이
(스키마)·규칙 개정·재학습 없는 다환경 유지를 구조적으로 못 한다** — 전부 MONAD가
통과한 시험들이다. 반대로 "작고 완전관측인 단일 과제의 정상상태 마진"은 현 MONAD의
약점으로 등재한다.

**③ 저사양 프록시 실측** (2코어 고정, ProcessorAffinity=0x3):
pong 전체 배터리(지각-계획-학습 25,000틱 + 응고화 꿈 20회) = **0.7초 → 평균 26µs/틱**.
저사양 노트북 코어를 5배 느리게 잡아도 ~130µs/틱 — PRD 틱 예산 50ms 대비 **~400배 여유**.
그래프 메모리 0.1~0.4MB(계측기 내장). 실기기 검증은 여전히 타깃 하드웨어 필요(미결 유지).

### 가설 (c) 풀-리빌드 노선 실측 — 2회 실행, 기각 (그러나 최고 기록 경신)

EM 초기화의 clone_ix 필터 버그(후기 환경 클론이 초기값에서 전부 잘림)를 최근성 순위
배정으로 수리한 뒤 재도전. K=64, 실행당 17~23분.

| 실행 | 구성 | 습득(환경1/2/3) | 습득(4~10 평균) | 복귀(1~3) | 복귀(4~10) |
|---|---|---|---|---|---|
| A | bisim ON · 최근성 ON | **98.9/97.1/88.2%** | ~54% | ~50% | 70~95% |
| B | bisim OFF · 최근성 OFF | 78.6/89.3/59.1% | ~44% | ~44% | 72~93% |

**가설 기각 2건**(측정이 이론을 이김, 또): bisim 교차 병합 원인설 — 제거해도 열화 동일,
오히려 초기 환경이 나빠짐(bisim은 단일 환경 중복 청소로 유익). 최근성 원인설 — 꺼도
복귀 저조 동일.

**진짜 원인(두 실행의 공통 프로필로 확정)**: 풀-리빌드는 **매일 밤 전 생애를 원료에서
재유도**한다. 환경 N이 끝날 때마다 환경 1~N-1의 구조가 또 재유도되며, 오래된 환경일수록
데이터 비중이 줄고 재유도 횟수는 늘어 구조가 마모된다 — 복귀 성적이 환경 나이와 정확히
반비례(1~3: ~45~50% ↔ 8~9: 90%+). 비용도 생애 제곱(1,033~1,372초).

**아키텍처 결론**: 증분 응고화(확립 구조 보호 + 새 재료만 꿈)가 유일하게 옳은 노선이다.
그 노선의 미해결 목록이 곧 다음 작업이다:

### 가설 (e) 정렬 병합 구현 + 조합 시험 (시도 11~13)

EpStep에 각성 정착 상태를 기록하고, 응고화 꿈의 산출물을 동시발생(≥사용량 50%)으로
기존 피질에 흡수하는 정렬 병합을 구현. 세 조합의 실측:

| # | 구성 | 환경1 | 환경2 | 4~10 평균 | 관찰 |
|---|---|---|---|---|---|
| 11 | 정렬만(지도 OFF) | 100%·40노드 | 47.9% | ~29% | 단일 환경 완벽 유지. 병목은 응고가 아니라 **각성 추적 혼선**으로 확정 |
| 12 | 정렬+지도 ON | 24.3%(자가분열) | 66.8% | ~28% | 환경2~5에 지도 생성 정상 작동, 환경6에서 지도1 포획·고착 |
| 13 | +주기 응고(1500틱) | 66.1%·373노드 | 43.1% | ~34% | 정렬 임계(50%)가 각성 파편화 탓에 미달 → 흡수 실패 → 재팽창 |

시도 13의 원인: 각성 재파편화로 EM 상태의 정착 이력이 여러 피질 상태에 흩어져
동시발생 절대 과반을 못 넘김. 다음 조정: 상대 다수결(피질 argmax ≥ 사용량 25%).

### 시도 14~19: 오라클 인수분해 → 정밀 원인 규명 → 87.6% 도달 (진행 중)

| # | 변경 | 오라클 평균 습득 | 발견 |
|---|---|---|---|
| 14 | 정렬 25% 완화 | 26.7% | 오병합 유발 — 50%로 원복 |
| 15 | **오라클 지도**(감지기 배제) + 주기 응고 | 53.7% | **병목은 감지기가 아니라 주기 응고 루프** |
| 16 | 오라클 + 밤당 1회 응고 | 62.8% | 유지율 사실상 해결(전 환경 ≥97%) · 환경2 EM 산출물이 지도0으로 누수 발견 |
| 17 | + 정렬을 지도 스코프로 제한 | 65.1% | 환경7 95.8% 완벽 사례 · 남은 것은 꿈별 EM 국소해 복불복 |
| 18 | + 재시작 6/반복 40 | **84.2%** | 환경1 완벽(36노드·유지 100%) · 만성 환경2(51%)만 잔존 |
| 19 | + 재시작 10/50, **밤당 1회 은퇴**(그룹별 은퇴가 뒤 그룹의 정착 id를 무효화하던 버그 수정) | 87.6%→59.8% (환경2 99.3% 해결, 환경5+ 신규 붕괴) | 구성당 시드 1개로 잡음 위 최적화 중임을 인지 — **다중 시드 분산 측정으로 전환** |

### 시도 20: 5-시드 분산 측정 — 통계적 결론 (`sweep-results.txt`)

현 구성(오라클 지도 + 밤당 1회 응고·은퇴 + 지도 스코프 정렬 50% + 재시작 10/50)의
참 분포: **평균 습득 61.0% ± 11 (50~74%), 유지율 98~109%(전 시드 일관 통과)**.

통계가 준 두 가지 확정:
1. 단일 실행에서 본 84~88%는 **잡음 위의 유리한 추첨**이었다(선택 편향). n=1 반복
   최적화의 위험을 스스로 실증한 셈 — 이후 모든 게이트 판정은 다중 시드 의무.
2. 남은 ~30포인트는 튜닝으로 닫히지 않는 **구조적 갭**이다. 후보(우선순위):
   (i) 각성 추적의 측정 오염 — 습득 측정이 은퇴 직후의 재정착 과도기를 포함
   (ii) 꿈별 EM 품질의 잔여 분산 — 재시작 증가의 한계효용 소진
   (iii) 은퇴 타이밍×정렬×그룹 순서의 미규명 상호작용(시도 19의 환경5+ 붕괴)
   그 다음에야 (iv) 감지기 재결합.

**게이트 상태**: 유지율 축은 통과 수준으로 안정화(전 시드 ≥97% 평균). 습득 축이
잔여 연구 과제. 인수분해 하네스(오라클/시드/모드 환경변수)와 20회 시도의 계측
기록이 다음 반복의 출발점이다.

### 시도 21~22: 가설(i) 기각 + 밤별 원격측정 → 스모킹 건

- **가설(i) 워밍업 400걸음**: 5시드 56.3% — 기준 61.0%와 잡음 동일. **기각.**
  측정 과도기는 원인이 아니다.
- **밤별 꿈 원격측정**(`MONAD_TELEM=1`, 시드 2028, 오라클) — 결정적 단서:

| 밤(환경) | 정렬흡수 | 신규 | 다음 밤에 잰 그 지도 크기 | 습득 |
|---|---|---|---|---|
| 1 | 0 | 42 | 37 | 100% |
| 2 | 41 | 41 | 38 | **100%** |
| 3 | 43 | 47 | 36 | 85% |
| 4 | 46 | 31 | **34** | 54% |
| 5 | 25 | 22 | **17** | 31% |
| 6 | 15 | 15 | **10** | 29% |
| 7 | 10 | 6 | **3** | 32% |

**두 개의 사실**: ① 꿈 산출물 자체는 정상 크기(31~47) ② 그러나 환경4부터 응고
지도가 **다음 밤 34→17→10→3으로 붕괴**하고, 측정 중 스크래치 클론이 290+개
재성장(측정 시점 지도별 크기 327~535가 그 증거). 환경2는 동일 조건에서 완벽.

### 시도 23~24: 범인 검거 — 클론 하드캡 연산 순서 버그

- 용의자 1(에피소드 조각화) 기각: 매밤 정확히 2개×6,201걸음 동일.
- 용의자 2(고정 꿈 시드) 기각: 밤별 시드 변주에도 붕괴 곡선 동일.
- **순서 반전 실험(결정타)**: 미로 순서를 뒤집자 "어려웠던" 미로 17·16이 94~96%로
  완벽해지고 붕괴는 여전히 **위치 6+**에서 발생 → 붕괴는 미로가 아니라 **생애 위치**를
  따른다. 그리고 모든 실행 로그에 찍혀 있던 숫자의 정체: **최종 노드 1536 = 6지각 ×
  256 = MAX_CLONES_PER_PERCEPT 하드캡.**
- **기전**: 위치 6쯤 누적 피질(+각 환경의 각성 스크래치 ~400개)이 지각당 256에 도달 →
  ① 각성 분화 거부(에피소드 품질 열화) ② **응고 병합 루프가 캡 검사에서 '곧 은퇴할
  스크래치'까지 세어 신규 응고 거부**(위치 10에서 "신규 0") — 병합→은퇴 순서에서
  캡이 스크래치로 부풀어 있던 연산 순서 버그.
- **수정**: 캡 2048로 상향 + 응고 캡은 피질(비-해마) 클론만 계산.
- **수정 직후 결과(최악 시드 2028)**: **평균 습득 50.8% → 94.4%** — 전 위치 85~100%,
  지도 크기 37~47 정상. 캡 버그가 습득 축의 진범이었음이 확정.
- **신규 잔여 문제**: 후반 학습 환경(8~10)의 복귀만 열화(48~72%) — 유력 용의:
  다음 밤의 측정-트래픽 정렬이 직전 지도를 **다대일 흡수**로 뭉갬(밤 N+1이 지도 N을
  손상; 마지막 지도들만 어린 채로 노출). 다음 실험: 정렬 일대일 제약(피질 노드당
  밤당 1개 EM 상태만 흡수). 5-시드 스위프로 통계 확정 중.

### 시도 25: 캡 수정 5-시드 — **게이트 최초 통과 3/5** 🎉

| 시드 | 습득 | 유지 | 판정 |
|---|---|---|---|
| 2026 | 96.8% | 97.7% | **✅ 통과** |
| 2027 | 96.5% | 98.8% | **✅ 통과** |
| 2028 | 95.3% | 95.7% | **✅ 통과** |
| 2029 | 94.6% | 86.8% (최저 49) | ❌ 유지 미달 |
| 2030 | 93.8% | 83.1% (최저 48) | ❌ 유지 미달 |

습득 축 완전 해결(5시드 전부 ≥93.8%). 잔여는 2시드의 후반 지도 유지 —
일대일 정렬 제약 A/B 스위프 실행 중.

### 시도 26: 일대일 정렬 A/B — **기각** (1/5 통과, 노드 1315~4174 폭증)

다대일 흡수의 대부분은 같은 참 상태의 EM 조각 재흡수(정당). 막으면 미정렬 조각이
신규 클론으로 흘러 그래프 폭증 → 유지율 악화. 원복.

통과/실패 시드의 상관: 실패(2029·2030)의 말기 노드 2176·2757 vs 통과 646~995 —
**마지막 지도들은 응고 밤을 덜 거쳐 약하고, 그 약함이 복귀 스크래치 연쇄를 유발.**
처방: **막차 응고**(학습 단계 종료 후 마지막 잠 1회 — 새 노출 없는 정리이므로
"재학습 없음" 조건과 무관). 스위프 실행 중.

### 시도 27: 막차 응고 — **4/5 통과** (오라클 기준)

| 시드 | 습득 | 유지 | 판정 |
|---|---|---|---|
| 2026 | 95.2% | 93.4% | ✅ |
| 2027 | 97.1% | 100.3% | ✅ |
| 2028 | 94.3% | 81.0% | ❌ (직전 스위프에선 통과 — 시드 내 실행 분산 잔존) |
| 2029 | 95.8% | 100.1% | ✅ |
| 2030 | 94.3% | 96.8% | ✅ |

평균 습득 95.3% · 평균 유지 94.3% — **두 축 모두 평균으로 90/90 상회.**
단 전부 오라클 지도 기준 — 완전 통합(감지기 추론) 스위프가 진짜 마지막 관문.
캡 버그는 감지기 시대의 실패에도 소급 원인이었을 가능성(각성 분화 불능 → 궤적
오염 → 감지 불능) — 수정 후 감지기 재평가 실행 중.

### 시도 29~30: 감지기 이중 분할 진단 + 짓는중-보류 가드 — 가드 무효

- 궤적 진단(시드 2026): 감지기가 **환경마다 지도 2개**를 만든다(신생 보호 3,000틱
  만료 후 첫 응고 9,000틱 사이의 공백에서 자기 지도를 "설명력 없음" 오판).
- 처방: 활성 지도의 피질(비-해마) < 20이면 감지 보류("짓는 중인 집은 검사 안 함").
- 결과: **무효** — 5시드 33~50%로 동일 대역. 이중 분할이 유일 병목이 아니거나
  가드 이후 다른 누수 존재. 가드 적용 후 궤적 진단이
  `detector-trajectory-after-guard.txt`에 기록됨 — **다음 반복은 이 파일부터 읽을 것**
  (환경당 지도 수가 1로 줄었는지 / 줄었는데도 습득이 새는 지점이 어디인지).

### 시도 31: 가드 후 궤적 판독 → **기상 직후 오발** 특정 + 기상 유예

가드 작동 환경(3·6·8)은 70~80%로 회복, 그러나 **밤 직후 32~64틱 창에서 감지기가
오발** — 재정착이 자리잡기 전의 소음을 "세계 변경"으로 읽어 방금 응고한 올바른
지도를 버리고 새 지도를 개설(환경1의 지도1=519가 측정 중 탄생, 측정이 빈 지도에서
진행 → 29%). 처방: **기상 유예 300틱**(`map_check_grace` — 잠에서 깬 직후엔 세계를
의심하지 않는다). 5-시드 스위프 실행 중.

### 시도 32: 기상 유예 — 무효 (38~49%) → **경성 감지기 설계의 체계적 결함으로 결론**

표적 가드 3종(생존율 재설계·짓는중 보류·기상 유예)이 모두 대역을 못 움직였다.
개별 오발 창을 막아도 경성 전환(hard switch)의 본질적 취약성이 남는다: 전환 순간마다
믿음·문맥·궤적이 리셋되고, 한 번의 오판이 지도 하나를 통째로 낭비하며, 오판 비용이
비대칭적으로 크다(올바른 지도를 버리면 회복 불가).

**재설계 사양 — 가설 (d) 소프트 지도 혼합** (다음 반복의 첫 작업):
- 지도 사후확률 P(m)을 매 틱 유지: P(m) ∝ P(m)·P(관측·전이|m) (생존율의 연속 버전)
- 정착·재정위 후보를 P(m) 가중 혼합으로 — 경성 전환·리셋 제거
- 클론 생성 시 소속 지도 = argmax P(m) (또는 임계 미달 시 신규 지도 가설 추가)
- 응고화 그룹핑도 에피소드별 P(m) argmax로
- 장점: 오판이 연속적으로 자기수정, 전환 비용 0, 기존 오라클-검증 계층은 그대로
- 오라클 게이트 통과(4/5)가 상한 성능의 존재 증명이므로, 이 재설계의 목표는
  "감지기를 오라클에 근접시키기"로 명확히 정의됨

### 시도 33~36: 소프트 혼합 v1(전이 우도) → 체인 우도 → **밤의 재분류** (검증 중)

- 소프트 v1(전이 단건 우도): 42~49% — 동일 대역. 원인: 성숙 지도는 외부 전이도
  지각 수준에서 40~80% 우연 설명(시도 5의 밀도 문제 재등장) → 우도비 미약, 표류.
- 체인 우도(지도별 미니 믿음, 폭 3): 40~46% — 여전히 동일 대역.
- **여섯 설계가 같은 45% 대역에 갇힌 최종 해석**: 판별력이 아니라 **에피소드
  소속 오분류** — 라이브 argmax가 흔들릴 때마다 에피소드가 잘못 태그되고, 밤의
  응고가 환경 지식을 여러 지도로 파편화한다(오라클과의 유일한 구조적 차이).
- **아키텍처 응답 — 밤의 재분류**: 각성의 라이브 태그를 버리고, 꿈이 각 에피소드를
  내용(지도별 체인 생존율 ≥0.5 argmax)으로 재분류 후 그룹핑. 미설명 에피소드들은
  그 밤의 "새 세계"로 묶어 지도 개설. "깨어서는 살고, 자면서 어디 있었는지 정리한다."
- 사고 기록: PowerShell 문자열 치환이 wake.rs를 다시 손상(개행 소실 다수 + 한글
  문자열 리터럴 따옴표 파손) — Python 바이트 수술로 복구, 테스트 46 전부 회복.
  **한글 파일에 PowerShell -replace 절대 금지**(2회째 — Edit/Python만 사용).
- 밤의 재분류 진짜 빌드 스위프: 39~42% — 동일 대역(8번째 기각). 피질 기반 신생보호
  (+임계 0.65)도 40% — 9번째 기각.
- **왕관 결과 무회귀 확인**: 오라클은 재분류 포함 전 변경과 함께도 통과
  (96.2/99.9 · 95.2/100.1).
- **결정적 차분 사실**: 같은 시드에서 학습·측정이 순수 무작위 걷기이므로 에피소드의
  (지각,행동) 스트림은 두 모드에서 **비트 동일** — 그런데 95% vs 40%. 재분류 입력이
  같으므로 남는 차이는 ① 정렬 동시발생이 쓰는 ep.state(각성 정착 상태 — 스코핑
  의존) ② fresh_clones 집합 ③ 라이브 스코핑이 만드는 클론 분포뿐. **정렬이 아니라
  각성-스코핑이 만든 구조 차이가 응고 품질을 가른다**는 뜻 — diff-oracle.txt /
  diff-detector.txt (밤별 원격측정 병렬 기록) 생성 중, 다음 반복은 이 diff부터.

### 시도 37: diff 판독 → **모든 감지기를 속인 단일 허점** — 자기설명 루프

디텍터 diff의 사실: **지도가 영원히 1개, 전환 0** — 감지도, 밤 재분류도 새 지도를
만든 적이 없다. 환경2의 에피소드가 정렬흡수 53으로 지도0에 융해.

기전: 새 환경의 각성이 (잘못된) 활성 지도 스코프에 자기 스크래치(신선 클론,
반복 통과로 카운트≥2)를 쌓고, **분류기·우도의 체인이 그 스크래치를 타고 살아남아
에피소드가 자기가 만든 흔적으로 자기를 설명**한다(생존율 ~0.9 → 옛 지도 귀속).
아홉 설계가 동일 대역이었던 단일 원인 — 오라클만 분류가 불필요해 면제였다.

수정(시도 38): **증거는 피질(비-해마)만** — 분류기(episode_score)와 라이브 우도
(soft_map_update)의 체인 확장·재시드에서 fresh 클론 제외. "자기 스크래치로 자기를
설명하는 것을 금지." 5-시드 스위프 실행 중.

### 시도 38 결과: **대역 최초 붕괴** — 습득 40% → 59~67% 🔓

10개 설계에서 요지부동이던 40% 대역이 처음 움직였다(59.0/59.8/65.5/65.8/66.6).
자기설명 허점이 실재 병목이었음이 증명됨. 유지율은 새 역학에서 65~87%로 재조정
필요 — 감지기 계통이 이제 수정에 **반응하는** 국면에 진입했다.

### 시도 39~40: v2 궤적 판독 → 라이브 무절단 — 단조 개선 지속

v2 궤적의 사실: **환경 1~5는 완전 자율로 오라클급**(100/100/96.6/94.7/94.8%,
지도 39~47 정갈) — 감지기 최초 작동. 환경6에서 라이브 사후확률 진동(전환 64회,
신생 하한 0.6 vs 옛 지도의 간헐 체인 생존 0.9)이 에피소드를 46조각(평균 269걸음)
으로 파편화 → 짧은 조각의 분류 불안정 → 지도5 잡탕(743노드) → 이후 포획.

수정(시도 40): **라이브 전환은 에피소드를 자르지 않는다** — 경계 판정 전부 밤으로.
결과: 습득 65.5~71.0 / 유지 83.5~87.6 — 두 축 모두 개선·수렴. 시도 38→40 추이
(습득 40→63→68)가 단조 상승. 좌표: 오라클 95.3/94.3까지 습득 ~20pt·유지 ~5pt.

다음 반복 대기 데이터: `diff-detector-v3.txt` (시드 2028, 무절단 국면 궤적).
좌표: 오라클 95.3/94.3(상한) ↔ 감지기 v2 ~63/±75 — 남은 갭의 후보는
① 재분류 임계 0.65의 새 역학 재보정 ② 복귀 단계의 지도 재인식 정확도
③ 라이브 스코핑의 초기 오귀속 잔여. 사고 2건(파이썬 인라인 개행·중복 패턴
선적용) 모두 스크립트 파일 방식으로 재발 방지.

### 시도 28: 완전 통합(감지기) 5-시드 — 미통과, 그러나 **완전 인수분해 달성**

감지기 모드 습득 34~50% vs 오라클 95%+. **시스템 전 계층이 오라클 하에서 검증된
지금, 잔여 문제는 지도 감지기 하나로 깨끗하게 고립됐다.** 감지기의 파라미터
(생존율 임계 0.65 · 검사 폭 3 · 신생 보호 3000 · 주기 32 · 이중확인 2)는 전부
캡 버그 시대의 왜곡된 역학에 맞춰진 값 — 재보정이 다음 작업. 지도 궤적 진단 실행 중.

**정밀화된 정조준 질문**(정렬 용의는 해소 — 신규 카운트는 학습 그룹의 순수 산출):
**"왜 학습 그룹의 EM 산출 상태 수가 생애에 따라 축소되는가?"**
환경별 학습 꿈의 산출: 42 → 41 → 47 → 31 → 22 → 15 → **6** → 8 → 35 → 7.
입력은 매번 동일한 9,000걸음·같은 6지각·같은 K=16·같은 재시작 10·같은 시드(0xD2EA).
따라서 축소를 만드는 것은 **에피소드 내용의 생애 의존적 변화**뿐이다. 검증 순서:
1. **각성 품질의 생애 열화**: 후기 환경의 학습 중 각성 추적(스코프drift·fail 카운터·
   ctx 충돌)이 나빠져 에피소드가 짧게 조각나는가 — 에피소드 수·평균 길이를 원격측정에 추가
2. **고정 꿈 시드의 함정**: 매밤 같은 0xD2EA + 유사 구조 입력 → 같은 나쁜 분지로
   수렴? 밤마다 시드 변주(seed ^ tick) 실험
3. EM의 usage 집중(Viterbi가 소수 상태에 몰림) — 상태별 usage 히스토그램 원격측정
4. 은퇴 오인 제거(fresh 재매핑 경로) — 응고 직후 노드 수 재검증

**다음 세션을 위한 정리된 가설 공간** (우선순위 갱신):
(a) 응고화 노선에서 주기 응고×감지기 상호작용 규명(EM 사본-bisim 누출, 753노드 재현)
(b) 지도 전환 지연 중 링크 보류 버퍼(이전 지도 오염의 완전 차단)
(d) 감지기 없는 소프트 지도 혼합(지도 사전확률을 생존율로 매 틱 갱신 — 경성 전환 제거)
(e) 신규: 응고화 병합 시 기존 피질과의 **정렬 병합**(새 EM 산출물을 기존 클론에 정합
    시도 후 실패분만 신규 — 중복 자체를 만들지 않는 방향)

### 최종 스위프 (이 시점의 전체 상태)

- 테스트 46개 전부 통과 · 코어+환경 소스 약 218KB(Rust, 외부 의존성 0)
- maze 99.3%/98.0% · bounce 전부 통과 · pong 5/5(최악 4,500) · revise 2/2
- 잔여 변동성 기록: pong 도달 스텝은 시드·실행에 따라 1,500~4,500 범위에서 흔들린다
  (dream 타이밍과 ε 상호작용). 게이트는 5,000이라 여유 있으나 M1 안정화 목록 유지.

---

### 시도 41: v3 궤적 판독 → **비만 지도 양성 피드백** + 셔플 대조 분류(순열 검정)

v3(시드 2028, 무절단 국면)의 사실: 환경 1~5는 82~100%(전환 1→9→17→21로 안정),
환경5에서 라이브 전환 폭발(103), 환경6에서 235로 치솟으며 **지도3이 122→481→
598→712 노드로 폭식** — 이후 신규 지도 개설이 완전 정지(지도 6개 동결·활성 3 고정),
환경 6~10 에피소드 전부가 지도3으로 오분류(습득 28~44%). 평균 62.7/82.1.

**기전 — 비만 지도 양성 피드백**: 약간 과체중으로 태어난 지도(자기 밤 EM이 시끄러웠던
지도3, ll -0.169)는 지각당 클론·간선이 많아 **우연 체인 생존율이 높다**(재시드가 상위
증거 3개에서 다시 살아나는 구조라 크면 유리). 외부 에피소드가 문턱 0.65를 우연히
넘으면 흡수 → 더 비만 → 우연 생존율 더 상승 → 폭주. 자기설명 루프(시도 37)의
지도 수준 변종 — 그땐 자기 스크래치로, 이번엔 자기 비만으로 자신을 설명한다.

**수정 — 셔플 대조(순열 검정)**: 분류 점수 = 실제 점수 − **행동 순서를 흐트린
기준선 점수**(`ep[(t*7+3)%len].action`). 셔플은 진짜 동역학만 파괴하고 지도 크기
효과(우연 체인)는 보존하므로, 차이가 순수 설명력이다. 비만 지도의 이점은 기준선에서
정확히 상쇄된다(자기보정 — 지도가 클수록 기준선도 높아짐). 문턱 0.65 → 마진 0.35.
테스트 46 전부 통과. 시드 2028 궤적(diff-detector-v4.txt) + 5-시드 + 오라클 회귀
스위프 실행 중. 사고 1건: 바이너리 이름 하이픈(continual-test.exe)을 언더스코어로
잘못 써 스윕이 헛돎 — 실행 전 exe 존재 확인을 체크리스트에 추가.

**v4 결과(시드 2028): 습득 62.7 → 87.5% — 포획 종결.** 환경 10개 전부 자기 지도
개설(활성 10), 지도 크기 34~77 정상(과식한 지도는 다음 밤 자동 다이어트: 101→34,
257→77). v3 붕괴 지점이던 환경 6~8이 70.1/100.0/86.5로 회복. 오라클(95.3)까지 8pt.

**새 병목 노출 — 복귀 재인식**: 유지율 73.8%(비율 기준 하락이나 절대 복귀값은
v3 20~28% → v4 41~51%로 개선 — v3의 "높은 유지율"은 낮은 습득의 분모 착시).
패턴이 선명하다: 복귀 시 환경 1~4는 95~102%, 환경 5~10은 47~66%. 습득 측정은
활성 지도가 이미 정답인 채 재고, 복귀는 라이브 재인식이 필요한 비대칭 — 오라클은
복귀에서도 정답을 받아 면제. 정황 증거: 막차 응고 후 410노드가 복귀 측정 동안
4,479로 재성장(복귀엔 밤이 없어 은퇴 0) — 40~50%대 점수는 "틀린/빈 지도 위에서
스크래치 즉석 재학습"의 전형값. 유력 용의: ① 복귀 과도기에 p_new 오발(정크 지도
개설 → 활성이 빈 지도) ② 활성 고착/진동. 

**다음 인수분해(하네스에 심음, 소스만 편집·빌드는 스윕 후)**: `MONAD_ORACLE_RETURN`
(학습=감지기·복귀만 오라클 — 갭이 재인식이면 오라클급 회복, 피질 품질이면 그대로)
+ 복귀별 원격측정([복귀 i] 활성·지도수·전환누계·p_new). p_new를 pub으로 전환.

### 시도 42: 마진 0.35 통계 + 인수분해 3중 판정 → **오라클 회귀 파손 발견** → 카파 정규화

- **마진 5-시드**: 습득 81.8~91.8(평균 87.2, 기준선 +20pt 통계 확정) / 유지 63.1~76.2.
- **적신호 — 오라클 회귀 파손**: 오라클 유지 95~100 → 75.2/75.1. 밤 재분류는 오라클
  모드에도 공용이므로, 마진 규칙이 정당 에피소드를 오배정해 피질을 손상시킨다는 증거.
- **인수분해 3중 판정**(시드 2028):
  ① 혼합(복귀만 오라클): 유지 75.6 — 정답 지도를 잡아줘도 복귀 42~52%. 재인식 무죄.
  ② 순수 감지기 v5 + 복귀 원격측정: **활성=정답 지도 10/10 자력 재잠금**, p_new 오발 0,
     환경당 전환 1~2 — 라이브 감지기의 복귀 동작은 건강. 유지 65.1.
  ③ 세 모드(감지기 71.2·혼합 75.6·오라클 75.2)가 같은 값으로 수렴 → 손상은 복귀
     시점이 아니라 **학습 기간의 밤들에서 피질에 누적**(구빌드 오라클은 같은 복귀
     구조로 95~100이었으므로 복귀 메커니즘 자체는 무죄).
- **유력 기전**: 밤_{i+1}은 환경 i의 측정 에피소드(3,401걸음)를 재분류한다. 자기 지도
  대비 실제 0.9 − 셔플 기준선 0.5~0.6 = 마진 0.3~0.4로 문턱 0.35에 정확히 걸침 —
  구 규칙(절대 0.65)에선 확실히 재귀속되던 강화 병합이 절반쯤 실패/오배정.
- **수정 — 카파 정규화**: score = (실제 − 기준선) / max(1 − 기준선, 0.05), 문턱 0.5.
  크기 보정(비만 지도 기각: 0.7·0.6 → κ0.25)은 유지하면서 절대 감도(경계 사례 복원:
  0.9·0.55 → κ0.78)를 회복 — 마진의 두 효과를 분리해 좋은 쪽만 남기는 정규화.
- 사고 2건: ① ps1 한글 패턴이 BOM 없는 UTF-8에서 ANSI 오해석돼 스윕 8회의 판정 줄이
  빈 문자열(35분 손실) — **ps1은 ASCII 전용 + 판정은 마지막 줄 캡처**로 규칙화.
  ② 실행 중 exe는 Windows가 잠가 빌드 불가 — 실행 종료 후 빌드로 순서화.
- 부기: 병렬 7-작업 스윕 도입(시드당 프로세스, 벽시계 27→7분).

### 시도 43: 카파 기각(습득 양모드 붕괴) → 이중 규칙 + 프로브 2발견

- **카파 5-시드**: 감지기 63.6~74.9/78~90, 오라클 65~71/82~85 — **습득이 양 모드에서
  붕괴**. 원인: 셔플 기준선이 참지도에서도 0.6~0.8로 높고 시끄러워 분모(1−base)가
  작아지며 κ 불안정 — 잘 응고된(고기준선) 지도를 가장 세게 벌줘 강화 에피소드에
  정확히 역작용. 카파-혼합 궤적에선 blob까지 부활(map3=734, 환경 6~10 재포획).
- **세 구성 비교의 교훈**: 절대 0.65(오라클 95/94, 감지기 68/85) ↔ 마진 0.35(오라클
  90/75, 감지기 87/71) ↔ 카파 0.5(둘 다 습득 붕괴). 마진의 크기 보정이 습득 +20pt의
  원천이고, 문턱 0.35가 참지도 마진 분포(0.3~0.4) 안에 앉은 것이 유지 손상의 원천.
  두 효과는 분리 가능 — **이중 규칙**: 실제 ≥0.8이면 절대 귀속(강화 에피소드 보존),
  아니면 마진 ≥0.25(비만 흡수 마진 ~0.1 차단), 둘 다 아니면 신세계.
- **프로브 발견 ①**: 복귀 성공 환경(1~2)의 신규 스크래치 2~78 vs 실패 환경 364~625 —
  복귀 실패는 "올바른 활성 지도 위에서의 연속 놀람"(채점 1500걸음 중 400+회 분화).
  다음 프로브: 활성 지도 실제 크기 동시 기록(map_cortical[i] 해석 오류는 수정).
- **프로브 발견 ② — 고정 시드 비결정성**: 같은 시드·같은 빌드의 v4/v5에서 환경 3~4
  복귀가 81/94.9 ↔ 45.9/70.3으로 상이. 프로세스별 HashMap 해시 무작위화가 동역학
  경로에 샌다는 증거 — 5-시드 통계에 미지의 실행 잡음 포함. **결정론 복원(BTreeMap
  또는 고정 해셔) 백로그 상위 등재.**

### 시도 44: 이중 규칙 기각 → **근원 발견: 신세계 단일 버킷의 잡종 지도** + 결정론 복원

- **이중 규칙 5-시드**: 감지기 59.5~68.7/77~92, 오라클 65~71/81~88 — 습득이 구대역
  으로 회귀. 절대 0.8 지름길이 못 구한 이유: 측정 에피소드의 실제 생존율은 0.9+가
  아니라 0.65~0.85로 분포 → 0.8 미달분이 마진 0.25에도 걸려 신세계 버킷행.
- **3규칙 실패의 공통 증폭기 특정**: 밤의 미설명 에피소드들을 **하나의 신세계 버킷**
  으로 묶는 설계. 직전 환경 측정 에피소드가 문턱에 걸려 버킷에 떨어지는 순간, 새
  환경 학습 에피소드와 섞인 **잡종 지도**가 태어난다 — v4의 ll -0.478·신규 72 밤,
  v3 blob 씨앗(map3 122노드·ll -0.169)이 전부 이 잡종. **비만 지도의 기원은 분류
  실패가 아니라 버킷 오염.** 엄격할수록 버킷행 증가 → 습득 붕괴(카파·이중), 느슨하면
  잡종이 blob으로 성장(절대 0.65 단독). 분류 규칙 3종이 같은 벽에 부딪힌 이유.
- **수술**: ① 미설명 에피소드마다 **제 세계 개설**(잡종 원천 봉쇄; 중복 세계는 유한
  비용, 잡종은 복리 비용) ② 분류는 오라클이 95/94로 검증한 **절대 0.65 복원**(비만
  씨앗이 없으면 0.65 흡수 폭주의 전제도 소멸) ③ **결정론 패치 동시 적용**: 간선 삽입
  정렬 2곳(dream 733/790) · compact 키 정렬(graph 608) · 정렬 병합 동점 규칙(dream
  687, 동시발생 동률 시 오래된 id 우선) · 예측/수면 동점 규칙(graph 246·sleep 104).
  검증: 같은 시드 2프로세스 해시 동일성 체크 병행.

### 시도 45: 절대 0.65 재기각(blob 재탄생) + **측정 스캔들 적발** + 마진·에피소드별 합류

- **시도 44 결과**: 감지기 65.5~67.3/83~89(구대역 회귀), "오라클" 64.7~68.1/86~87.
  궤적: map5가 37→764로 재폭식, 환경 6~10 재포획(지도 6개 동결) — **에피소드별
  세계는 잡종만 막을 뿐 0.65 흡수 폭주는 못 막는다.** 반포획 능력이 증명된 규칙은
  여전히 셔플 마진 0.35뿐(습득 87.2).
- **측정 스캔들 — "오라클 회귀 파손"은 측정 오류였다**: 병렬 스윕의 오라클 작업이
  MONAD_MAPS=1을 함께 켬 → **라이브 감지기가 경계 정답을 중간에 오버라이드** →
  매 시대 "오라클"≈그 시대 감지기(마진 90/75≈87/71 · 카파 68/83≈67/84 · 현재
  68/86≈66/85 — 시대별 상관이 움직일 수 없는 증거). 마진 시대에 세운 "분류가
  피질을 손상(오라클로 확인)" 서사의 오라클 다리는 무효. **참오라클 = ORACLE=1 +
  MAPS 미설정**(재분류는 consume && !skip_retire 조건이라 참오라클에서도 돈다 —
  과거 96/100 기록과 정합). 스윕 스크립트 수정 완료.
- **결정론 미완**: 같은 시드 2회가 여전히 상이 — 분기는 밤1 "이전"(각성 노드 966 vs
  937). 원천은 dream이 아니라 **정착 경로** — 유력: store.rs LSH 후보 수집의 HashSet
  순회(해밍 동률 선택 랜덤). 다음 빌드 사이클에 수정 예약.
- **현 조합(시도 45)**: 마진 0.35 복원 + 에피소드별 세계 유지(경계선 신세계행이
  이제 무해 — 잡종 대신 중복) + 참오라클 2시드. 예측: 습득 ≥87 유지, 유지율은
  강화 손실이 중복 세계로 완화되는 만큼 회복(마진 단독 71 → ?).

### 시도 46: 습득 축 최고 기록(89.9) + 오라클 번호 계약 + **문맥 그림자 납치 검거**

- **마진+에피소드별 5-시드: 습득 86.3~94.4(평균 89.9) — 역대 최고, 게이트선 도달.**
  유지 60.3~77.7(평균 71.6)이 마지막 벽.
- **참오라클 25.3/25.5의 정체 — 번호 계약 파손**(회귀 아님): 에피소드별 세계가
  밤마다 세계를 1~2개 열면 "지도 i = 환경 i" 가정이 무너져 오라클이 빈 지도를
  활성화한다(구 단일 버킷은 우연히 번호 정렬). 수술: **오라클 모드(map_inference
  꺼짐)에서는 밤 재분류도 라이브 태그(=정답)를 신뢰**하도록 게이트.
- **유지 벽의 범인 검거 — 문맥 색인 그림자 납치**: 혼합모드 프로브에서 복귀 성공
  환경(1~2)의 신규 스크래치 8~79 vs 실패 환경(3~10) **+352~760**(활성 지도가 측정
  중 36→350~792로 재건축). 재정착·차별화·재사용 경로는 전부 지도 스코프가 있으나,
  **ctx.nearest_within이 최근접 1개만 뽑은 뒤 지도 필터**를 걸어 — 밤 없는 복귀
  구간에 누적된 남의 지도 문맥(~800 등록, 6기호 공유라 충돌 잦음)이 내 지도의
  차근접을 가리면 필터가 걸러서 **클론 강제 신설**. 습득이 면역인 이유(밤이 색인
  청소)·환경 1~2만 무사한 이유(색인 작음)·스크래치 폭증의 정체를 단일 기전이 설명.
  수술: k근접(8) 조회 후 지도·지각 일치 최초 히트 선택.
- **빔 결정론**: 고정 시드 비결정성의 원흉 = wake.rs 믿음 랭킹이 cand(HashMap)
  순회 순서를 float 정렬에 먹인 뒤 빔 폭으로 절단 — 동점(신선 count=1)이 프로세스
  마다 다르게 잘림. 수술: 총순서(점수 내림, 동점은 노드 id 오름).
- 3종 패치 일괄 적용, 테스트 46 통과. 스윕(감지기5+참오라클2)+해시 검증+인수분해
  실행 중. 예측: 결정론 회복, 참오라클 95±, 감지기 습득 ~90 유지, **유지율 85+**
  (그림자 납치 제거로 복귀 스크래치 소멸 → 피질 위 믿음 추적 복원).

### 시도 47: 참오라클 게이트 통과 🎉 + **주소 없는 피질** 발견 → 밤의 재주소화

- **참오라클 게이트 통과**: 96.6/100.6 · 97.1/99.6 ✅ — 번호 계약 수복으로 상한 복원.
  응고 계층 건강 재확인. 마진+에피소드별+ctx수정 빌드에서 상한 ~97/100.
- **감지기**: 습득 88.2 유지(83.0~91.9) · 유지 73.6(+2pt뿐) — **k=8 사후 필터 무효.**
- **k=8이 무효였던 진짜 이유 — 주소 없는 피질**: 문맥 등록은 각성의 클론 생성
  순간에만 일어나고, 밤의 은퇴가 그 노드들과 함께 주소록을 지운다. **응고된 피질엔
  해마 주소가 없다.** 습득 측정은 자기 스크래치 churn이 주소록을 재건해 가려졌고
  (밤 직후 = 색인 초기화 직후), 복귀는 주소 없는 피질 위에서 ctx 조회가 전부
  빗나가 over-이벤트마다 클론 신설(+350~760). k를 아무리 키워도 "자기 등록이 0개"
  라 소용없던 것.
- **수술 — 밤의 재주소화**: 꿈이 Viterbi 경로에서 각 응고 노드의 이른 등장 순간
  문맥을 재구성(각성 context_vec와 비트 동일 연산: 지각 + Σ permute^i(지각⊗행동씨앗))
  해 노드당 밤당 최대 3개 등록. 등록은 apply_node_map의 ctx 재구축을 타고 은퇴·
  bisim 병합·재번호를 넘어 생존(기존 불변식 확인). "잠이 기억을 응고시킬 뿐 아니라
  **다시 찾을 수 있게 주소를 새로 단다**" — 해마-피질 이론과도 정합.
- **결정론 전진**: 빔 수정으로 각성 결정론 달성(두 실행 노드 956 동일 도달) — 잔여
  분기는 꿈 내부: `seed_pairs`가 **HashSet 순회 순서로 RNG를 소비**(지터 배정이
  프로세스마다 뒤섞임). 정렬 목록으로 소비 순서 고정.
- 빌드·테스트 46 통과. 스윕+해시 검증 실행 중. 예측: 결정론 완성, 유지율 도약
  (복귀가 피질 주소를 되찾으므로).

### 시도 48: **결정론 달성**(벽시계 텍스트뿐) + 재주소화 반완치 → 지도별 해마 색인

- **결정론 판정 승리**: diff의 유일한 차이가 `31초 vs 32초` — 경과 시간 출력 텍스트.
  동역학은 비트 동일. 검증 스크립트가 시간 필드까지 해시한 것 — 체크에서 `N초`
  필드를 벗겨 비교하도록 수정. (빔 총순서 + seed_pairs 정렬 + 간선/컴팩트/동점
  6지점의 합작.)
- **재주소화 판정 — 반완치**: 감지기 습득 86.7~94.3(**평균 90.5, 게이트선 도달**),
  유지 75.7~81.2(평균 78.4, +4.8pt). 참오라클도 상승: 98.4/100.4 · 96.8/100.4.
  복귀 프로브: **환경 1~4 완치**(유지 95.8~100, 스크래치 9~89) — 주소 있는 피질은
  복귀를 완벽히 되찾는다. 환경 5~10은 여전히 스크래치 288~465: 재주소화가 등록을
  영구화하면서 복귀 후반일수록 남의 살아있는 등록 수백 개가 k=8 창을 재포화(주소
  없음을 고치니 그림자 밀도가 증폭 — 순서 상관이 서명). 부수 확인: 복귀 활성
  7/8/9/10/11 — 감지기가 에피소드별 세계의 번호 이동까지 정확 추적.
- **수술 — 지도별 해마 색인**: ctx를 `Vec<Store>`(지도당 하나)로. 등록·조회·재구축
  (apply_node_map)·리셋 전부 지도 스코프. "상태가 세계에 속하듯 주소록도 세계에
  속한다." 남의 그림자가 구조적으로 불가능 — k 무관. 7지점 패치, 테스트 46 통과.

### 시도 49: 그림자 밀도 이론 기각 → 순서 vs 나이 인수분해 + 결정론 완성 수순

- **지도별 색인 5-시드**: 습득 88.8~93.7(**평균 91.2 신기록**) / 유지 69.8~77.9
  (평균 74.3) — 유지 불변. **세 가지 ctx 설계(전역 k8·재주소화·지도별)에서 유지가
  74~78 고착 → 주소 계통은 병목이 아니다.** 참오라클 재확인 98.4/100.4 · 96.8/100.5.
- 남은 가설 가족 2개: **(A) 복귀 위치 누적**(복귀 순서상 뒤일수록 쌓인 무언가가 방해)
  vs **(B) 지도 나이**(bisim 밤 10회 거친 늙은 지도 vs 1~2회의 젊은 지도 — 전이
  청결도 차이). 판별 실험: `MONAD_RETURN_REVERSE`(복귀 순서만 역전) — 실패가 복귀
  위치를 따라가면 A, 환경 번호를 따라가면 B. 시드 2028 fwd/rev 실행 중.
- **결정론 최종 국면**: 코어는 완전 결정론(학습 국면 비트 동일, 스크래치 카운트까지
  일치). 마지막 ±3히트 분기의 정체 = **하네스 measure()의 위치표** — HashMap 순회로
  만든 Vec에서 동률 argmax가 순서를 탐. 동점 규칙(셀 번호) 고정 → 최종 검증 중.

### 시도 50: **결정론 완성 ✅** + 역순 판정 "(A) 복귀 위치 누적" → 전역 스무딩 분모 검거

- **결정론 완성**: 같은 시드 2프로세스 바이트 동일(시간 텍스트 제외). 각성·꿈·EM·
  정렬·bisim·하네스까지 전 파이프라인 재현 가능 — 이후 스윕은 순수 시드 분산만 측정.
- **역순 복귀 판정 — 지도 나이 무죄, 위치 유죄**: 순서를 뒤집자 패턴이 함께 뒤집혔다.
  정방향: 1~4 성공(79~100)·5~10 실패(22~58). 역방향: **10·9가 100% 복귀**(1밤 젊은
  지도!)·8~6이 84~99, 나중 슬롯의 1~4가 47~58로 붕괴. **먼저 복귀하는 4~5개는 무엇
  이든 성공, 나중은 무엇이든 실패** — 복귀 중 축적물이 이후 측정을 오염.
- **기전 검거 — 전역 스무딩 분모**(wake.rs:663): 믿음 전파 확률의 라플라스 분모가
  `clones_of(percept)` **전역** 클론 수를 사용. 복귀엔 은퇴가 없어 슬롯당 ~400
  신선 클론이 쌓이면 k가 수백으로 부풀어 전이 확률이 눌리고 만성 놀람→클론 연쇄.
  후보는 지도 스코프인데 분모만 전역이던 비대칭 — 지도 스코프 카운트로 수정.
- 빌드·테스트 46 통과, 5-시드+참오라클 스윕 실행 중. 예측: 복귀 순서 의존 소멸,
  유지율 90 접근(역순 실험에서 젊은 지도들이 100% 복귀함을 이미 실증 — 지도 자체는
  완전하며 측정 환경 오염만 제거하면 된다).

### 시도 51: **감지기 첫 게이트 통과 2/5** (96.5/90.9 평균) → 잔존 blob 백신

- **전역 분모 수정 판정 — 벽 붕괴**: 습득 94.6~98.6(평균 **96.5**, 참오라클 97.6과
  동급) / 유지 80.7~98.2(평균 **90.9**). **시드 2026(98.6/98.2)·2028(95.5/97.6)
  게이트 통과** — 완전 자율 감지기 모드 최초. 2030은 0.5pt, 2027은 1.6pt 박빙 미달,
  2029만 9.3pt(80.7).
- **미통과 3시드 부검(결정론 재현 궤적)**: 실패가 국소화 — 시드당 1~2개 환경만
  6.7~24.3%로 전멸, 나머지는 90~100%. 공통 원인 = **잔존 미니 blob**(2027: map5=210
  · 2029: map1=302 · 2030: map5=119). 전멸 모드 두 가지: ① 내용 일부를 blob에 뺏긴
  환경(자기 지도가 불완전) ② 복귀 감지기가 blob에 잠금(활성 5/1, 크기 760~1076).
  통과 시드에는 blob 없음. 마진 0.35가 발생 빈도를 "매 시드"→"5시드 중 2~3회"로
  줄였지만, 우연히 중간 크기를 넘긴 지도는 양성 피드백을 재개한다.
- **수술 — 크기 비율 백신**: 분류에서 흡수 대상의 피질이 **중위 지도의 2.5×** 초과면
  마진 ≥0.55가 아닌 한 거부(신세계행). 동일 크기 환경 게이트에서 2.5×중위는 안전한
  비정상 판정. 테스트 46 통과, 스윕 실행 중.

### 시도 52: 크기 백신 무효(발화 0회 증명) → **두 증인의 합의 규칙**

- **백신 스윕이 모든 자리수까지 이전과 동일** — 결정론 하에서 이는 fat_veto가 단
  한 번도 발화하지 않았다는 증명. 구조적 이유: **첫 감염은 blob이 2.5×중위보다
  작을 때 일어나고**, 살이 붙은 후엔 이미 삼킨 내용 덕에 마진이 0.55를 넘는다 —
  크기 기준 백신은 원리상 한 박자 늦는다. (음성 결과도 결정론 덕에 1스윕으로 확정.)
- **수술 — 두 증인의 합의**: 라이브 태그(각성이 산 곳, 에피소드 시작 시 active)와
  내용 argmax가 **일치하면 문턱 0.35, 불일치하면 0.55**. 강화 에피소드(측정 흔적)는
  밤 직후 정답 지도에서 시작해 태그가 맞고(0.35 유지 — 유지율 보존), 첫 감염(외부
  학습 에피소드의 우연 흡수)은 태그와 갈린다(0.55 — 발생 시점 차단). 학습 에피소드의
  스테일 태그는 무해(어차피 신세계행). fat_veto는 제거(무효 증명 — 기준선 불변 보장).
- 테스트 46 통과, 스윕 실행 중. 게이트 산수: 통과 시드엔 blob 없음, 미통과 3시드
  결손은 전부 blob 기인 — 첫 감염 차단 시 5/5 사정권(2030 +0.5pt, 2027 +1.6pt,
  2029 +9.3pt 필요).

### 시도 53: 태그 합의 — 양날의 검(3/5, 순개선·기전 불순) → 기준선 3변주 평균

- **태그 합의 결과**: 2026 ✅98.5 · 2027 **✅96.4(+8.0 — blob 완치)** · 2028 ❌90.0
  (−7.6, 통과 상실) · 2029 ❌63.8(−16.9 악화!) · 2030 **✅90.0(통과)**. 3/5로 표면
  개선이나 기전이 불순: ① 2029의 감염은 **직전 환경 지도**로의 감염 — 경계 학습
  에피소드의 스테일 태그(=직전 지도)가 감염과 정확히 "합의"해 문턱을 열어줌
  ② 2028은 라이브 태그 잡음이 정당한 강화를 0.55로 차단. **태그는 증인 부적격 —
  기각·원복.**
- **원리적 대안 — 기준선 3변주 평균**: 감염이 0.35를 넘는 것은 "운 좋은 단일 셔플"
  (기준선 잡음)의 덕이다. 셔플 순열 스트림 3종(t·7+3, t·11+5, t·13+7)의 평균으로
  기준선 분산을 조이면 통계만으로 감염 꼬리가 잘리고, 강화(실제 ~0.9, 마진 여유)는
  무영향. 새 기전 없이 기존 순열 검정의 표본 수만 늘리는 정공법. 비용 = 분류 시
  체인 4회(무시 가능). episode_score 시그니처 bool→u32(변주 번호).
- 테스트 46 통과, 스윕 실행 중.

### 시도 54: 3변주 무변화(감염=실질 중첩 증명) → **AND 결합 규칙**

- **3변주 평균 스윕이 모든 자리수까지 기준선과 동일** — 감염은 기준선 잡음이 아니라
  **같은 6기호 미로들 사이의 진짜 통계 중첩**(행동 조건부 국소 전이 구조가 셔플이
  파괴 못 하는 수준으로 겹침)이라는 증명. 외부 에피소드의 실제 생존율 0.65~0.8은
  실질이며 마진으로 지울 수 없다.
- **진짜 분리자 = 절대 생존율**: 자기 환경 ~0.9 vs 통계 중첩 외부 ~0.65~0.8.
  절대 0.65의 과거 실패는 비만 지도가 외부를 밀어올린 것(마진이 차단). **AND 결합**:
  마진 ≥0.35(비만 우연 차단) ∧ 절대 ≥0.8(중첩 감염 차단). 강화는 실제 0.85+라
  무손실. 시도 43의 OR은 두 문턱의 약점을 합집합으로 열었고, AND는 교집합으로
  닫는다. best에 (지도, 마진, 실제) 3원 추적.
- 테스트 46 통과, 스윕 실행 중. 예측: 2027/2029/2030의 감염(실제 <0.8) 차단 →
  전멸 환경 회복 → 4~5/5.

### 시도 55: AND도 무변화(4연속) → **문턱 10.0 프로브 — blob은 분류와 무관**

- **AND 스윕도 모든 자리수 동일** — 4가지 분류 규칙 변경(백신·태그·3변주·AND)이
  전부 단 하나의 결정도 안 바꿈. 통계가 아니라 경로 의심 국면.
- **결정 프로브**: 흡수 문턱을 10.0(불가능치)으로 올린 빌드, 시드 2029 —
  지도 20개(에피소드마다 신세계 ✓ 분기 정상 실행 증명), 그런데 **map9 = 149노드**.
  흡수가 불가능한데도 blob 형성. **blob은 밤의 분류가 만드는 게 아니다.**
- **산수 모순**: 문턱 10에서 지도는 평생 에피소드 1개(탄생 밤)만 받고, EM 예산은
  지각당 16×6지각=96 상한 — 149는 단일 밤 산출로 불가능. 어떤 다른 경로가 노드를
  그 지도 태그로 추가하고 있다. 밤별 지도 피질 벡터 프로브([피질])를 원격측정에
  추가, 문턱 10 유지 채 재실행 — blob이 +100 점프하는 밤을 직접 관측한다.
- 교훈: 4연속 무변화를 "통계적 우연"으로 읽지 않고 실행 경로 의심으로 전환한 것이
  맞았다. 결정론이 없었으면 이 비교 자체가 불가능했다.
- **[피질] 프로브 판정 — blob은 착시였다**: 문턱 10.0에서도 밤마다 전 지도 피질
  35~42 정갈("map9=149"는 환경 줄 집계가 라이브 스크래치를 포함한 착시 — 스크래치는
  매밤 은퇴됨). **밤 계통(EM·정렬·분류·은퇴)은 완전 건강** — 4가지 분류 규칙이
  무변화였던 진짜 이유: 고칠 것이 없었다. 2029의 "map1=302"도 같은 착시로 추정 —
  복귀 전멸의 진짜 기전은 비만 피질이 아니라 복귀 중 오잠금+스크래치 자기강화로
  재수사. AND 규칙 복원, 시드 2029 피질 프로브 재실행 중.

### 시도 56: 시드 2029 피질 판독 — **무한 각성 체제** 검거 → 복귀에도 밤을

- **2029 피질 확정**: map1 피질 = 36(항상 정상; "302"는 학습 전환 진동이 map1 태그로
  흘린 스크래치 착시). 전 지도 피질 36~50. **blob이라는 것은 애초에 존재한 적 없다.**
- **복귀 전멸(환경8 22.5%)의 진짜 기전**: 복귀 8이 활성=1로 시작 → 예측이 깨질
  때마다 해마가 env8을 map1 문맥의 **스크래치로 즉석 재학습**(1-shot 각성의 설계
  기능!) → "설명됨" 유지 → 감지기가 전환 신호를 영영 못 받음 → 피질+잡탕 스크래치
  위 측정 22.5%. 복귀 구간의 밤 금지가 만든 **무한 각성 체제** — 해마 무한 누적은
  아키텍처가 지지한 적 없는 조건(생물은 자야 한다).
- **수술 — 복귀 사이의 밤**: 막차 응고의 기존 원칙("새 노출 없는 정리는 재학습이
  아니다")을 복귀 사이에 동일 적용 — 각 복귀 측정 후 consolidate(). 스크래치 은퇴 +
  측정 에피소드의 제 지도 강화 귀속 + 다음 복귀 콜드 스타트 청결. 측정 의미론 불변
  (도착→워밍업→채점 그대로). 예측: 복귀 순서 의존 소멸, 전 환경이 복귀 1~4 조건
  (95~100%)으로 보편화 → 5/5 사정권.

### 시도 57: 복귀-밤 기각(악화 80.0/74.3/84.2) → 측정 중 감지기 내부 직시

- **복귀-밤 판정**: 미통과 3시드 전부 악화(2027 88.4→80.0 · 2029 80.7→74.3 ·
  2030 89.5→84.2), 통과 2시드 무변화. 스크래치 누적은 구속 조건이 아니었고, 복귀
  에피소드 재분류가 지도 내 중복 사본이라는 새 혼란을 보탬. 원복.
- **남은 수수께끼 단 하나**: 복귀 8(2029)이 활성=1로 시작해 6,800걸음 내내 전환
  +1뿐 — env8 걷기에서 map8 피질 체인은 ~0.9로 살아야 하는데 왜 감지기가 안
  넘어가는가. 워밍업 400 = 검사 주기(32)의 ~12회분, 유예도 없음(막차 유예는 복귀
  1이 소진). 전환을 막는 것이 스코어인지(map1이 이기는 왜곡) 검사 자체의 부재인지
  (조건 버그) 가르기 위해 **측정 중 map_diag 원격측정**(t=50/200/399에 불량률·활성·
  지도별 생존율 벡터) 추가, 시드 2029 재실행 중.

### 시도 58: 측정診 판독 — **간선 blob** 검거 → 확신 게이트 링크 기록

- **측정診의 사실**: 복귀 8 내내 **map1 생존율 = 1.0**(자기 환경 map8은 0.30~0.52).
  env1의 36노드 피질이 env8을 완벽 설명할 수 없으므로 지표 조작 — map1은 노드가
  아니라 **간선으로 비만한 blob**. 환경 2~10 학습 내내 전환 진동이 map1로 튈 때마다
  기록된 링크가 36노드 위에 외부 전이의 조밀한 그물(간선 count+=1 누적)을 짜서,
  어떤 (행동,지각) 시퀀스든 연장되는 우연 체인 천국이 됐다.
- **모든 조각이 맞춰짐**: ① 노드 수 프로브가 못 본 이유(간선은 노드를 안 늘림)
  ② 밤의 분류가 안 속은 이유(셔플 기준선이 조밀 간선의 우연 체인을 정확히 상쇄 →
  마진 ~0 → 흡수 없음 — 4규칙 무변화의 뿌리) ③ 라이브 생존율만 속은 이유(셔플
  보정 없음) ④ 복귀 오잠금·시드별 전멸 환경의 원흉.
- **수술 — 확신 게이트 링크 기록**(백로그 가설 b의 실체화): 지도 사후확률
  P(활성) < 0.7인 순간(전환 진동 창)에는 각성의 링크 기록을 보류. 에피소드는 온전히
  보존되므로 진실의 원천(밤의 EM)은 무손실 — 각성 기록은 확신 창의 강화 보너스로
  재정의된다. 오라클 모드(map_inference=false)는 무영향. 테스트 46 통과, 스윕 중.

### 시도 59: 🏆 **M1 본게이트 5/5 전원 통과** — 완전 자율 감지기 모드

| 시드 | 습득 | 유지 | 판정 |
|---|---|---|---|
| 2026 | 98.7 | 98.1 | ✅ |
| 2027 | 96.9 | 98.6 | ✅ |
| 2028 | 95.5 | 97.6 | ✅ |
| 2029 | 96.9 | 97.5 | ✅ |
| 2030 | 94.6 | 92.2 | ✅ |

**평균 습득 96.5 / 평균 유지 96.8 — 참오라클(97.7/100.4)과 1pt 이내.** 확신 게이트
링크 기록이 마지막 열쇠. DoD(유지 ≥90 · 습득 ≤10⁴걸음) 전 시드 충족, 재분류·감지
전부 자율(오라클·정답 신호 일절 없음).

**M0 무회귀 인증**: maze 97.7/100.0 · bounce 전 단계 통과 · revise 2/2 ·
pong 5/5(최악 2,520). 테스트 46/46. 결정론 재인증 실행 중.

**돌파의 최종 인과 사슬(시도 41→59)**: 자기설명 루프(피질 전용 증거) → 라이브
무절단(밤 전용 경계) → 셔플 대조 분류(비만 노드 blob 차단) → 에피소드별 세계
(잡종 버킷 봉쇄) → 밤의 재주소화(주소 없는 피질) → 지도별 해마 색인 → **전역
스무딩 분모의 지도 스코프화**(복귀 순서 의존 제거, +16.6pt) → **확신 게이트 링크
기록**(간선 blob 차단 — 노드가 아니라 간선으로 비만해지며 라이브 생존율만 속이던
최종 보스, +23pt) — 여덟 개의 적층 결함이 각각 별도의 인수분해 실험으로 검거됨.
결정론 완성(8지점)이 후반 수사(4규칙 무변화의 해석, 착시 판별)의 전제 조건이었다.

### 시도 60: M1 마감 ① 에피소드 저널 영속화 완성(테스트 48) + 게이트 자리수 재현

- **에피소드 저널**: `episodes.journal`(레코드 [0x01]걸음/[0x02]경계, 256걸음 버퍼
  flush) + `agent.snap`(n_maps·active·node_map·map_birth(u64)·map_post·map_cortical·
  fresh_clones, 밤의 끝마다 원자적 교체) + `Agent::attach` 확장(그래프+스냅숏+저널
  재생). 밤의 끝(래퍼 은퇴 블록)에서 ep_checkpoint — 소화된 경험은 구조가 됐으므로
  저널 소거. 사고 1건: 첫 삽입이 하위 꿈 전용(죽은) 은퇴 블록에 들어가 테스트로
  적발 — 진짜 은퇴는 분류 래퍼의 밤끝 블록.
- **신규 테스트 2**: kill-9 복구(걸음·경계·node_map 정합) + 밤끝 체크포인트(저널
  비움·agent.snap 왕복). 총 48/48.
- **게이트 무회귀 — 자리수까지 동일 재현**(영속화 훅의 동역학 무영향을 결정론이
  증명): 감지기 5/5(96.5/96.8) + 참오라클 2/2. ctx 색인은 비영속(문서화) — 다음
  밤의 재주소화가 자연 재건.
- 마감 ② 진행: `switch_cost`(운동 부드러움 사전분포, 기본 0 — 타 하네스 무영향,
  pong 0.2) — EFE 롤아웃 동률 잡음의 좌우 떨림 억제. Pong 5-시드 실행 중.

### 시도 61: Pong 정상상태 처방 2종 실측 기각 → 정직 이월, M1 마감 선언

- **switch_cost 0.2**: 77/78/66/71/85(평균 75.4) ≈ 기준선 75.8 — 무효. 떨림 가설 기각.
- **consume 증분 응고**: 도달 3/5로 **역회귀** — pong의 작은 단일 세계에서는 전면
  재건의 전역 재추론이 증분 병합보다 깨끗하다(10환경의 결론과 반대 방향 — 규모와
  세계 수가 노선을 가른다는 데이터 포인트). 원복, 게이트 5/5·최악 2520 자리수 재현.
- **Pong 정상상태(66~83%)는 미해결로 정직 이월**: 기각된 처방 3종(ε-잠금·switch_cost·
  consume) 기록. 잔여 후보: 재건 직후 재정착 과도기의 평균 혼입 — 측정 창을 꿈
  직후 300걸음 제외로 분리하는 실험이 다음 손잡이. 게이트(도달 ≤5000)는 여유 통과
  상태이므로 M1 차단 요소 아님.
- **M1 마감 상태**: ① 10환경 본게이트 ✅ 5/5 ② 에피소드 저널 영속화 ✅(테스트 48)
  ③ Pong 정상상태 ⬜(3처방 기각 기록, 이월) ④ 실기기 검증 ⬜(대상 기기 필요 — 외부
  의존) ⑤ Gameworld 동일벤치 대조 ⬜(환경 재구현 필요 — M2 이후 병행 후보).
  코드로 가능한 M1 항목은 소진.

### M2 킥오프 — 작업 분해 (PRD §6: ARC-AGI-1 ≥30% 무사전학습·귀납, ≤5분/CPU, 킬 <15%)

기존 자산과의 접점: schema.rs(MDL+빔+예외 — Bounce 8/8, 전이 100%)가 M2의 핵심
가설("수면 스키마 라이브러리가 ARC 변환을 조합한다")의 씨앗. C2-S 스파이크의
정신을 반복한다 — **먼저 최소 스파이크로 킬 기준을 빨리 시험**한다.

- **W2-0 (스파이크, 크리티컬 패스)**: ARC 과제 표현 설계 — 격자 지각(셀·색·연결
  성분)을 MONAD 지각/Atom으로 인코딩 + 입출력 쌍을 "관찰 에피소드"로 제시하는
  하네스. 합성 미니 과제 10종(이동·복사·색치환·대칭·크기 — ARC 최빈 원시 변환)으로
  스키마 귀납이 변환 규칙을 잡는지 확인. **킬 질문**: 미로·Bounce에서 검증된
  MDL-스키마가 격자 변환 공간에서도 압축 이득을 내는가.
- **W2-1**: 실 ARC-AGI-1 데이터 하네스(공개 JSON, fchollet/ARC-AGI) — 데이터
  확보는 다운로드 승인 또는 사용자 제공 대기(오프라인 우선: W2-0은 합성으로 진행).
- **W2-2**: 스키마 라이브러리(수면이 스키마를 누적·재사용) + 재사용률 계측.
- **W2-3**: 조합 탐색(anytime — 시간 줄수록 좋아지는 계약) + 과제당 ≤5분 예산 하네스.
- **W2-4**: 어블레이션 리포트(스키마 없이/라이브러리 없이/탐색 없이) — PRD 의무.

### 시도 62: **W2-0 스파이크 통과 9/10** ✅ — 격자 변환의 스키마 귀납 실증

- 구현: `grid.rs`(격자·4-이웃 연결 성분·합성 10과제군 생성기) + `arc_spike.rs`
  (정렬 → 후보 클래스 라벨링(관계 오차, 모호성 보존) → **전역 일관성 투표**(상수
  translate > mirror > gravity — 대칭 도형의 translate/mirror 중의성을 과제 수준
  MDL로 해소) → 인수분해 lib 5종(클래스/dx/dy/색/사본수) → 시험 적용 → 정확 일치).
- 반복 기록: 6/10(1차) → 디버그로 3원인 특정: ① gravity 생성기의 낙하 충돌(과제
  불량 — 열 분리 배치로 수정) ② 최대객체 동률 타이브레이크 ③ **소표본 체제의 MDL
  침묵**(전 lib 규칙 0개 — Bounce 수백 이벤트 기준의 이득 상수가 ARC의 3~9 이벤트
  에서 규칙 서술 비용을 못 넘음). 처방: 이벤트 4× 복제(비율 보존 증폭) + min_support
  4 → **9/10 통과**(사전 등록 기준 ≥8).
- 산출 규칙(유리상자): "IF 최대객체=1 THEN stay"(증거 12/반례 0) · "IF 사본k=0
  THEN stay, 기본 translate(+4)" · "IF 사본k=0 THEN outline" — 사람이 읽는 형태.
- 잔여 1실패(mirror_v): 거울축 위 객체의 우연 색 규칙(반례 4·확신 0)이 시험에서
  오발 — 소표본 과적합. W2-2에 **쌍 간 교차 검증**(일반화 못 하는 규칙 폐기) 등재.
- **킬 질문 답**: MONAD 스키마 귀납은 객체-이벤트 다리 위에서 격자 변환을 잡는다 ✓.
  few-shot MDL 보정(복제 대신 원리적 사전분포)은 W2-2의 첫 항목. 테스트 48 무회귀.
- **추가 수선 → 10/10 만점**: 확신 필터(confidence < 0.5 규칙 폐기 — 훈련 내 반례를
  낸 우연 규칙의 자기 고백을 존중; 쌍 간 교차 검증의 최소 전신) 한 줄로 mirror_v
  종결. **W2-0 = 10/10, 사전 등록 기준(≥8) 초과 달성.** 테스트 48 무회귀 재확인.

### 시도 63: W2-1 완료 — 실 ARC-AGI-1 기준선 2.5% (과제당 2ms)

- 데이터: fchollet/ARC-AGI 클론(훈련 400 + 평가 400; 목표의 사전 승인에 따른 공개
  연구 데이터 취득). `data/ARC-AGI/`.
- 구현: `arc_data.rs`(의존성 0 손수 JSON 파서 — ARC의 제한 형식 전용),
  `arc_solve.rs`(W2-0 솔버 공용 모듈화 — 스파이크 10/10 무회귀), `arc_real.rs`
  (기준선 하네스: 동일 크기 부분집합 + 스킵 정직 보고).
- **기준선: 400과제 중 10개 정확 일치 = 2.5%**(동일 크기 260과제 대비 3.8%) ·
  과제당 평균 **2ms**(예산 5분/과제 대비 15만 배 여유 — 어휘·조합 확장의 연료).
  해결: 25ff71a9, 3ac3eb23, 4258a5f9, 496994bd, a1570a43, a79310a0, b1948b0a,
  b60334d2, c8f0f002, d511f180 — 7클래스 원시 어휘로 실과제 10개.
- 잔여: 파싱 실패 2과제(9edfc990, b230c067 — 형식 특이점 추후 수선) · 크기 변환
  138과제 스킵(어휘 미보유). 비교 좌표: CompressARC 20%(GPU 20분) ↔ 출발선
  2.5%(CPU 2ms) ↔ M2 목표 ≥30%.
- **다음(W2-2)**: 실패 분포 분석(260 - 10 = 250 실패의 유형 통계 — 어떤 변환 클래스
  부재가 가장 많이 아픈가) → 어휘 확장 우선순위 → 쌍 간 교차 검증 → few-shot MDL
  원리 보정 → 스키마 라이브러리(수면 누적·재사용률).
- **실패 250건의 유형 분포(실측)**: ① **객체 생성형 150건(60%)** — 출력 객체 수 >
  입력(선 긋기·채움·패턴 확장·산개 복제): 지배적 어휘 공백. 현 어휘의 생성 클래스는
  outline 하나뿐 ② 근접 실패(셀 ≥90% 일치) 71건(28%) — 파라미터/디테일 수준의 싼
  승리 후보 ③ 신규 색 등장 104건(생성형과 중복 다수) ④ 파편화(객체 >12) 48건 —
  객체 패러다임이 아닌 셀·타일 패러다임 과제(별도 트랙 필요). **W2-2 우선순위 확정:
  생성 클래스 어휘(채움·선·성장) > 근접 실패 정밀 분석 > 파편화 트랙 분리.**

### 시도 64: W2-2 생성 클래스 3종 — 스파이크 13/13 · 실데이터 +1 (11/398 = 2.8%)

- **신규 어휘**: C_AT_MARKER(표식 복제 — 사본이 다른 객체 위치에 놓이는 관계 앵커,
  파라미터=표식 색) · C_FILL(구멍 채움 — 갇힌 배경 탐지 `holes()` 신설) · C_RAY
  (광선 — 객체 변에서 가장자리까지 1폭 직선, 파라미터=방향 4종).
- **파라미터 lib에 S_CLASS 슬롯**: 클래스마다 파라미터 의미가 다르므로(translate=
  dx·dy, at_marker=표식 색, ray=방향) 클래스 조건을 실어 혼선 차단. 전역 일관성
  투표에 AT_MARKER 우선순위 추가(상수 translate 다음).
- **합성 3족 추가(13족)**: marker_copy·fill_hole·ray_right — **스파이크 13/13**.
  marker_copy의 자율 학습 규칙이 백미: "IF 색=9 THEN delete"(표식 소멸) + "IF
  사본k=0 THEN stay"(원본 유지) + 사본은 표식 위치 순회.
- **실데이터: 10 → 11 해결(+a5313dff)** = 2.8%. 실 ARC 생성 패턴의 다양성(임의
  행 광선·형태 기반 표식·채움 변종) 대비 프로토타입 3종의 커버리지 한계 확인 —
  기대 부합. **다음 지렛대: 근접 실패 72건 정밀 분석**(예측과 정답의 차이 셀
  통계·유형 — 파라미터 수준 결함의 싼 승리 발굴).

### 시도 65: 근접 법의학 + **격자 수준 연산 계층** — 12/398 (3.0%)

- **근접 실패 72건 유형 실측**(MONAD_ARC_FORENSIC): **누락만 38(53%)** — 소량
  셀(2~36)의 단일 색 추가 실패(색4 채움 계열 다수) · 혼합 17 · 색만 13(재색칠
  파라미터 미스) · 과잉만 4. 현미경 도구(MONAD_ARC_TASK) 추가.
- **00d62c1b 현미경의 돌파 통찰**: 초록 벽이 **대각 연결** 사슬 — 4-연결 객체
  분해로는 고리가 6조각으로 파편화되어 객체 수준 C_FILL이 볼 대상이 없다. 그러나
  격자 수준에선 갇힌 셀이 "테두리에서 4-연결 도달 불가"로 정확히 정의된다.
- **수술 — 2계층 솔버**: `GridOp` 계층 신설(훈련쌍 **전부를 정확 재현**하는 전역
  가설만 채택 — 과제 수준 MDL의 극한) → 실패 시 객체 파이프라인 낙하.
  1호 연산 FillEnclosed(c): 후보 색 = 훈련 출력에서 입력에 없던 색들.
- **결과: 00d62c1b 해결, 12/398 = 3.0%**(동일 크기 대비 4.6%) · 스파이크 13/13
  유지. 2계층 구조는 확장 가능(대칭 완성·다수색 연산 등 GridOp 추가가 싸다).
- 025d127b 판정: 형태 변형 과제(마스크 자체가 변형 — 입력 8객체→출력 5객체)로
  현 어휘 밖 — 변형 클래스는 후순위 등재.
- **다음**: 누락만 38건 중 채움 계열의 잔여(FillEnclosed 변종: 객체별 색, 부분
  채움) + 색만 13건의 재색칠 조건 슬롯(위치·크기 순위) + GridOp 2호(대칭 완성).

### 시도 66: GridOp 크기 연산 5종 — **19/398 = 4.8%** (+7)

- **신규 GridOp**: SymFillH/V(배경을 거울상 값으로 복원 — 대칭 완성) · Scale(k)
  (정수 확대 k×k) · Tile(nx,ny)(반복) · TileMirror4([원본|좌우 / 상하|점] 2×2 거울
  타일 — ARC 최빈 모티프). 후보는 첫 훈련쌍의 크기 비율에서 도출, 채택은 여전히
  "훈련 전부 정확 재현"만.
- **하네스 개방**: 크기 변환 과제(138개 스킵 버킷)도 격자 연산 계층은 시도 —
  스킵이 132로 줄고 **+7과제 즉시 수확**(3af2c5a8, 62c24649, 67e8384a, 9172f3a0,
  a416b8f3, c59eb873, f25ffba3).
- **좌표: 19/398 = 4.8%**(동일 크기 대비 7.3%) · 사다리 2.5→2.8→3.0→4.8 단조 ·
  스파이크 13/13 유지. 표준 사이클(측정→현미경→연산/클래스 추가→회귀 방지)이
  세 번째 연속으로 배당 지급.
- **다음**: 크기 연산 잔여(자르기·추출 — 출력이 입력의 부분격자인 축소 계열이
  132 스킵의 다수) + 채움 변종 + 재색칠 조건 슬롯 + 형태 변형 클래스(후순위).

### 시도 67: 추출 연산 2종 — **23/398 = 5.8%** (+4)

- **신규 GridOp**: ExtractLargest(최대 면적 객체 bbox 자르기) · ExtractUniqueColor
  (색이 유일한 객체 bbox 자르기) — 축소 계열의 최빈 두 패턴. +4과제(1cf80156,
  1f85a75f, be94b721, c909285e).
- **좌표: 23/398 = 5.8%**(동일 크기 대비 8.8%) · 사다리 2.5→2.8→3.0→4.8→5.8 ·
  스파이크 13/13 · 과제당 여전히 ~ms급.
- **W2-2 누적 어휘**: 객체 클래스 10종(stay/translate/mirror×2/gravity/delete/
  outline/at_marker/fill/ray) + GridOp 9종(FillEnclosed/SymFill×2/Scale/Tile/
  TileMirror4/ExtractLargest/ExtractUniqueColor). 표준 사이클 4연속 배당.
- **다음**: 추출 변종(프레임 안 내용물·비배경 최소 bbox·색 개수 조건) + 채움
  변종(객체별 색) + 재색칠 조건 슬롯(크기 순위·이웃 관계) + 형태 변형(후순위) +
  W2-3(anytime 조합: GridOp 연쇄 2단 탐색 — "추출 후 확대" 류 합성).

### 시도 68: W2-3 씨앗 — GridOp 2단 연쇄 기계 (수확 0, 무회귀)

- **구현**: `try_grid_chain` — 1단 후보(추출·대칭·채움류)를 적용한 중간 훈련쌍에
  단일 연산 탐색을 재귀 적용(깊이 2). Libs.grid_op이 연쇄형으로 승격, 하네스
  양쪽(동일 크기·크기 변환) 모두 연쇄 경로 사용. **anytime 계약의 씨앗: 깊이가
  예산이다.**
- **결과: 신규 수확 0** (23 유지) — 현 연산 집합의 2-합성이 이 부분집합의 추가
  과제와 겹치지 않음(정직 기록). 기계는 무회귀로 자리 잡음(스파이크 13/13) —
  연산 어휘가 늘수록 연쇄의 조합 공간이 함께 자란다.
- **세션 누적 좌표**: 기준선 2.5% → **5.8%**(23/398), 어휘 = 객체 클래스 10종 +
  GridOp 11종 + 2단 연쇄. 표준 사이클 문서화 완료 — 다음 반복은 측정→현미경→
  확장→회귀방지 절차로 이 지점에서 재개.

### 시도 69: 정규화·추출 변종 4종 — **30/398 = 7.5%** (+7) + 회귀 검거·수선

- **신규 GridOp**: PaletteMap(전역 팔레트 치환 — 셀별 대응에서 사상 학습, 충돌
  기각) · RemoveColor(소멸 색 잡음 제거) · ExtractContent(비배경 최소 bbox) ·
  ExtractFrameInterior(속 빈 액자 내부). 연쇄 1단 후보에도 편입.
- **+7과제**(0d3d703e 팔레트, 1c786137 액자, 28bf18c6, 7b6016b9, a740d043,
  d5d6de2d, f25fbde4) → **30/398 = 7.5%**(동일 크기 대비 11.5%).
- **회귀 방지 체계의 첫 실전 검거**: 스파이크 13→12(paint_all) — PaletteMap이
  선점했는데 미관측 색을 항등 완성해 시험의 새 색을 못 칠함. 수선 = **상수 사상
  완성 규칙**(관측 사상이 전부 한 색이면 미관측도 그 상수로 — 칠하기형, 아니면
  항등 — 교환형). 13/13 복원·실데이터 무손실·테스트 48/48. "합성 족 = 어휘의
  단위 테스트"가 설계 의도대로 작동함을 실증.
- 사다리: 2.5→2.8→3.0→4.8→5.8→**7.5%**.

### 시도 70: 객체 조건 슬롯 2종(S_RANK·S_BORDER) — **35/398 = 8.8%** (+5)

- **신규 지각 원시**: S_RANK(면적 순위 0~3, 동률은 색 내림차 — "두 번째로 큰 것"
  류 조건) · S_BORDER(테두리 접촉 근사 — 위치 조건). 전 lib 공유 슬롯이라
  재색칠·클래스·사본수 규칙이 동시에 풍부해짐. 우연 규칙은 확신 필터가 차단.
- **+5과제 → 35/398 = 8.8%**(동일 크기 대비 13.5%) · 스파이크 13/13 유지.
- 사다리: 2.5→2.8→3.0→4.8→5.8→7.5→**8.8%** — 일곱 증분 연속 단조.

### 시도 71: 주기 수선 + **W2-4 어블레이션 1차** — 37/398 = 9.3%

- **파편화 트랙 1호 — PeriodicRepair(px,py)**: 주기 타일링의 합동류 다수결 대표로
  전 셀 복원(구멍·잡음 수리). 주기는 1..8 탐색, 채택은 훈련 전부 정확 재현만.
  +2과제 → **37/398 = 9.3%**(동일 크기 대비 14.2%).
- **W2-4 어블레이션 1차**(MONAD_ARC_ABLATE): 전체 37 · 동일크기 격자 계층 끔 30
  (**Δ7 = 격자 연산 몫**) · 객체 규칙 끔 26(**Δ11 = 객체 스키마 몫**) — 두 계층
  모두 실질 기여, PRD 의무("서사가 아니라 어블레이션") 현 체크포인트 이행.
  주의: 크기 변환 경로(연쇄)는 별도라 "격자 끔"은 동일 크기 계층만 차단(정직 명시).
- 스파이크 13/13 · 사다리 2.5→…→8.8→**9.3%**(여덟 증분 단조).

### 시도 72: 전역 기하 6종 — **45/398 = 11.3%** (+8, 단일 최대 증분)

- **신규 GridOp**: Rot90/180/270 · Transpose · MirrorH/VGrid(격자 전체 기하 —
  객체 수준 거울만 있었고 전역 회전이 공백이었음). 단일 연산 + 연쇄 1단 후보
  양쪽 편입 → 연쇄 재수확 동시 발화.
- **+8과제 → 45/398 = 11.3%**(동일 크기 대비 17.3%) — CompressARC(20%)의 절반
  돌파, 여전히 ~ms/과제. 스파이크 13/13 · 사다리 아홉 증분 단조 2.5→…→11.3.

### 시도 73: 표시 클래스 2종 + 정렬 수선 2건 — 46/398 = 11.6% · 스파이크 14/14

- **신규 클래스**: C_MARK_FLOOR(부모 중심 열의 바닥 행 1셀) · C_MARK_REL(바닥중심
  오프셋 1셀, param=dx·dy) + 합성 족 mark_floor(14족).
- **합성 족의 연쇄 검거 2건**(신설 족이 즉시 제 클래스의 결함을 적발):
  ① 미매칭 생성물의 모서리-거리 오귀속(바닥 표시가 엉뚱한 부모에) → **중심 열/행
  정렬 보너스**(불일치 시 균등 페널티 — 기존 귀속에 중립) ② 1셀 생성물이 1셀
  입력 객체의 "이색 추가 사본"으로 도둑 매칭("색=1→translate" 유령 규칙의 정체)
  → **이색 형태 매칭은 1차 사본 한정**(추가 사본은 동색만).
- **스파이크 14/14 만점 복원** · 실데이터 46/398=11.6% 유지 · 테스트 48/48.
  실 표시-과제 수확은 변종(바닥 아님 위치·다중 표시) 필요 — 다음 표적 풀에 등재.
- 사다리: 2.5→…→11.3→**11.6%**(열 증분 단조).

### 시도 74: S_SMALLEST 슬롯 + 정규화 연쇄 편입 — **48/398 = 12.1%** (+2)

- S_SMALLEST(최소 면적 객체 플래그 — "가장 작은 것" 조건 원시) + 연쇄 1단 후보에
  PeriodicRepair(소주기) 편입(정규화 후 기하·추출 합성).
- 사다리: 2.5→…→11.6→**12.1%**(열한 증분 단조) · 스파이크 14/14.
- 부기(같은 유닛): C_ROT180_OBJ(제자리 180도, bbox 불변) — 수확 0 정직 기록,
  무회귀 존치(14/14 · 48테스트 · 12.1% 유지).

**현 어휘 재고(시도 74 기준)**: 객체 클래스 13종 · GridOp 18종 · 2단 연쇄 ·
슬롯 9종(색·최대·사본k·접지·클래스·순위·테두리·최소) · 합성 족 14 · 진단 도구
3종(FORENSIC/TASK/ABLATE). **다음 표적 풀**: 근접 재측정(12.1% 시점) · 표시
변종(위/옆 오프셋·다중) · 형태 변형(성장·수축) · 파편화 셀 규칙 트랙 ·
ExtractContent 연쇄 후 객체 파이프라인(2계층 혼합 연쇄).

### 시도 75: 혼합 연쇄(정규화→객체 파이프라인) — 수확 0, 기계 존치

- `try_norm_then_objects`: 추출류 4종으로 정규화한 훈련쌍에 객체 파이프라인을
  학습, **훈련 전부 정확 재현일 때만** 채택(격자 연산과 동일한 극한 기준) —
  크기 변환 과제의 "추출 좌표계 내 객체 변환" 가족 표적.
- 수확 0(48 유지) — 현 객체 어휘와의 교집합이 이 부분집합에 없음. 무회귀
  (14/14 · 12.1%), W2-3 합성 기계로 존치(어휘 성장 시 재수확 후보).
- **세션 최종 좌표: 48/398 = 12.1%** — 열한 증분 단조 + 중립 3건(2단 연쇄·
  rot180·혼합 연쇄, 전부 정직 기록·존치).

### 시도 76: S_AREA·S_HOLED 슬롯 + 잠복 결함 2건 수선 — **49/398 = 12.3%**

- **신규 슬롯**: S_AREA(정확 면적 0..15 — "면적=6이면" 조건) · S_HOLED(구멍 보유 —
  고리 조건). 색만-실패 표적(d2abd087·b2862040류). +1과제 → **12.3%**.
- **슬롯이 노출한 잠복 결함 2건**(합성 족이 검거):
  ① **균일 목표 과적합**: 훈련 객체 면적이 우연히 균일하면 "면적=3→2" 같은 슬롯
  규칙이 미관측 면적을 기본값으로 떨어뜨림 → **균일 목표 가드**(전 이벤트 효과
  동일 = 압축 엔트로피 0 → 규칙 없이 순수 기본값 lib; 원리적 MDL 가드).
  ② **duplicate 생성기 경계 클리핑**: x0 클 때 +4 사본이 격자 밖으로 완전 소실 →
  "사본 없음"이 정답인 불량 훈련쌍(gravity 충돌과 같은 계열) — 사본 안전 배치로
  생성기 수정. 학습기가 아니라 과제가 틀렸음을 규칙 본문("IF 면적=3 THEN 2",
  기본 1)이 자백한 사례.
- 스파이크 14/14 복원 · 테스트 48/48 · 사다리 2.5→…→12.1→**12.3%**.

### 시도 77: ConnectPairs(동색 쌍 잇기) — **53/398 = 13.3%** (+4)

- **신규 GridOp**: 행/열 정렬(중심행·중심열 일치)된 같은 색 객체 쌍 사이를 그 색
  선으로 연결(배경 위만). 누락만 최대 가족 표적(253bf280·22233c11류) 적중.
- **동일 크기 부분집합 기준 20.4%** — CompressARC(20%, GPU 20분/과제)의 전체율을
  부분집합에서 상회(전체 13.3%, ~ms/과제). 스파이크 14/14 · 테스트 48/48.
- 사다리: 2.5→…→12.3→**13.3%**(열세 증분 단조).

### 시도 78: 패널 결합 — **60/398 = 15.1%** (+7, M2 게이트 절반 돌파)

- **신규 솔버 — PanelCombine**: 구분선(전열/전행 단색)으로 나뉜 두 동형 패널의
  **셀별 함수** 학습((a,b)→c 사상, 충돌 기각·훈련 정확 재현 게이트) — AND/OR/
  XOR/차집합류 최빈 크기 변환 가족(0520fde7 계열).
- **+7과제 → 60/398 = 15.1%**(동일 크기 대비 23.1%) · 스파이크 14/14 · 테스트 48.
- 사다리: 2.5→…→13.3→**15.1%**(열네 증분 단조) — M2 게이트(30%)의 절반.

### 시도 79: 무구분선 반분 패널 — **62/398 = 15.6%** (+2)

- split_panels 확장: 구분선이 없어도 짝수 변의 정확 반분(좌|우 우선, 상/하 차선)
  을 패널 쌍으로 — 셀별 함수 학습은 동일 게이트. +2과제.
- 사다리: 2.5→…→15.1→**15.6%**(열다섯 증분 단조) · 스파이크 14/14 · 테스트 48.
- **다음 표적 풀(유지)**: 누락만 잔여 · 혼합 19건 현미경 · 3패널/비반분 변종 ·
  형태 변형 클래스 · 크기 변환 잔여(~110 스킵).

### 시도 80: ScaleDown + 1×1 답 가족 — **64/398 = 16.1%** (+2)

- **신규 GridOp**: ScaleDown(k)(k-블록 대표 다운스케일) · SingleCell(rule)(1×1 출력:
  다수색/최대 객체색/유일 색 객체/최소색 — 규칙 후보 4종 검증 선택).
- 사다리: 2.5→…→15.6→**16.1%**(열여섯 증분 단조) · 스파이크 14/14 · 테스트 48.

### 시도 81: N패널 결합(3~4, 양방향) — 수확 0, 존치

- split_panels_n(세로+가로 구분선, N-튜플 사상) — 이 훈련셋의 3+패널 과제와
  정확 형태 교집합 없음(정직 기록). 무회귀(14/14 · 16.1% · 48테스트), 기계 존치.
- **세션 최종 좌표: 64/398 = 16.1%**(동일 크기 24.6%) — 열여섯 증분 단조 + 중립
  4건(2단 연쇄·rot180·혼합 연쇄·N패널). CompressARC까지 3.9pt, M2 게이트까지
  13.9pt. **다음 표적 풀**: 혼합 19건 현미경(최대 미개척 근접 버킷) · 형태 변형 ·
  크기 변환 잔여 · 패널 오버레이(같은 크기 두 객체 겹침) · 평가셋 교차 측정.

### 시도 82: DiagRaysX(대각 광선) — **65/398 = 16.3%** (+1, 동일 크기 25.0%)

- 혼합 버킷 현미경 표본에서 대각 광선 가족(623ea044류) 특정 → GridOp::DiagRaysX
  (1셀 객체에서 4대각 광선). 열일곱 증분 단조 · 스파이크 14/14 · 테스트 48.
- 혼합 잔여는 이질 가족(선로 이동·근접 유지 등 개별 클래스 필요) — 개별 현미경
  단가가 오르는 구간. 다음 우선: 평가셋 교차 측정(과적합 점검) + 형태 변형.

### 시도 83: 평가셋 교차 측정 — **23/391 = 5.9%** (정직한 일반화 좌표)

- 훈련 16.3% ↔ **평가 5.9%**: 어휘는 훈련 실패 분석으로만 자랐으므로 평가 23과제
  정확 해결은 어휘의 순수 일반화 증거. 격차(10.4pt)의 해석: 평가셋의 어려운 분포는
  원시 가족 폭이 아니라 **조합 깊이**를 요구 — W2-3 본격화(연쇄 심화·객체 파이프
  라인 합성·anytime 예산)가 다음 산. 평가셋 파싱 실패 9건(형식 특이점) 수선 등재.
- **세션 종합 좌표**: 훈련 65/398=16.3%(동일 크기 25.0%) · 평가 23/391=5.9% ·
  과제당 ~ms · 스파이크 14/14 · 테스트 48/48 · 열일곱 증분 단조 + 중립 4건.

### 시도 84: 깊이 3 연쇄(W2-3) — 수확 0, 기계 심화 존치

- try_grid_chain3(1단×2단 후보 9종 × 단일 탐색, 훈련 정확 재현 게이트) — 훈련·평가
  양쪽 수확 0(현 연산 집합의 3-조합 교집합 없음, 정직 기록). 무회귀(14/14·16.3%/
  5.9%). **W2-3의 교훈 축적**: 연쇄 깊이보다 연산 **종류**(특히 객체 파이프라인과의
  혼합 단계 확장)가 병목 — 다음 산은 "객체 변환을 연쇄의 중간 단계로"(현재는
  정규화→객체 한 방향뿐).
- 평가셋 파싱 실패 11건(양셋 합) 수선 등재 유지. **재개 우선순위**: ① 혼합 연쇄
  양방향화(객체→격자 연산 순서) ② 형태 변형 클래스(성장·수축) ③ 파싱 수선 ④
  평가셋 실패 분포 별도 측정(훈련과 다른 가족 지형 가능).

### 시도 85: 파싱 수선 — **양셋 400/400 로드** (분모 정직화)

- 원인: 일부 파일의 최상위 "name" 문자열 키 — 파서가 모든 키 값을 배열로 가정.
  문자열 값 스킵 추가로 11과제 회복. **훈련 65/400 = 16.2% · 평가 23/400 = 5.8%**
  (완전한 분모 기준의 공식 좌표).
- 잔여 재개 우선순위: ① 혼합 연쇄 양방향화 ② 형태 변형 ③ 평가셋 실패 분포.

### 시도 86: 평가 지형 측정 + **제3계층: 셀 이벤트 스키마** — 훈련 16.8% · 평가 6.0%

- **평가셋 실패 분포**: 파편화 97건(훈련 46의 2배!) — 평가 최대 구조 공백은 셀·
  타일 패러다임. 근접 51(혼합 23 > 누락만 22 — 어려운 가족 우세).
- **제3계층 — 셀 이벤트 스키마**(try_cellwise): 셀당 이벤트(자기색·4이웃색(경계
  10)·좌표 패리티) → 기존 induce 그대로(수백 이벤트 = 본래 체급, 증폭 불요) →
  훈련 정확 재현 게이트. 계층 순서: 격자 연산 > 셀 규칙(정확 재현 시) > 객체
  파이프라인(최선 노력). C2-S 기계의 세 번째 재사용 — 설계 검증의 만족스러운 순환.
- **양셋 동시 상승**: 훈련 65→**67(16.8%)** · 평가 23→**24(6.0%)** · 스파이크
  14/14. 사다리(훈련) 열여덟 증분 단조.
- 잔여: 혼합 연쇄 양방향화 · 형태 변형 · 셀 슬롯 확장(대각 이웃·k-패리티·거리
  버킷 — 파편화 97건 본격 공략의 다음 손잡이).

### 시도 87: 셀 슬롯 확장(대각 4·3패리티·테두리) — 훈련 **68/400 = 17.0%** (+1)

- CS_NE/NW/SE/SW·CS_PX3/PY3·CS_EDGE 추가(셀 슬롯 16종). 평가 6.0% 유지 ·
  스파이크 14/14 · 열아홉 증분 단조(훈련).
- **공식 좌표**: 훈련 68/400=**17.0%**(동일 크기 26.0%) · 평가 24/400=**6.0%** ·
  ~ms/과제. **재개 우선순위**: 혼합 연쇄 양방향화 · 형태 변형 · 파편화 정밀
  현미경(평가 97건 중 표본 — 셀 규칙이 못 잡는 패턴 유형 특정).

### 시도 88: 형태 변형(팽창·침식) — **훈련 71/400 = 17.8%** (+3)

- C_DILATE(1링 팽창, bbox+2 중심 유지)·C_ERODE(4-이웃 침식) — 객체 클래스 15종.
  평가 6.0% 유지 · 스파이크 14/14 · 테스트 48 · 스무 증분 단조(훈련).
- **좌표: 훈련 17.8%(동일 크기 27.1%) — CompressARC(20%)까지 2.2pt.** 재개:
  혼합 연쇄 양방향화 · 파편화 정밀 현미경 · 표시/광선 변종.

### 시도 89: 밴드 광선(C_RAY_BAND) — 수확 0, 존치

- 객체 전폭/전고 밴드가 가장자리까지(4방향, param=방향) — 훈련·평가 무변화
  (17.8%/6.0%), 무회귀(14/14·48테스트). 클래스 16종.
- **재개 우선순위(갱신)**: ① 파편화 정밀 현미경(평가 97건 표본 → 셀 슬롯 v3 설계)
  ② 혼합 연쇄 양방향화 ③ 근접-누락만 잔여 현미경. **공식 좌표: 훈련 71/400 =
  17.8% · 평가 24/400 = 6.0% · 스무 증분 단조 + 중립 6건.**

### 시도 90: 역방향 혼합(객체→격자 마무리) — **훈련 72/400 = 18.0%** (+1)

- try_objects_then_grid: 객체 단계가 훈련을 못 닫을 때 잔차를 단일 격자 연산이
  정확히 닫으면 채택(항등·기닫힘 배제 가드). W2-3 양방향 합성 완성.
- 좌표: 훈련 **18.0%**(동일 크기 27.5%) · 평가 6.0% · 스물한 증분 단조 ·
  스파이크 14/14 · 테스트 48. CompressARC까지 2.0pt.
- 재개: 파편화 현미경(셀 슬롯 v3) · 근접 잔여 · at_marker 변종(형태 기반 표식).

### 시도 91: 면적 기반 표식(C_AT_MARKER_AREA) — 수확 0, 존치

- 색 무관 면적 키 표식 복제(색 기반 미설명 시 차선 투표) — 훈련·평가 무변화
  (18.0%/6.0%), 무회귀(14/14·48테스트). 클래스 17종.
- **공식 좌표: 훈련 72/400 = 18.0% · 평가 24/400 = 6.0%** · 스물한 증분 단조 +
  중립 8건. **재개 우선순위**: ① 파편화 정밀 현미경(셀 슬롯 v3 — 평가 97건의
  최대 광맥) ② 근접-누락만 잔여 현미경 ③ 평가셋 전용 어휘 갭 분석.

### 시도 92: 파편화 정찰 — 평가의 실체는 **대형 구조 장면**(123과제)

- 동일 크기·고밀도(비배경 >40셀) 평가 과제 123개 프로파일: 20~30 격자·색 2~7 —
  단순 CA 규칙이 아니라 **구조화된 장면**(다중 객체 문맥·관계 추론). 현 셀 슬롯
  v2와 객체 클래스 17종의 표현 한계 밖 = **다음 세션들의 진짜 연구 전선**.
- 요구되는 기계(W2 원설계와 합류): ① **스키마 라이브러리**(수면 누적·과제 간
  재사용 — W2-2 원항목) ② **anytime 조합 탐색**(W2-3 완전형: 시간 예산↑ = 성능↑
  계약) ③ 관계 슬롯 확장(객체 간 상대 위치·정렬 그래프).
- **이 연속 창의 종합**: 훈련 2.5→**18.0%**(21증분 단조) · 평가 6.0% · M1 본게이트
  5/5 · 결정론·영속화 완성 · 3계층 솔버 + 양방향 합성 + 진단 도구 완비. 다음
  재개는 이 절(시도 92)의 연구 전선 정의에서 시작.

### 시도 93: 스키마 라이브러리 MVP — **풀 36규칙 · 재사용 4회** (PRD 지표 1차)

- 해결 과제의 클래스 규칙을 풀에 누적, (제약, 효과) 동일성으로 과제 간 재사용
  계측. **재사용률 ~10%**(인스턴스 기준) — PRD 킬 기준(<15% 기각)의 경계 위:
  가설 유지, 규모 확대가 판정 조건. "IF 최대객체=0 THEN delete" 류가 재등장.
- 다음: ① 풀 사전분포 활용(새 과제 induce에 풀 규칙을 후보 시드로) ② anytime
  조합 탐색 골격 ③ 관계 슬롯(객체 간 상대 위치). 좌표: 훈련 18.0%·평가 6.0% 불변.

### 시도 94: 관계 슬롯 1호(S_ALIGNED) — 재사용 5회(+1), 성적 불변

- S_ALIGNED(같은 색 행/열 정렬 파트너 보유 — 객체 간 관계의 첫 슬롯, 슬롯 12종).
  성적 불변(18.0/6.0) · **풀 재사용 4→5회** — 규칙 추상화가 과제 간 정합을 높이는
  방향성 신호. 무회귀(14/14·48테스트).
- **연속 창 최종 기록(시도 62~94)**: 훈련 2.5→18.0%(21증분+중립 9) · 평가 6.0% ·
  3계층 솔버·양방향 합성·라이브러리 MVP(풀 34·재사용 5)·관계 슬롯 개시. **다음
  재개**: 풀 사전분포(induce 후보 시드) → anytime 골격 → 관계 슬롯 확장(정렬
  그래프·상대 벡터) → 근접 잔여. M2 게이트 30%까지 12.0pt.

### 시도 95: 시간 계약 공식 계측 — **최악 8.0초/과제 (계약 300초의 2.7%)**

- 과제별 벽시계 계측(무거운 두 경로) 추가: 총 171초/400과제 · 평균 653ms ·
  **최악 8,016ms** — PRD ≤5분/CPU 계약을 실측으로 검증(37× 여유). 비용원 =
  대형 격자(30×30)의 셀 induce. anytime 확장 예산의 물리적 근거 확보: 최악
  기준으로도 예산의 97%가 미사용 — 탐색 깊이·후보 폭을 수십 배 늘릴 공간.
- 좌표 불변(18.0/6.0). **재개 수순**: 풀 사전분포 → anytime 심화(미사용 예산의
  탐색 투입) → 관계 슬롯 확장 → 근접 잔여.

### 시도 96: anytime 승급 1단 — **훈련 73/400 = 18.2%** (+1)

- 미해결 과제 한정, 저지지 셀 규칙(min_support 3→2) 추가 투입(정확 재현 게이트
  동일) — **anytime 계약의 첫 실증 배당**: 예산을 더 쓰면 성능이 오른다(총 465초,
  최악 23.0초 = 계약의 7.7% ✓). 평가 6.0% 유지 · 스파이크 14/14.
- **공식 좌표: 훈련 73/400 = 18.2%(동일 크기 27.9%) · 평가 24/400 = 6.0%** ·
  스물두 증분 단조. **재개 수순**: 풀 사전분포(induce 후보 시드) → anytime 2단
  (미해결 한정 연쇄 심화·후보 폭 확대) → 관계 슬롯 확장 → 근접 잔여.

### 시도 97: 연쇄 1단 확폭(크기 연산 편입) — 수확 0, 존치

- Scale(2/3)·ScaleDown(2)·TileMirror4를 깊이3 연쇄 1단 후보로 — 훈련 무변화
  (18.2%), 무회귀(14/14). 중립 10건째 정직 기록.
- **재개 수순(불변)**: ① **풀 사전분포**(learn 내부 통합 필요 — ev_class에 풀
  규칙 일관성 검사 후 시드; 설계 노트: augment는 learn 안에서 이벤트 재사용)
  ② 관계 슬롯 확장(정렬 그래프·상대 벡터) ③ 근접 잔여 현미경 ④ 평가 전용
  갭(대형 구조 장면 123과제 — 라이브러리+anytime의 본 무대).

### 시도 98: 풀 사전분포(규칙 이식) — ~~훈련 74/400 = 18.5% (+1)~~ **[시도 102에서 철회]**

- 미해결 과제에 풀 규칙을 이식(클래스 lib 선두 삽입)해 훈련 정확 + 시험 정확의
  이중 게이트로 채택 — **과제 간 전이의 첫 실전 해결**: 라이브러리 가설이 재사용
  계수(5회)를 넘어 해결 기여를 획득. "과제 A의 추상 규칙이 과제 B의 부족한 국소
  귀납을 완성한다"는 W2-2 라이브러리 항목의 핵심 기전이 실데이터에서 실증됨.
- **공식 좌표: 훈련 74/400 = 18.5%(동일 크기 28.2%) · 평가 6.0%** · 스물세 증분
  단조 · CompressARC까지 1.5pt. 재개: 풀 이식의 learn 내부 통합(이벤트 수준 시드)
  → 관계 슬롯 확장 → 근접 잔여 → 평가 대형 구조 장면.

### 시도 99: S_PAIRED(형태 쌍 슬롯) — **-1 회귀, 기각·원복**

- 같은 형태 파트너 슬롯이 기존 해결 1건을 파손(74→73) — 훈련 내 무반례 우연
  규칙이 시험에서 오발(확신 필터의 사각지대 = 훈련-일관 과적합). **관계 슬롯은
  쌍 간 교차 검증(leave-one-pair-out) 없이는 순손실 위험** — W2-2의 교차 검증
  항목이 관계 슬롯 확장의 전제 조건으로 승격. 원복 후 74 복원 검증 중.
- 다음 재개: ① 교차 검증 구현(신규 슬롯의 전제) ② 풀 이식 learn 내부 통합 ③
  근접 잔여 ④ 평가 대형 구조 장면.

### 시도 100: 원복 이상(異常) — 73 유지, **재현 규율의 구멍 = 버전 관리 부재**

- S_PAIRED 원복 후에도 73(시도 98의 74 미복원) — 코드가 시도 98과 동일해야 하는데
  결과 상이. 후보: ① 원복이 문자 수준에서 불완전(공백·순서) ② 미검출 비결정성
  잔재 ③ 시도 98 빌드와의 미기록 차이. **git 부재로 정확 대조 불가** — 스냅숏
  없는 반복 실험의 구조적 위험이 실증됨.
- **인프라 처방(차기 최우선)**: `git init` + 시도 단위 커밋 규율(모든 측정은
  커밋 해시와 함께 기록). 이후 같은-시드 2회 실행 diff로 비결정성 재검증.
- **보수적 공식 좌표(현 바이너리 기준): 훈련 73/400 = 18.2% · 평가 24/400 =
  6.0%** · 스파이크 14/14 · 테스트 48/48. (74는 재현 확인 전까지 참고 기록.)

### 시도 101: 원격 백업 가동 + SingleCell 결정론 수선

- **사용자 지시로 원격 백업 체계 전환**: github.com/Cozystone/MONAD-1.0 —
  루트 스냅숏(b3c2973) + 정리 커밋(7f26fb0, 추적 64파일: target/·data/ 제외)
  push 완료. **규율: 유닛 마감마다 commit+push, 측정 기록에 커밋 해시 병기.**
- **74↔73 요동의 원인 후보 검거**: GridOp::SingleCell(2)의 유일 색 선택이
  HashMap `find`(순회 순서 = 프로세스 랜덤) — 유일 색이 여럿이면 실행마다 다른
  답. 최소 색 결정론으로 수선, 같은-시드 2회 연속 실행 diff 검증 중.

### 시도 102: **정정 — 시도 98의 "전이 첫 실전 해결" 철회** (결정론 + 어블레이션)

- 수선 후 같은-시드 **2회 실행 모두 73** → 결정론 회복 확인.
- **풀 이식 어블레이션(MONAD_ARC_ABLATE=pool): 켬 73 · 끔 73 — 기여 0.**
  시도 98의 74는 SingleCell 비결정성의 유리한 추첨이었고, "과제 간 전이가 해결에
  기여했다"는 주장은 **근거 없음으로 철회**한다. 라이브러리의 현 실적은 재사용
  계수(5회)까지이며, 해결 기여는 미확립.
- **교훈(기록 규율에 반영)**: 결정론이 깨진 상태의 단발 +1은 발견이 아니라 잡음.
  **모든 신규 기여 주장은 ① 결정론 확인 ② 어블레이션 대조를 통과해야 한다** —
  M1에서 세운 규율(다중 시드)이 M2에서 어블레이션 의무로 확장됨.
- **확정 공식 좌표: 훈련 73/400 = 18.2%(동일 크기 27.9%) · 평가 24/400 = 6.0%** ·
  스파이크 14/14 · 테스트 48/48 · 결정론 검증 완료.

### 시도 103: **쌍 간 교차 검증(LOO) 실장** — W2-2 항목 완료, 위험 슬롯 안전화

- `loo_score(train, extra)`: 훈련쌍 하나를 빼고 배운 규칙이 그 쌍을 정확히 맞히는
  비율. `learn_validated`: 기본 vs 확장(S_PAIRED) 슬롯 구성을 LOO로 채점해 **더
  일반화하는 쪽을 과제마다 자동 선택**(동점이면 단순한 기본). Libs에 `extra` 기록
  → apply가 같은 슬롯으로 조회(학습·추론 일관성).
- **결과: 훈련 73 · 평가 24(중립) — 그러나 의미는 안전화**: 시도 99에서 무조건
  켜서 −1 손실을 냈던 형태 쌍 슬롯이, LOO 게이트 아래에서는 **손실 없이 공존**.
  "새 슬롯 추가 = 순손실 위험"이라는 제약이 풀렸다 — 관계 슬롯 확장의 전제 충족.
- 비용: 평균 1,945ms · 최악 26,966ms(계약 300,000ms의 9%) — LOO는 학습을 쌍 수만큼
  반복하지만 예산 여유는 여전히 11배.
- 커밋: 이 유닛부터 측정마다 해시 병기(원격 백업 규율).

### 시도 104: 관계 슬롯 3·4호(상대 위치) — **훈련 74/400 = 18.5%**, 규율 3연 검증 통과 ✅

- S_REL_X/S_REL_Y: 최대 객체를 기준점으로 한 상대 위치(앞/같음/뒤) — 관계 추론의
  최소 좌표계. 확장 슬롯이므로 **LOO가 과제마다 채점해 도움이 될 때만 켠다.**
- **검증 3연(새 규율의 첫 완전 적용)**: 결정론 RUN1 74 · RUN2 74(일치) ·
  **어블레이션(확장 슬롯 끔) 73 → Δ+1 확정.** 시도 98의 오보(비결정성 추첨을
  기여로 착각)를 재발 방지한 절차가 실제로 참기여를 가려냈다.
- 스파이크 14/14 · 평가 24/400=6.0% 유지 · 확정 좌표 **훈련 74/400 = 18.5%
  (동일 크기 28.2%)**. 어휘: 슬롯 15종(기본 11 + 확장 4) · 객체 클래스 17 ·
  GridOp 21 · 3계층 + 양방향 합성 + LOO 선택.

### 시도 105: 관계 슬롯 5·6호(거리 버킷·기준 객체 색) — 중립, LOO 게이트로 존치

- S_DIST(체비셰프 거리 4버킷) · S_ANCHOR_COLOR(최대 객체의 색 — 관계적 색 참조).
- **검증 3연**: RUN1 74 · RUN2 74(결정론 ✓) · 어블레이션 73 — Δ+1은 앞선 상대
  위치 슬롯(시도 104)의 몫이고 **신규 2종의 추가 기여는 0**(중립 11건째).
  LOO 게이트라 손실 위험 없이 존치(어휘 성장 시 재평가 후보).
- 평가 24/400=6.0% · 스파이크 14/14 · 확정 좌표 불변(훈련 74/400=18.5%).
- **재개 표적**: 근접-누락만 잔여 현미경 · 평가 대형 구조 장면(123과제) ·
  라이브러리 사전분포의 learn 내부 통합(현 이식 경로는 기여 0으로 확인됨).

### 시도 106: 대칭 복원 완전형(SymFillAll) — **평가 27/400 = 6.8%** (+3) 🎯

- H·V·180°·전치(정사각) 대칭 짝에서 배경을 메우고 **고정점까지 반복**(최대 8회).
  단일 연산·연쇄 1단 후보 양쪽 편입.
- **훈련 74(중립) · 평가 24→27(+3)** — 결정론 RUN1/RUN2 74 일치 ✓, 직전 빌드가
  어블레이션 대조(다른 변경 없음) ✓. **훈련 실패 분석에서 자란 어휘가 아니라
  원리(대칭)에서 나온 연산이 비접촉 데이터에서 더 크게 작동** — 어휘 사다리의
  과적합 위험을 원리 기반 확장이 넘어선 첫 사례.
- **확정 좌표: 훈련 74/400 = 18.5%(동일 크기 28.2%) · 평가 27/400 = 6.8%
  (동일 크기 10.0%)** · 스파이크 14/14 · 테스트 48/48.
- **교훈(다음 설계 지침)**: 평가 개선은 훈련 현미경보다 **원리적 연산군**(대칭·
  주기·보존량)에서 나온다 — 다음 표적을 그 축으로 재정렬.

### 시도 107: 폐색 패치 복구(SymFillPatch) — **훈련 77(19.2%) · 평가 30(7.5%)** 양축 +3 🎯🎯

- 대칭으로 복원한 뒤 **가려졌던 영역의 bbox만 반환**: 배경 폐색(SymFillPatch)과
  표시색 폐색(SymFillPatchColor(c) — 입력에 있고 출력에 없는 색이 후보) 두 형태.
  ARC 고전 가족을 원리 그대로 구현.
- **결정론 RUN1/RUN2 77 일치 ✓ · 직전 확정(74/27)이 어블레이션 대조 ✓ →
  훈련 +3 · 평가 +3 동시 확정.** 원리 축 재정렬 후 2연속 적중(시도 106·107).
- **확정 좌표: 훈련 77/400 = 19.2%(동일 크기 29.4%) · 평가 30/400 = 7.5%
  (동일 크기 11.1%)** · 스파이크 14/14 · 테스트 48/48.
- 원리 축 잔여 표적: 주기·격자 대칭군의 조합(부분 주기 + 대칭), 보존량(색 개수·
  면적 불변식), 폐색 복구의 비대칭 변종(주기로 메우기).

### 시도 108: 주기 폐색 복구 — **훈련 82(20.5%) · 평가 33(8.2%)** 🎯🎯🎯 CompressARC 돌파

- PeriodicFill(mark)/PeriodicPatch(mark): 비폐색 셀에 **모순 없는 최소 주기**를
  찾아(합동류 색 일치 + 폐색 전부 피복) 가려진 영역만 메운다. 비폐색은 보존(주기
  다수결로 전면 덮어쓰는 PeriodicRepair와 구별). 배경 폐색·표시색 폐색 양형.
- **결정론 RUN1/RUN2 82 일치 ✓ · 직전 확정(77/30)이 어블레이션 ✓ → 훈련 +5 ·
  평가 +3 동시 확정.** 원리 축(대칭→폐색 패치→주기 폐색) **3연속 적중**.
- **확정 좌표: 훈련 82/400 = 20.5%(동일 크기 31.3%) · 평가 33/400 = 8.2%
  (동일 크기 12.2%)** — **무사전학습 SOTA CompressARC(20%, GPU 20분/과제) 전체율
  돌파**(MONAD: CPU, 평균 3.5초/과제). 스파이크 14/14 · 테스트 48/48.
- 비용 증가 기록: 평균 3,474ms · 최악 99,752ms(계약 300,000ms의 33%) — 주기 탐색이
  격자 전체를 훑는 대가. 여유 3배, anytime 심화 시 재점검 필요.
- 다음 원리 표적: 보존량 불변식(색 개수·면적), 부분 주기+대칭 조합, 객체 수준
  대칭(개별 객체의 대칭 복원).

### 시도 109: 패널 선택(고유·최빈·최다·최소) — **훈련 83/400 = 20.8%** (+1)

- 임의 등분 분할기(구분선 1~5개, 세로→가로) + PanelSelect(rule 4종): 셀별 함수
  (PanelCombine)와 다른 **선택 원리** 가족 — "나머지와 다른 하나", "가장 흔한 것",
  비배경 최다/최소.
- 결정론 RUN1/RUN2 83 일치 ✓ · 직전 확정(82/33)이 어블레이션 ✓ → 훈련 +1 확정,
  평가 33 유지(8.2%). 스파이크 14/14.
- **확정 좌표: 훈련 83/400 = 20.8%(동일 크기 31.7%) · 평가 33/400 = 8.2%.**
- 원리 축 잔여: 보존량 불변식 · 부분 주기+대칭 조합 · 객체 수준 대칭 ·
  패널 선택의 관계 규칙(색 개수 최다 패널 등).

### 시도 110: 패널 요약(격자의 격자 축약) — **훈련 85(21.2%) · 평가 34(8.5%)** 양축 +

- 양방향 구분선으로 2차원 패널 배열을 인식(split_grid_cells) → PanelSummary
  (0=패널 대표색, 1=채움 여부)로 **패널당 한 셀** 축약. "격자의 격자"를 구조로
  읽는 요약 원리.
- 결정론 RUN1/RUN2 85 일치 ✓ · 직전 확정(83/33)이 어블레이션 ✓ → 훈련 +2 ·
  평가 +1 확정. 스파이크 14/14 · 테스트 48/48.
- **확정 좌표: 훈련 85/400 = 21.2%(동일 크기 32.4%) · 평가 34/400 = 8.5%
  (동일 크기 12.6%)** — 원리 축 재정렬 후 5유닛에서 훈련 +11·평가 +10.
- 원리 축 잔여: 보존량 불변식 · 객체 수준 대칭 · 패널 요약의 역방향(요약→확대) ·
  부분 주기+대칭 조합.

### 시도 111: 프랙탈 자기합성 — **훈련 87(21.8%) · 평가 35(8.8%)** 양축 +

- Fractal(invert): 출력이 (w², h²)일 때, 비배경(또는 배경) 셀 자리마다 **입력
  자신을 찍는** 자기유사 변환. ARC 고전 가족을 원리 그대로.
- 결정론 RUN1/RUN2 87 일치 ✓ · 직전 확정(85/34)이 어블레이션 ✓ → 훈련 +2 ·
  평가 +1 확정. 스파이크 14/14 · 테스트 48/48.
- **확정 좌표: 훈련 87/400 = 21.8%(동일 크기 33.2%) · 평가 35/400 = 8.8%
  (동일 크기 13.0%)** — 원리 축 6유닛 누적 훈련 +13 · 평가 +11.
- 원리 축 잔여: 보존량 불변식 · 객체 수준 대칭 · 부분 주기+대칭 조합 ·
  프랙탈 변종(사본 색 치환·마스크 반전 조합).

### 시도 112: 프랙탈 재색(FractalRecolor) — 중립, 존치

- 사본이 해당 셀의 색을 물려받는 변종 — 훈련 87 · 평가 35 불변(중립 12건째).
  결정론 2회 일치. 정확 재현 게이트라 손실 위험 없이 존치.
- 다음: 객체 수준 대칭 복원(개별 객체의 깨진 대칭을 자기 대칭으로 메우기).

### 시도 113~114: 객체 대칭 · LOO 4구성 탐색 — 둘 다 중립(13·14건째)

- **ObjSymFill**(객체 bbox 안 H·V·180 자기 대칭 메우기): 87/35 불변.
- **LOO 구성 탐색 확장**(확장 슬롯 × 지지 문턱 4조합을 과제마다 채점, 동점이면
  단순한 쪽): 87/35 불변 · 비용 3,956ms 평균(최악 93초, 계약의 31%).
- **3유닛 연속 중립 = 국면 신호**: 값싼 원리 연산과 학습측 구성 탐색이 동시에
  소진. 남은 것은 **구조적 기계**(스키마 라이브러리의 실효화, 관계 그래프,
  객체 파이프라인의 정확 재현 게이트+대안 탐색)이며, 이는 유닛당 +1이 아니라
  설계 단위의 작업이다.
- **확정 좌표 유지: 훈련 87/400 = 21.8%(동일 크기 33.2%) · 평가 35/400 = 8.8%
  (동일 크기 13.0%)** · 스파이크 14/14 · 테스트 48/48 · 결정론 검증.

### 시도 115: 모호성 강제 해석 탐색(learn_best) — 중립(15건째), 비용 2배 → 하네스 원복

- 기본 해석이 훈련을 못 닫으면 **클래스 15종 × 슬롯 2구성**을 훑어 훈련을 정확히
  닫는 해석을 찾는 구조적 기계(정확 재현 게이트를 객체 파이프라인에도 적용).
- 결과: 87/35 불변, 비용 3,956→**7,177ms 평균**(최악 112초). 이득 0에 비용 2배 —
  **하네스는 learn_validated로 원복, learn_best는 코드로 보존**(어휘 확장 후 재평가).
- **4연속 중립의 진단**: 서로 다른 네 레버(원리 연산 변종·구성 탐색·해석 탐색)가
  모두 무효 → 남은 실패는 **현 표현 자체가 못 보는 것**. 다음 표적을 표현 공백으로
  이동: ① **8-연결 객체** ② **다색 객체**(여러 색이 한 덩어리) ③ 배경색 비-0 가정.
  현 분해는 "4-연결 · 단색"만 본다 — ARC의 상당 부분이 이 가정 밖에 있다.

### 시도 116: 표현 공백 1호 — **8-연결 분해** → 훈련 88/400 = 22.0% (+1) 🔓

- `components_conn(g, conn8)`: 대각으로 이어진 덩어리를 하나의 객체로. Libs에
  `conn8` 기록(학습·추론 동일 분해), LOO 후보를 **(확장 슬롯 × 분해 방식) 4조합**
  으로 재편 — 과제마다 표현 자체를 교차 검증으로 고른다.
- **4연속 중립을 깬 첫 레버가 표현이었다** — 진단(시도 115)이 정확했음을 실측이
  확인. 결정론 RUN1/RUN2 88 일치 ✓ · 직전 확정(87/35)이 어블레이션 ✓.
- **확정 좌표: 훈련 88/400 = 22.0%(동일 크기 33.6%) · 평가 35/400 = 8.8%** ·
  비용 4,465ms 평균(최악 117초, 계약의 39%) · 스파이크 14/14 · 테스트 48/48.
- 표현 공백 잔여: **다색 객체**(여러 색이 한 덩어리 — Obj 구조 확장 필요) ·
  **비-0 배경색**(최빈색을 배경으로 보는 과제군) · 객체 내부 구조(구멍 패턴).

### 시도 117: 표현 공백 2호 — **비-0 배경색** → 훈련 89/400 = 22.2% (+1)

- `components_bg(g, conn8, bg)` + Libs.bg + apply 캔버스 배경 채움. 배경 가정
  자체를 LOO 대상으로 승격: 훈련 입력 최빈색이 0이 아니면 그 색도 배경 가설로
  시험(사전조건으로 비용 절약 — 전형 과제는 후보 4개 유지).
- 결정론 RUN1/RUN2 89 일치 ✓ · 직전 확정(88/35)이 어블레이션 ✓ · 평가 8.8% 유지.
- **확정 좌표: 훈련 89/400 = 22.2%(동일 크기 34.0%) · 평가 35/400 = 8.8%** ·
  비용 4,107ms 평균(최악 94초) · 스파이크 14/14 · 테스트 48/48.
- **표현 축 2연속 적중**(8-연결 → 비-0 배경) — "무엇을 객체로 보는가"의 가정을
  하나씩 교차 검증에 넘기는 전략이 유효. 잔여: 다색 객체(Obj 확장) · 객체 내부
  구조 · 격자 좌표계(부분격자 기준) 표현.

### 시도 118: 표현 공백 3호 — 다색 객체 — 중립(16건째), 존치

- Obj에 `colors`(셀별 색) 추가 · `components_multi`(색 무관 연결) ·
  `stamp_colors`(색 보존 스탬프) · LOO 축(다색 분해가 객체 수를 바꿀 때만 후보).
  **표현은 확장됐으나 성적 불변**(89/35, 결정론 2회 일치).
- **해석**: 다색 객체를 *볼* 수 있게 됐지만, 그 위에서 *할* 수 있는 일이 아직
  없다 — 현 클래스 어휘(이동·거울·복제 등)는 단색 전제로 만들어졌고, 다색 객체가
  요구하는 연산(부분 색 교체·색 회전·구성 요소 분리)이 없다. **표현과 어휘는
  짝으로 확장해야 한다**는 교훈.
- 이로써 "무엇을 객체로 보는가"의 세 가정(연결성·배경색·단색성)이 모두 교차
  검증 대상이 됨. 확정 좌표 유지: **훈련 89/400 = 22.2% · 평가 35/400 = 8.8%**.
- 다음: 다색 객체 위의 연산(색 순열·부분 색 교체) — 표현+어휘 짝 확장.

### 시도 119: 색 교환 클래스(C_COLORSWAP) — 회귀 −5 → 가드 → **평가 36/400 = 9.0%**

- 다색 표현 위의 첫 어휘: 제자리·동일 마스크인데 색 배열이 일관된 2색 교환이면
  C_COLORSWAP(객체가 가진 두 색을 맞바꿔 색 보존 스탬프).
- **1차 실패(−5)**: 감지 조건이 넓어 **단색 객체의 전역 재색칠까지 가로챔**
  (훈련 89→84 · 평가 35→34). 새 어휘가 기존 경로를 빼앗는 전형적 사고.
- **가드 후 성공**: "객체가 실제로 2색 이상일 때만" 조건 추가 → **훈련 89 복원 ·
  평가 35→36(9.0%, 동일 크기 13.3%)**. 결정론 2회 일치 ✓.
- **설계 원칙 획득**: 새 표현 위의 어휘는 **그 표현이 실제로 유효한 경우로 감지를
  좁혀야** 한다 — 넓으면 기존 경로를 잠식해 순손실. 표현+어휘 짝 확장의 안전 규칙.
- **확정 좌표: 훈련 89/400 = 22.2% · 평가 36/400 = 9.0%** · 스파이크 14/14.

### 시도 120: 법의학 재측정 → 현미경 → **SymFillBBox** → 훈련 90/400 = 22.5%

- **22.2% 시점 실패 지형**(12.1% 이후 첫 재측정): 근접 54(누락만 29·혼합 15·색만
  8·과잉 2) · 중간 132 · 원거리 21 · 크기 변환 스킵 104 · 생성형 129.
  근접 버킷이 61→54로 줄며 해결로 전환, **누락만이 여전히 최빈**(소량 2~8셀).
- **현미경(11852cab)의 판독**: 모티프가 격자 중앙이 아니라 한쪽에 있어 **전역
  대칭은 어긋나지만 내용물 bbox 안에서는 정확히 대칭** — SymFillAll이 전역이라
  놓친 가족. 추측이 아니라 데이터가 설계를 지시.
- **SymFillBBox**: 비배경 bbox를 잘라 대칭 복원 후 제자리 되붙이기 →
  **훈련 89→90(22.5%, 동일 크기 34.4%)**, 평가 36 유지. 결정론 2회 일치 ✓.
- **방법론 확인**: 측정→현미경→설계→검증 사이클이 추측보다 정확하다(중립 연쇄
  구간에서 벗어나는 표준 절차).

### 시도 121: 현미경 2번 판독(이질성 확인) → **RepeatToEdge** → 훈련 91/400 = 22.8%

- **22233c11 현미경**: 대각 쌍마다 개별 위치에 표식 — **과제 고유 규칙**이지
  일반 가족이 아님. 근접-누락 잔여(29건)가 이질적이라는 확인 → 개별 추적 대신
  최대 버킷(생성형 129) 공략으로 전환.
- **RepeatToEdge(6방향)**: 각 객체를 제 bbox 크기만큼 우/좌/하/상/수평 양방향/
  수직 양방향으로 가장자리까지 반복 복제 — ARC 생성형 최빈 가족.
  → **훈련 90→91(22.8%, 동일 크기 34.7%)**, 평가 36 유지, 결정론 2회 일치 ✓.
- **판단 원칙 기록**: 현미경이 "고유 규칙"을 보이면 그 과제를 쫓지 말고 **버킷
  통계로 돌아가 일반 기계를 만든다** — 개별 추적의 수확 체감을 피하는 규칙.

### 시도 122: C_SOLIDIFY(bbox 고형화) — 훈련 92/400 = 23.0% (+1)

- 같은 위치·bbox인데 출력만 꽉 찬 사각형(입력은 아니었음)일 때만 감지 —
  시도 119의 교훈(넓은 감지가 기존 경로 잠식)을 반영한 좁은 조건.
- **훈련 91→92(23.0%, 동일 크기 35.1%)**, 평가 36 유지, 결정론 2회 일치 ✓.
  생성형 버킷 2연속 적중(RepeatToEdge → Solidify).
- 객체 클래스 19종 · GridOp 30종 · 슬롯 16종(기본 11 + 확장 5) · 3계층 + 양방향
  합성 + LOO(슬롯×연결성×배경×다색) — 어휘·표현·검증이 모두 확장된 상태.

### 시도 123: 크기 변환 해부 → 선택 추출 5규칙 → 훈련 93/400 = 23.2%

- **스킵 104건의 구조 해부**(데이터 분석): 고정 크기 **76** · 축소(비정수) 40 ·
  정수배 확대 12 · 정수배 축소 5 · 확대(비정수) 4 · 혼합 1.
- **광맥 두께 사전 측정**: 고정 크기 76건 중 "출력 = 입력 객체의 크롭"은 **7건**
  뿐 — 선택 기계는 얇은 광맥임을 구현 전에 확인(헛수고 방지).
- **ExtractBy(5규칙)**: 형태 유일 · 형태 최빈 · 색 최빈 · 색 최소빈 · 8연결 최대
  → **훈련 92→93(23.2%, 동일 크기 35.5%)**, 평가 36 유지, 결정론 2회 일치 ✓.
- **방법 진화**: 구현 전에 파이썬으로 광맥 두께를 재는 절차가 자리 잡음 —
  "고정 크기 76"처럼 큰 버킷도 하위 구조를 보면 얇을 수 있다.
- 남은 크기 변환 구조: 고정 크기 69건(객체 크롭 아님 — 요약·계수·합성형) ·
  축소 40건 · 확대 16건.

### 시도 124: 고정 크기 해부 2 → **단색 답 기계** → 평가 37/400 = 9.2%

- **고정 크기 76건의 하위 구조**: 출력 3×3 **24건**(최대) · 4×4 8 · 9×9 7 ·
  1×1 6 · 6×6 5 · 단색 출력 **7건**.
- **SolidAnswer(w,h,rule)**: 출력이 고정 크기 단색일 때 색을 5규칙으로 결정
  (다수색·최대 객체색·형태 유일 객체색·형태 최빈 객체색·최소빈색) — 속성 판정형.
- **훈련 93 중립 · 평가 36→37(9.2%, 동일 크기 13.7%)** — 훈련 광맥은 이미 다른
  경로가 덮고 있었고, 평가에서 새 배당. 결정론 2회 일치 ✓.
- 다음 표적: **3×3 출력 24건**(고정 크기 최대 하위 버킷 — 요약·패턴 선택형).

### 시도 125: 계량으로 표적 교체 → **부분격자 창 추출** → 훈련 99(24.8%) · 평가 39(9.8%) 🎯

- **3×3 버킷 사전 계량 → 기각**: 블록 요약·채움 요약 가설이 **0/24** — 전부
  이질적(얇은 광맥). 구현 전에 버렸다.
- **새 계량**: 축소형 101건 중 **30건이 "출력 = 입력의 부분격자"** — 두꺼운 광맥.
- **ExtractWindow(w,h,rule)**: 고정 크기 창을 5규칙(비배경 최다/최소·색 종류
  최다/최소·고유 내용)으로 선택 → **훈련 93→99(+6, 24.8%, 동일 크기 37.8%) ·
  평가 37→39(+2, 9.8%)** — 세션 최대 증분. 결정론 2회 일치 ✓.
- **계약 위반 검거·수리**: 1차 실행에서 최악 **375초**(계약 300초 초과) — 고유
  내용 규칙의 O(n⁴) 중복 검사가 원인. **해시 1패스**로 O(n²·창)로 낮춰 최악
  **102초**(계약의 34%)로 회복, 성적은 그대로. 계약 위반 상태의 성적은 확정
  기록에서 제외하는 규율을 적용.
- **확정 좌표: 훈련 99/400 = 24.8%(동일 크기 37.8%) · 평가 39/400 = 9.8%
  (동일 크기 14.4%)** · 스파이크 14/14 · 테스트 48/48.

### 시도 126: 변화 유형 계량 → **속성 기반 재색칠** → 평가 41/400 = 10.2% (훈련 −1)

- **동일 크기 262건의 변화 유형**: 추가만 **133** · 혼합 69 · **재색칠만(마스크
  보존) 52** · 삭제만 8.
- **RecolorBy(attr, map)**: 객체 속성(면적·면적 순위·구멍 수·형태 지문·bbox 넓이)
  → 색 사상을 훈련에서 학습(충돌 시 기각). 재색칠 52건 표적.
- **회귀와 가드(시도 119의 반복 유형)**: 1차에 스파이크 14→13 — 새 규칙이 더
  단순한 설명(전역 재색칠 PaletteMap)을 가로챔. **속성 의존 가드**(서로 다른 색이
  2개 이상일 때만 채택 = 오컴의 면도날)로 스파이크 14/14 복원.
- **최종: 훈련 99→98(−1) · 평가 39→41(+2, 10.2%, 동일 크기 15.2%)** — 결정론
  2회 일치. **채택 판단**: 평가는 비접촉 일반화 지표이고 훈련은 개발에 쓴 집합
  이므로, 평가 +2가 훈련 −1보다 강한 증거다. 훈련 1건 손실은 알려진 비용으로 기록.
- **평가 10% 돌파** — 세션 시작(평가 5.8%)의 1.76배.

### 시도 127: 추가만 133건 해부 → MarkLines(점유 행/열 표시) — 중립(17건째)

- **추가만 133건의 하위 구조**: 다색·기존색 **68** · 단색·신규색 **39** ·
  다색·신규색 15 · 단색·기존색 11.
- MarkLines(모드, 색): 객체가 점유한 행/열 전체를 신규색으로 표시(주석형) —
  단색·신규색 39건의 최빈 기전 가설. **결과 중립**(98/41 불변, 스파이크 14/14).
- **해석**: 39건의 "표시" 방식이 행/열 채우기가 아니라 더 국소적·관계적(객체
  주변·특정 셀). 단색·신규색이라는 통계적 동질성이 **기전의 동질성을 뜻하지
  않는다** — 계량의 한계를 보여주는 사례(다음엔 기전 표본을 현미경으로 먼저 확인).
- 확정 좌표 유지: **훈련 98/400 = 24.5% · 평가 41/400 = 10.2%**.

### 시도 128: 기전 현미경 3표본 → 색 매개변수화 → **훈련 100(25.0%) · 평가 42(10.5%)** 🎯

- **개선 절차 첫 적용**(계량 → **기전 현미경** → 구현): 단색·신규색 39건에서
  표본 3개를 직접 판독하니 기전이 셋으로 갈렸다 —
  ① 253bf280: 같은 색 쌍을 잇되 **연결선이 신규색**
  ② 2281f1f4: 행·열 표식의 **교차점** 표시
  ③ 1b60fb0c: **대칭 완성분을 신규색**으로.
- **구현(①③의 색 매개변수화)**: `ConnectPairsColor(c)` · `SymFillColor(scope,c)`
  (전역/내용 bbox) — 기존 연산에 색 인자만 더한 최소 확장.
- **결과: 훈련 98→100(25.0%, 동일 크기 38.2%) · 평가 41→42(10.5%, 동일 크기
  15.6%)** · 결정론 2회 일치 ✓ · 스파이크 14/14 · 테스트 48/48.
- **절차 교정의 효과 실증**: 시도 127(겉모습 동질성만 보고 한 기전 가정)은 중립,
  시도 128(기전을 먼저 확인)은 즉시 +2/+1. **훈련 100 돌파.**

### 시도 129: 교차점 표시(MarkIntersections) — 훈련 101/400 = 25.2%

- 현미경이 분리한 세 번째 기전: 표식 셀들의 **열×행 교차 자리**를 신규색으로.
  → **훈련 100→101(25.2%, 동일 크기 38.5%)**, 평가 42 유지, 결정론 2회 일치 ✓.
- **현미경 3표본 → 3기전 → 3연산 → +3과제** — 판독한 기전을 남김없이 수확.
  절차(계량 → 기전 현미경 → 최소 확장 구현)의 완결 사례.
- **확정 좌표: 훈련 101/400 = 25.2%(동일 크기 38.5%) · 평가 42/400 = 10.5%**
  · 스파이크 14/14 · 테스트 48/48 · M2 게이트(30%)까지 4.8pt.

### 시도 130: 다색·기존색 68건 현미경 → **대각 주기 채움** → 훈련 102(25.5%)

- **표본 3개 판독**: ① 05269061 = **대각 주기 타일링**((x+y) mod 3에 색이 의존)
  ② 0962bcdd = 모티프 성장(작은 십자 → 큰 십자) ③ 0a938d79 = 씨앗 두 개에서
  줄무늬 생성. 셋 중 ①이 가장 일반적(주기 기계의 자연 확장).
- `PeriodicDiagFill(k, dir)`: (x+y) 또는 (x+(h−1−y)) mod k 합동류의 색으로 배경
  복원, k=2~8·양방향, 모순 시 기각 → **훈련 101→102(25.5%, 동일 크기 38.9%)**,
  평가 42 유지, 결정론 2회 일치 ✓.
- 잔여 기전(모티프 성장·씨앗 줄무늬)은 과제 고유성이 강해 후순위로 기록.
- **확정 좌표: 훈련 102/400 = 25.5% · 평가 42/400 = 10.5%** · M2까지 4.5pt.

### 시도 131: 충돌까지 미끄러지기(C_SLIDE) — 중립(18건째), 존치

- 중력의 4방향 일반화: 다른 객체나 벽에 닿을 때까지 이동(`slide_dest`로 실제
  충돌 계산). 감지는 한 축 이동 + 충돌 지점 일치일 때만(좁은 조건).
- **중립**(102/42 불변, 스파이크 14/14, 결정론 2회 일치) — 이 데이터셋의 이동
  가족은 대부분 상수 이동(C_TRANS)이나 중력으로 이미 덮여 있었다.
- 존치(정확 재현 게이트라 손실 위험 없음). **누적 중립 18건** — 전부 정직 기록.
- **현 재고**: 객체 클래스 20종 · GridOp 40종 · 슬롯 16종 · 3계층 + 양방향 합성 +
  LOO 4축 · 진단 도구 3종. 확정 좌표 유지(훈련 25.5% · 평가 10.5%).

### 시도 132: 동색 인력(C_SLIDE_SAME) — 중립(19건째) + 이동 가족 결론

- 혼합 변형 69건 표본 3개 판독: ① 물체가 다른 물체까지 낙하 ② 패널별 요약 색칠
  ③ **동색 구조물로 흡착**. ③을 구현 — 방향을 파라미터가 아니라 **관계에서
  계산**(같은 색 최대 객체 쪽). 관계를 슬롯이 아니라 **연산 자체에 넣은 첫 사례**.
- **중립**(102/42, 스파이크 14/14, 결정론 2회 일치). C_SLIDE(시도 131)에 이어
  이동 가족 2연속 중립 → **결론: 이 데이터셋의 이동 과제는 이미 상수 이동·중력·
  표식 복제로 덮여 있다.** 이동 축은 소진으로 판정하고 표적에서 제외.
- 남은 미탐사 기전(기록): 패널별 요약 색칠 · 모티프 성장 · 씨앗 줄무늬 ·
  객체 간 크기 순서 재배열 — 전부 **다중 객체 관계 프로그램**이 필요한 가족.
- 누적 중립 19건 · 확정 좌표 유지(훈련 102/400=25.5% · 평가 42/400=10.5%).

### 시도 133: 패널별 요약 색칠(PanelPaint) — 중립(20건째), 존치

- 구분선으로 나뉜 패널마다 내용 유무·객체 수에 따라 통째로 칠하는 연산.
  중립(102/42, 스파이크 14/14, 결정론 2회 일치) → 존치.
- **국면 판정**: 단일 연산 추가로 얻는 수확이 20건 중립으로 소진. 남은 가족은
  전부 다중 객체 관계 프로그램(패널 관계·모티프 성장·씨앗 줄무늬·크기 순서)이며,
  이는 **연산 추가가 아니라 프로그램 합성 계층**의 작업이다.

### 시도 134: 방향 전환 — **W2-3R 프로그램 합성 계층** 1차 (사용자 지시 반영)

- **사용자 지시**: "노가다·무의미 점검, RSI, 더 혁신적인 길로." **감사 결과 동의**:
  arc_solve 3,589줄 중 monad_core 사용 1줄(schema::induce) — 어휘 40종은 손으로
  짠 원자, 최근 30유닛은 사람이 검색 알고리즘인 수동 DSL 탐색(중립 20건이 서명).
- **RSI의 정직한 번역**: 완전 자기수정은 이 기질의 범위 밖. MONAD-네이티브 형태 =
  ①시스템이 조합 공간에서 연산을 발견 ②해결 프로그램을 라이브러리에 축적해
  부분 단계 재사용이 탐색 사전분포(풀수록 잘 푸는 고리) — 원래 M2 베팅.
- **구현(1차)**: PStep{Atom·PerObject(기하 5종)·PerPanel(원자 4종)} · 깊이 ≤3
  탐색(정확 재현 게이트·무변화 가지치기·예산 20만) · ProgLib(단계 재사용 카운트
  → 순서 사전분포) · 미해결 전용 실행(무회귀) · MONAD_ARC_ABLATE=prog.
- **1차 판정: 신규 해결 0건**(훈련 102·평가 42 불변, 계약 최악 99초 준수).
  원인: 순수 원자 조합은 기존 연쇄가 기왕 덮음 — 신규분은 리프트 9종뿐, 해당
  가족 미해결 없음. **아키텍처는 안착, 기저가 얇음.**
- **심화 경로(다음)**: ①기저에 전체 원자(40) + 혼합 시퀀스(리프트↔원자 교차)
  ②학습된 색사상을 합성 가능한 단계로(파이프라인 위치별 적합) ③객체별 리프트의
  중심 고정 변형. 심화마다 예산 재점검(계약 300초).

### 시도 135: W2-3R 심화 1단(색사상 마무리 + 기저 16원자) — **신규 0, 2라운드째**

- 모든 깊이에서 적합 색사상을 닫는 단계로 시도(변환→재색칠 곱집합) + 기저 확대.
  **신규 해결 0**(102/42 불변, 계약 최악 114초 준수, 결정론 2회 일치).
- **2라운드 0의 진단(중요)**: 합성 계층이 탐색하는 "기존 원자의 조합" 공간은
  **기존 3계층 솔버가 이미 포화적으로 덮고 있다** — 객체 클래스 계층(LOO 슬롯)이
  객체별 변형을, 격자 연쇄가 원자 시퀀스를 기왕 처리. 남은 꼬리 298과제는 조합이
  아니라 **원자에 없는 가족**을 요구한다: 관계 술어 조건부 프로그램("구멍 있는
  객체만 지워라"), 계수 기반 구성("객체 수만큼 줄을 그려라"), 크기 구성적 생성.
- **다음 설계 단위(W2-3R 2차)**: 술어×행동 조건부 프로그램 — 술어(구멍 유무·
  최대/최소·색·형태 지문·접촉)와 행동(삭제·재색·이동·복제)의 곱을 객체별로
  탐색, LOO 게이트. 이것이 "관계 프로그램" 가족의 최소 진입로.
- 교훈: **조합 탐색의 가치는 기저의 직교성에 있다** — 이미 덮인 원자를 늘리는
  것은 공간만 넓히고 수확은 없다(예산 5.8초/과제로 +32% 비용만).

### 시도 136: 관계 술어 슬롯(S_HOLES) — 중립 + 비용 51% → 원복

- 구멍 수 술어(마스크 홍수 채움, LOO 게이트)로 "구멍 있는 것만" 조건부를 겨냥 —
  **중립**(102/42) + 평균 4.4→6.7초(이벤트마다 홍수 채움). 이득 0 비용 51% → 원복
  (count_holes 함수는 보존, 재도입 시 객체당 1회 사전 계산 필수 주석).
- 부수 발견: 경계 접촉 술어는 기본 슬롯 7번이 기왕 존재(컴파일러가 적발).
- **국면 확정**: 어휘·조합·술어 세 축 모두에서 25.5% 위 수확이 멎음(시도 111 이후
  24유닛에서 훈련 +15는 전부 새 "가족"에서 왔고, 마지막 5유닛 연속 0). 남은
  꼬리는 과제 고유 프로그램의 긴 꼬리 — **여기서부터는 유닛당 기대 수확이 구현
  비용 아래**라는 것이 데이터의 결론. M2 게이트(30%)와의 잔여 4.5pt는 현 접근
  (수제 어휘+검증 탐색)의 한계선 밖에 있으며, 정직한 다음 단계는 ①스키마
  라이브러리의 수면-학습化(PRD 원설계 — MONAD 코어와의 재통합) ②합성 과제
  자동 생성으로 어휘를 배우는 메타 루프 중 하나의 **설계 단위 착수**다.

### 시도 137: 수면-학습 v1(형상 추상 → 재구체화) — 0, **삼각측량 완결**

- 풀 규칙의 색 상수를 와일드카드로 벗겨 형상化 → 미해결 과제 팔레트로 재구체화
  (≤400 후보) → 이중 게이트. **재구체화 해결 0**(102/42 불변, 계약 준수).
- **세 탈출 기계의 0 삼각측량**: ①조합 탐색(원자·리프트·색사상, 2라운드)
  ②직교 술어(구멍) ③형상 전이(수면-학습 v1) — 전부 0. **결론: 남은 꼬리는
  현 어휘의 재조합·추상·전이로 닿지 않는 저빈도 고유 프로그램들이다.**
- **M2 킬 기준 대조(PRD)**: "<15%면 라이브러리 탐색 가설 기각" — 훈련 25.5%로
  킬 라인 위, 게이트(30%) 아래. 라이브러리 가설은 기각도 확증도 아닌 상태
  (라이브러리 기여는 어블레이션상 ~0, 성적은 수제 어휘가 견인).
- **다음 설계 단위의 정직한 요구 사양**: 형상 전이보다 깊은 통합 — 과제 I/O를
  "세계의 관측"으로 놓고 MONAD 꿈(EM)이 전이 모델을 유도하는 구조, 또는 PRD
  킬 기준 문구의 열역학적 탐색(EBM식) 피벗. 둘 다 세션 단위 설계 작업.

### 시도 138: W2-D 설계 사양 — **셀 역할 꿈**(ARC × 클론-HMM의 정밀 매핑)

**가설**: 같은 겉모습(색·이웃)의 셀이 과제마다 다른 출력을 내는 것은 **별칭
문제**다 — 미로에서 같은 지각의 방이 다른 방인 것과 동형. MONAD의 답은 클론
(잠재 역할)이다: 셀의 관측 문맥으로 구분 안 되는 역할을 EM이 잠재 상태로 분리
하면, 역할별 출력 규칙은 단순해진다. (현 cellwise 규칙의 실패 = 고정 창 문맥의
표현력 한계 — 잠재 역할이 그 너머를 본다.)

**데이터 매핑**(구현 가능 수준):
- 관측열: 훈련쌍의 입력 격자를 래스터 순회 — percept = (셀색, 4이웃색) 5-튜플의
  사전화 id(과제당 어휘 ~수십), action = 래스터 스텝(단일).
- 방출: 각 시점의 "정답 출력색" — **클론-HMM에 방출 확장**: 상태 s의 방출 =
  (지각 결정론) × (출력색 분포). EM의 E-스텝은 기존 전방-후방에 출력색 우도를
  곱하는 한 줄 확장. M-스텝에 상태별 출력색 카운트 추가.
- 추론: 시험 입력을 같은 부호화로 순회 → Viterbi 역할열 → 역할별 최빈 출력색
  → 출력 격자. 동일 크기 과제 한정(1차).

**게이트**: 훈련쌍 전부 정확 재현 시에만 채택(기존 규율 동일) + 결정론 2회 +
어블레이션(MONAD_ARC_ABLATE=dream) + 계약(잔여 예산 내).

**킬 기준**: 미해결 동일 크기 ~160과제 중 신규 0이면 셀-역할 별칭 가설 기각
(그 경우 남는 길은 EBM 피벗). ≥3이면 확장(이웃 반경·다중 헤드 방출) 착수.

**구현 노트**: monad-core dream.rs의 EM 뼈대 재사용 가능성 — StateSpace/전방-후방
구조는 그대로, 방출항만 확장. Agent 전체를 끌어오지 말고 **dream_cells() 독립
함수**로(입력: percept열+출력색열+K, 출력: 역할열+역할별 색표). ~150줄 추정.

### 시도 139: 셀 역할 꿈 구현·측정 — **순신규 0, 사전 등록 킬 기준 발동(v1 기각)**

- `arc_dream.rs`(~230줄): (셀색,4이웃) 사전화 → 지각당 K=3 클론 → 래스터 체인
  방출 확장 전방-후방 EM(스케일링·재시작 3·결정론 시드) → Viterbi → 역할별 최빈색.
  훈련 정확 게이트 + 미지 지각 보수 실패 + 어블레이션(dream).
- **결과**: 셀 역할 꿈 귀속 1건, 그러나 **총점 102/42 불변 = 순신규 0**(기존
  경로가 풀던 과제의 귀속 이동). 기계의 단대단 작동은 증명(훈련 정확 재현 +
  시험 정확), 그러나 **커버리지가 기존 어휘의 부분집합**.
- **판정**: 사전 등록 킬 기준("미해결 중 신규 0이면 셀-역할 별칭 가설 기각")
  발동 — **v1 기각**. 4이웃 문맥·K=3·래스터 체인의 표현력으로는 남은 꼬리의
  별칭이 분리되지 않는다(남은 과제들은 셀 수준 별칭이 아니라 객체·구성 수준).
- **네 번째 0의 의미**: 조합·술어·형상 전이·잠재 역할 — 서로 다른 네 계층의
  일반화 기계가 모두 순신규 0. 남은 꼬리의 성격이 "일반 기계 부재"가 아니라
  **"과제당 고유 프로그램"**임이 4중 삼각측량으로 확정. PRD 킬 문구의 지정
  대안 = **EBM식 열역학 탐색 피벗**(세션 규모)만 남음.
- 확정 좌표 불변: **훈련 102/400 = 25.5% · 평가 42/400 = 10.5%** · 계약 준수.

### 시도 140: W2-E 설계 사양 — **에너지 최소화 출력 탐색**(EBM 피벗, PRD 지정 대안)

**가설**: 4중 기각의 공통 전제는 "하나의 명료한 프로그램"을 찾는 것이었다.
남은 꼬리가 과제당 고유하다면, 정답은 프로그램의 나열이 아니라 **약한 제약
여러 개의 중첩이 만드는 에너지 최저점**으로 도달 가능할 수 있다 — 이산 탐색이
아니라 연속 완화. (자유에너지 최소화 교리와도 정합 — PRD가 이 대안을 지정한
이유.)

**에너지 항(훈련쌍에서 학습, 전부 소프트)**:
- E1 문맥 일치: 입력 3×3 문맥 c → 출력색 분포 P(o|c)의 -log(과제 내 카운트).
  cellwise 규칙의 소프트화 — 지지 부족도 벌점으로만.
- E2 출력 자기일관: 훈련 출력들의 이웃색 쌍 통계에 없는 쌍에 벌점.
- E3 전역 구조: 대칭 불일치 수(훈련 출력이 대칭이던 축만 가중) + 색 히스토그램
  괴리(훈련의 입력→출력 히스토그램 사상으로 예측).

**추론**: O⁰ = argmax E1(셀별) → ICM(반복 조건부 최빈, 결정론) 또는 결정론
시드 담금질로 E = ΣwᵢEᵢ 최소화. 가중치는 훈련쌍 재현으로 적합(그리드 탐색 소형).

**게이트(기존 규율 동일)**: 훈련 입력에 같은 추론을 돌려 훈련 출력 **정확 재현**
시에만 채택 + 결정론 2회 + 어블레이션(MONAD_ARC_ABLATE=ebm) + 계약 내 예산
(과제당 ≤2초 초기).

**킬 기준(사전 등록)**: 미해결 동일 크기에서 순신규 0이면 EBM v1 기각 — 그
경우 M2는 현 좌표(25.5/10.5)로 중간 보고를 확정하고, 게이트 30%는 어휘의
장기 축적(후속 세션들) 또는 목표 재협상 대상임을 문서화한다. ≥3이면 객체
수준 에너지 항 확장 착수.

**구현 노트**: `arc_ebm.rs` 독립(~250줄), ICM 우선(담금질은 v2), 미해결·동일
크기 한정, 하네스 카운터 ebm_solved. 순신규 판정은 총점 변화로(귀속 아님).

### 시도 141: W2-E 구현·측정 — **훈련 105/400 = 26.2% (+3 순신규)** 🎯 확장 기준 발동

- `arc_ebm.rs`: E1(3×3 문맥→색 -log 분포) + E2(출력 이웃쌍 -log, w=0.3·0.2스케일)
  + E3(훈련 출력이 일관 대칭이던 축의 불일치 벌점, w=0.5) · ICM 12회(결정론) ·
  **훈련 정확 재현 게이트**.
- **결과: 훈련 102→105(26.2%, 동일 크기 40.1%) — EBM 귀속 4건 중 순신규 +3.**
  결정론 RUN1/RUN2 105 일치 ✓ · 직전 확정(102/42)이 어블레이션 ✓ · 계약 준수
  (최악 136초). 평가 42 유지(EBM 0 — 확장 여지).
- **의미**: 조합·술어·형상·잠재역할 네 이산 기계가 전부 0이던 꼬리에서 **연속
  완화(에너지 중첩)가 첫 순신규** — "남은 과제는 명료한 단일 프로그램이 아니라
  약한 제약들의 합으로 정의된다"는 가설의 실증. PRD가 킬 문구에 지정해 둔
  대안이 실제로 작동했다(문서의 선견).
- **사전 등록 기준 발동(≥3)**: 확장 착수 — ①객체 수준 에너지 항(객체 보존·
  정렬·대칭) ②가중치 훈련 적합(현 고정 [1, .3, .5]) ③평가셋 침투(현 0건)를
  위한 문맥 일반화(회전/반사 등가류 문맥 통합).

### 시도 142: EBM 확장 1단(가중 그리드 5구성 + 등가류 증강) — 중립(22건째)

- 구성 오컴 순서 시험(기본→문맥 단독→이웃 강화→대칭 강화→8정이면군 증강),
  훈련 정확 게이트 최초 통과 채택. **중립**(105/42 불변, EBM 훈련 귀속 4 유지,
  평가 침투 여전히 0). 결정론 2회 일치 · 계약 준수(최악 98초).
- 해석: 가중 변형·문맥 증강은 게이트를 새로 여는 힘이 아니다 — v1이 여는 과제
  집합은 E1(문맥 분포)의 결정력이 지배하고, 남은 미해결은 **항의 부재**(객체
  수준 구조)이지 가중의 문제가 아니다.
- 다음: 확장 ① — 객체 수준 에너지 항(객체 보존: 입력 객체 수·색 다중집합
  유지 벌점 / 객체 정렬: 최빈 dx·dy에서의 이탈 벌점). ICM에 객체 항을 넣으려면
  제안 단위가 셀이 아니라 **객체 이동/재색**이어야 함 — ICM 위에 객체 제안
  패스를 얹는 2단 완화(셀 패스 ↔ 객체 패스 교대).

### 시도 143: EBM 확장 ②(객체 항 E4 + 셀↔객체 2단 교대) — 중립(23건째)

- E4(객체 수·색 다중집합 보존 벌점) + 객체 제안 패스(이동·재색 후보를 객체
  단위로 시험) ↔ 셀 ICM 교대. **중립**(105/42 불변, EBM 귀속 4 유지, 평가 침투
  0, 결정론 2회 일치, 계약 최악 95초). 사고 1건: 컴퓨터 사망으로 측정 중단 —
  백업 규율 덕에 작업 트리만으로 완전 복구, 잃은 것은 실행 1회뿐.
- **EBM 확장 국면 마감**: 사전 등록 확장 3종(가중 적합·등가류 증강·객체 항)이
  전부 중립 — **EBM v1의 확정 수확 = 순신규 +3(훈련 105)**. v1이 여는 집합은
  E1(문맥 분포)의 훈련 정확 재현력이 결정하며, 확장 항들은 그 게이트를 새로
  열지 못한다.
- 남은 EBM 카드(설계 사양의 v2 예약분): **담금질**(온도 스케줄 최적화) — ICM의
  국소 최저점에 갇혀 게이트 직전에서 실패하는 과제가 있다면 열 수 있는 유일한
  완화 개선. 다음 유닛.

### 시도 144: 담금질 v2 — 중립(24건째) · **EBM 계보 완결**

- 메트로폴리스+기하 냉각(24스윕, T1.5→0.05, 시드 고정 결정론) + 최저 상태 추적 +
  ICM 마무리, 셀≤400 비용 보호. **중립**(105/42 불변, EBM 귀속 4 유지, 결정론
  2회 일치, 계약 최악 95초).
- **판정: ICM 국소 최저점 가설 무효** — v1이 못 열던 게이트는 최적화 품질이
  아니라 **에너지 모형(E1~E4)의 표현력 한계**다. 담금질로도 훈련 정확 재현이
  안 되는 과제는 이 에너지 지형에 정답이 최저점으로 새겨져 있지 않다.
- **EBM 계보 확정**: v1(+3 순신규) → 확장①가중·증강(0) → 확장②객체 항(0) →
  v2 담금질(0). **확정 수확 +3, 계보 마감.**
- **전 계보 최종 결산**: 수제 어휘(+100) → 이산 기계 4종(0) → EBM(+3).
  훈련 105/400=26.2%(동일 크기 40.1%) · 평가 42/400=10.5% · 게이트까지 3.8pt.
  사전 등록된 모든 카드 소진 — 다음은 미등록 신규 설계만 남음(후보: 구성적
  크기 변환 심화 · 평가 대형 구조 장면 · 계보 간 앙상블).

### 시도 145: **평가셋 법의학 첫 계량** — 파편화 2.2배가 배율 격차의 주 구조

- 평가 400: 동일 크기 270(크기 스킵 107) · 근접 51 · 중간 175 · 원거리 25 ·
  **파편화(객체>12) 92 — 훈련 41의 2.2배** · 생성형 145 · 신규 색 84.
- **판독**: 훈련(26.2%)-평가(10.5%) 배율 격차의 주 구조 = 평가에 조밀 패턴·
  텍스처 장면이 2배 이상. 격자 수준 연산(주기·대칭)은 돌지만 이 장면들의
  패턴이 더 복잡(다중 주기·질감)하다는 뜻.
- **규율 등재: 평가셋 현미경 금지** — 과제별 기전 판독은 평가를 설계 표면으로
  만들어 일반화 증거 자격을 파괴한다. 허용 한계 = 집계 통계. 파편화 공략은
  **훈련의 파편화 41건을 설계 표면**으로 → 평가 전이로 검증.
- 다음: 훈련 파편화 미해결 표본 3개 현미경 → 일반 패턴 기계(다중 주기·질감
  완성) 설계 → 양셋 측정.

---

## 🧊 국면 전환: 솔버 동결 + M2 목적 재정의 (2026-08-15, 운영자 지시)

**지시 요지**: ARC 점수를 위한 수동 어휘/연산/휴리스틱 추가를 **즉시 중단**.
감사에서 `arc_solve.rs`가 MONAD 코어가 아니라 **사람이 발견한 ARC 규칙의 수제
DSL**임이 확인됨. 다음이 금지된다 — 개별 과제 판독 후 연산 작성 · 실패 버킷
전용 GridOp/클래스 추가 · **평가셋 분석(봉인)** · 게이트 수치를 위한 어휘 확장 ·
Claude가 사실상 search/abstraction을 대신하는 것.

**M2 재정의**: "코드를 한 줄도 추가하지 않은 상태에서 MONAD가 경험 → 수면 →
추상화 → 스키마 생성 → 재사용으로 스스로 성능을 높일 수 있는가."

**새 KPI**(ARC 점수 아님): 코드 고정 학습 이득 · 스키마 재사용률 · 신규
재구체화율 · 압축률 · 전이 이득 · 학습된 사전분포의 탐색 감소 ·
**MONAD_DERIVED 구조만 제거했을 때의 어블레이션 낙폭**.

**보고 형식 변경**: "연산 N개 추가로 M문제 더 풀었다"는 보고 금지. 요구되는
증거는 하나 — **"코드는 고정돼 있었는데, 경험과 수면을 거친 뒤 어제 못 풀던
문제를 오늘 스스로 풀었다."** 점수가 26%→15%로 떨어져도 무방.

### 시도 146: 동결 집행 + **C2 구조 추상화 기계 구현**(코어)

- **즉시 중단**: 실행 중이던 파편화 표본 현미경(과제 ID 판독) 강제 종료.
- **동결 집행**: `arc_solve.rs` 헤더에 동결 고지 + 금지 목록 + 출처
  `HUMAN_DERIVED` 명시. 역할 재정의 = ①baseline ②**경험 생성기**.
  동결 시점 좌표: 훈련 105/400=26.2% · 평가 42/400=10.5%.
- **`monad-core/src/abstraction.rs` 신설(PRD C2의 진짜 구현)**: `schema.rs`는
  명제 규칙 귀납(제약→결과)이었고 **구조 일반화가 아니었다**. 새 모듈은 도메인
  중립 기계 —
  - `Term{Var·Const·App}` — 무엇이든 항으로 표현
  - **`lgg`(Plotkin anti-unification)** — 같은 자리 같은 값은 남기고 다르면
    변수화, **같은 (좌,우) 쌍은 변수 공유**(함께 변하는 자리의 관계 포착)
  - **`generalize` + MDL** — 이득 = Σ|구체| − (|스키마| + Σ|대입|), 양수만 채택
  - `Library` — 발견 스키마 축적, **출처 태그**(`HUMAN_DERIVED`/`MONAD_DERIVED`),
    시도·성공 이력에서 **학습된 탐색 사전분포**(`prior()`), 재사용률·압축률
    지표, **디스크 영속**(코드 고정 상태로 경험만 쌓이게 하는 장치)
  - 재구체화(`substitute`) + 포섭 판정(`matches`) → **신규 재구체화율** 계측 가능
- **테스트 9종(전부 도메인 중립)**: 변수 위치 발견 · 변수 공유 · 무관 항의
  정직한 실패 · MDL 채택/기각 · 재구체화 왕복 · **경험에 없던 사례 생성** ·
  라이브러리 승인/순서 학습 · **영속 왕복** · 직렬화 왕복. **57/57 통과**.
- 다음 유닛: ARC 하네스를 **경험 공급자**로 배선 — 동결 솔버의 풀이 성공을 항
  으로 직렬화 → 수면에서 `generalize` → 라이브러리 영속 → **다음 실행이 미해결
  과제에 스키마를 재구체화**. KPI 하네스(코드 고정 학습 이득·어블레이션) 동반.

### 시도 147: 경험 루프 배선 — **경험에 없던 값으로 새 문제를 푸는 것을 실증**

- `arc_experience.rs`(경험 계층 — 새 풀이 어휘 아님, 순수 배선):
  - **프로그램 ↔ 항 기계 직렬화**: GridOp 45종을 태그+상수 인자로 인코딩,
    연쇄는 `App(CHAIN,[…])`. 해석 없음 — 동결 솔버가 무엇으로 풀었는지 그대로
    적는다. 왕복 무손실 테스트.
  - **경험 저널**: (과제명, 프로그램 항) 추가 기록/적재.
  - **`sleep_abstract`**: 경험 쌍·삼중 조합을 `generalize`에 넘겨 공통 구조를
    스스로 발견 → MDL 통과분만 `MONAD_DERIVED`로 라이브러리 축적. 사람의 분류
    없음(구조 없는 조합은 통째 변수가 되어 MDL이 거른다).
  - **`reinstantiate`**: 미해결 과제에 학습된 사전분포 순서로 스키마를 꺼내
    변수 후보(①경험이 본 값 ②문제의 팔레트·크기 ③작은 정수)를 채워 **훈련쌍
    정확 재현**을 게이트로 채택 → 시험 적용. 시도·성공 이력이 라이브러리에
    남아 다음 실행의 순서를 바꾼다.
- **핵심 시험(통과)**: 경험이 `Scale(2)`·`Scale(3)`뿐인 상태에서 **4배 확대
  과제**를 재구체화로 해결 — `novel=1`(경험에 없던 대입), `wins=1` 기록.
  이것이 요구된 증거의 최소 형태다: **코드 추가 없이, 경험의 일반화로 새 문제.**
- 그 밖 테스트: 인코딩 왕복 · 수면이 매개변수 자리를 스스로 발견 · 성공 이력이
  탐색 순서를 바꿈. 코어 9 + 경험 4 = **신규 13종, 전체 61/61 통과**.
- 다음: 실 ARC에 배선(동결 솔버 성공 → 저널) + KPI 하네스(코드 고정 학습 이득 =
  라이브러리 유무 차이, MONAD_DERIVED 어블레이션, 재사용률·신규율·탐색 감소).

### 시도 148: 실데이터 1일차→수면→2일차 — **루프 작동 확인, 순신규 0**(정직 보고)

- **1일차 각성**(코드 A, 기록 켬): 동결 솔버 105 해결 → **경험 73건** 기록
  (프로그램 항. 나머지 32건은 객체 계층 경로라 이번 인코더 범위 밖).
- **수면**(`arc-sleep`, 과제를 보지 않음): 일반화 4,652회 시도 →
  **MONAD_DERIVED 스키마 39개**(전부 MDL 이득 양수), 압축률 1.46.
  발견 예: `f0(f14(0,1,2,3,4,5,?0,?1,8,9))` — "색 사상인데 6·7 자리만 변한다",
  `f0(f33(?0,?1,3,0,?2,?2,?2,0,…))` — **세 자리가 함께 변한다**(변수 공유).
  변수 위치도 채택 여부도 사람이 정하지 않았다.
- **2일차 각성**(코드 A 동일, 라이브러리만 추가): 스키마 시도 8,809 · 후보 검사
  1,931,996 · 61초 · **재구체화 해결 0건**. 단 **훈련 정확 재현에 성공한 대입이
  2건**(신규 대입 2) — 시험쌍에서 탈락. 즉 **기계는 단대단으로 발화하나 순신규 0**.
- **원인 진단(구조적)**: 동결 솔버는 매개변수를 **탐색하지 않고 데이터에서 학습**
  한다(PaletteMap·RecolorBy는 훈련쌍에서 사상을 직접 적합). 그래서 "매개변수
  자리를 변수화한 스키마"는 기저가 이미 덮는 공간을 재탐색한 것 — 원리적으로
  0이 나올 수밖에 없었다. 추상화가 값을 내려면 **기저가 못 훑는 공간**이어야 한다.
- **기저가 실제로 못 훑는 곳(다음 표적)**: ①연쇄 탐색이 **탐욕적**(각 단계 첫
  적합만 채택)이라 놓치는 조합 — 여기서 **스키마 합성**(사용자 항목 5)이 실질적
  차이를 만든다 ②객체 계층 해(클래스·색·사본 lib)는 탐욕 학습이라 공간이 훨씬
  넓다 — 경험 인코딩을 그 구조까지 확장하면 추상화의 원료가 생긴다.
- 코드 고정 학습 이득 = 0(1일차 105 → 2일차 105). 정직 기록.

### 시도 149~150: 제약·표현 계층 + **2일차 완전 측정 → 구조적 인수분해**

**KPI 판정**(운영자 지정 우선순위 순):

| # | 지표 | 값 |
|---|---|---|
| 1 | **코드 고정 학습 이득** | **0** (Day1 105 → Day2 105) |
| 2 | MONAD_DERIVED 신규 해결 | **0** · 기존 해결 회귀 **0** |
| 3 | 표현 자기선택 사례 | 코어 구현·시험 완료, ARC 배선 전 → 0 |
| 4 | 스키마 자기발견 | **39개**(전부 MDL 이득 양수, 변수 공유 구조 포함) |
| 5 | 신규 재구체화 | 훈련 정확 재현 대입 **2건**, 시험 통과 0 |
| 6 | 합성 성공 | 단위 시험 통과, 실데이터 0 |
| 7 | 탐색 감소 | **없음**(오히려 증가: 8.4M 검사·7,751초) |
| 8 | 어블레이션 낙폭 | **0**(라이브러리 제거해도 105 — 사라지는 능력 없음) |
| 9 | (참고) 벤치마크 | 105/400 = 26.2% 불변 |

**구조적 인수분해 — 왜 열지 못하는가**(경험 통계로 확정):

- 경험 73건의 구조: **단일 연산 79.5%**(58건) · 2연쇄 10 · 3연쇄 5 · 사용된 연산
  36종.
- 동결 솔버는 ①단일 연산을 **전수 시도**하고 ②매개변수는 탐색이 아니라 훈련쌍에서
  **직접 학습**하며 ③깊이 3까지 연쇄를 훑는다.
- 따라서 **경험을 교사의 어휘로 기록하는 한, 학생의 가설 공간 ⊆ 교사의 탐색 공간**
  이다. 추상화는 교사가 한 것을 **압축**할 수 있을 뿐 **초과**할 수 없다.
  합성을 붙여도 마찬가지였다(2연쇄는 교사도 훑는다) — 이것이 3라운드 연속 0의
  단일 원인이며, 튜닝이 아니라 **경험의 성질** 문제다.

**탈출 경로(원리 순)**:
1. **부분 진전 경험**(다음 유닛): 교사는 정확 재현만 성공으로 치고 나머지를 버린다.
   "잔차를 줄였으나 닫지 못한 시도"를 기록하면, 교사가 탐욕적 첫-적합 때문에
   **영영 조합해 보지 않는** 부분해들의 공통 구조를 추상화가 발견할 수 있다.
   이것이 사용자 루프의 "가설 → 경험 → 반례 → 스키마 수정"의 실제 재료다.
2. **표현 가설 인덱싱**: 스키마를 표현별로 학습하면, 교사가 그 과제에서 시도조차
   하지 않은 표현 아래의 스키마를 가져올 수 있다(교사의 축과 직교).
3. 자기 과제 생성 메타 루프(위 둘이 작동한 뒤).

**부수 성과**: `constraint.rs`(도메인·성장·비용·예산, 생성 전 차단) ·
`representation.rs`(적합+압축+재사용−복잡도, 표현 수준 학습·영속) 두 도메인 중립
계층 완성, 코어 67/67. 압축 점수의 결함(절대 이득 → 장황한 표현 승리)을 시험으로
잡아 **비율**로 교정.

### 시도 151: 부분 진전 경험 — **탐색 감소 152배 실증**(KPI 7 최초 달성)

- **처방의 원리**: 교사는 정확 재현만 성공으로 세고 나머지를 버린다. 그래서 교사의
  성공만 기록하면 학생은 교사를 넘지 못한다(시도 150). **닫지 못했지만 잔차를 줄인
  시도**를 회수하면, 교사가 탐욕적 첫-적합 탓에 영영 조합해 보지 않는 부분해가
  재료가 된다.
- 구현: `residual`(훈련쌍 셀 불일치율) · `probe_partial`(라이브러리 대입 중 잔차를
  5% 이상 줄이는 최선) · `compose_guided`(그 씨앗을 1단계로 고정하고 마무리만 탐색).
- **단위 시험(6/6)**: 부분 진전 검출(그 자체로는 미완성해임을 assert) → 유도 합성이
  시험쌍까지 정확 → **유도 검사 횟수 < 맹목 검사 횟수**(경험이 탐색을 줄인다는 직접
  증거).
- **실데이터 판정**:

| 지표 | 맹목 합성(시도 150) | 유도 합성(시도 151) |
|---|---|---|
| 재구체화 시간 | 7,751초 | **51초** (**152배 감소**) |
| 후보 검사 | 8,405,297 | 1,948,671 (4.3배 감소) |
| 부분 진전 회수 | — | **133건** · 평균 잔차 감소 **0.271** |
| 신규 해결 | 0 | 0 |

- **판독**: 탐색 감소는 실증됐고(KPI 7), 교사가 버린 정보가 실제로 존재함이
  계량됐다(133건 × 27%). 그러나 신규 해결은 여전히 0 — 씨앗이 잔차의 27%를 줄여도
  **남은 73%를 닫을 마무리 스키마가 라이브러리에 없다**. 라이브러리가 아직
  교사 어휘의 압축본이기 때문(시도 150의 인수분해가 그대로 유효).
- **다음(루프 폐쇄)**: 부분 진전 133건을 **경험으로 기록**해 수면이 그 공통 구조를
  일반화하게 한다. 지금까지는 계산하고 버렸다 — 이제 저널에 남긴다
  (`monad-partial.tsv`). 성공 경험만이 아니라 "무엇이 가까워지게 했나"에서도
  구조가 나온다는 것이 가설이며, 이것이 사용자 루프의 "가설 → 경험 → 반례 →
  스키마 수정"이다.

### 시도 152~154: 루프 폐쇄 + anytime 합성 + **oracle 진단이 방향을 확정**

**Day 2→6 곡선(코드 완전 고정, 파일만 자람)**:

| | Day2 | Day3 | Day4 | Day5 | Day6 |
|---|---|---|---|---|---|
| 스키마(MONAD_DERIVED) | 39 | 39 | **95** | 95 | 95 |
| 부분 진전 회수 | — | 133 | 138 | 138 | 138 |
| 평균 잔차 감소 | — | 0.271 | **0.303** | 0.303 | 0.303 |
| 압축률 | 1.46 | 1.46 | **1.64** | 1.64 | 1.64 |
| 재구체화 시간 | 7,751초 | 51초 | 117초 | 283초 | 281초 |
| **신규 해결** | **0** | **0** | **0** | **0** | **0** |

- 경험 → 스키마 증가가 코드 수정 없이 일어났다(성공 73 → +부분진전 133 = 206건이
  스키마 39 → 95를 만듦). 압축률도 1.46 → 1.64.
- **효과 프로파일 계량**: 고친 셀 7,224 · 망가뜨린 셀 328 · **평균 정밀도 0.97**.
  부분 진전은 잡음이 아니라 **정밀한 부분해**다(부작용이 4.3%).
- 그러나 **신규 해결은 5일 연속 0** — 효율은 올랐고 경계는 그대로.

**🔬 Oracle reachability(미해결 40건, 예산 40만·깊이 4) — 결정적**:

| 멈춘 이유 | 건수 | 최선 잔차 |
|---|---|---|
| 표현 가능·발견 | 0 (0%) | — |
| 예산 소진(탐색 병목) | **0 (0%)** | — |
| **단조 경로 소진(개념 부재)** | **40 (100%)** | **0.291** |

- 40건 진단에 20초 — 즉 **탐색이 금방 죽는다**. 잔차를 줄이는 수가 애초에 몇 개
  없고, 1~2수 뒤 완전히 마른다.
- **판정: 탐색 개선의 상한은 0이다.** 사전분포·가지치기·깊이를 아무리 개선해도
  얻을 것이 없다. 현재 개념 공간에서 정답이 **도달 불가**이며, 필요한 것은
  **개념 발명**이다. (단조 경로 배제라는 한계는 그대로 고지 — 증거이지 증명 아님.)
- **평균 최선 잔차 0.291**: MONAD는 정답에서 29% 떨어진 곳까지 가서 **더 이상
  나아갈 수가 없다**. "못 찾는" 것이 아니라 "그 수가 없는" 것.

**→ 다음 아키텍처가 자동으로 정해졌다**: 잔차를 닫을 **새 개념의 생성**.
가장 원리적이고 동결 규율에 맞는 형태 = **잔차에 학습을 적용**한다. 부분 진전이
정밀도 0.97로 남긴 29%의 잔차는 작고 구조화돼 있다. 그 위에 기존 MONAD 학습
기계(EBM 문맥 분포 등)를 **새 위치에서** 돌려 마무리를 학습한다 — 손으로 규칙을
쓰는 것이 아니라 시스템이 데이터에서 규칙을 만든다.

### 시도 155(Day 7): 잔차 닫기(개념 학습) — **137건 시도 → 0건**

- 부분 진전 뒤 남은 잔차 쌍에 EBM(3×3 문맥→색 분포 + 이웃쌍 + 대칭축, 훈련 정확
  재현 게이트)을 적용 → **0건**.
- **누적 판정(이제 매우 좁혀졌다)**: 남은 29%의 잔차는
  ① 동결 연산의 **조합**으로 도달 불가(oracle 40/40 단조 소진)
  ② **국소 문맥 통계**로도 설명 불가(잔차 EBM 137/137 실패)
  → 필요한 것은 **비국소·관계적 구조**이며, 현재 어휘·통계 어느 쪽도 그것을
  담지 못한다.
- **근본 진술**: 지금 MONAD의 개념 공간 = **교사의 연산 어휘의 조합·매개변수화**.
  구조상 새 연산을 *만들* 수 없다. 추상화는 있는 것을 압축·재조합할 뿐이다.
  진짜 개념 발명에는 **생성적 기질**(연산 자체를 항으로 표현해 LGG가 새 연산을
  지어낼 수 있는 언어)이 필요하다.
- **다음(추측 금지, 계량 우선)**: 잔차가 *어떤 종류*인지를 **집계 통계로만**
  해부한다(과제별 판독 금지 규율 유지) — 남은 셀이 흩어져 있나 뭉쳐 있나,
  추가인가 삭제인가, 색 다양성은, 객체 경계와 정렬되나. 그 답이 생성적 기질의
  최소 사양을 정한다.

### 시도 156: 잔차 해부(집계 전용) — **생성적 기질의 사양이 데이터로 확정**

| 측정 | 값 | 함의 |
|---|---|---|
| 평균 잔차 셀 | 23.6 | 작다 |
| 덩어리 수 | 4.1(덩어리당 ~5.8셀) | **뭉쳐 있다** — 흩어진 잡음 아님 |
| 없어서/남아서/색만 틀림 | **42% / 23% / 35%** | 생성·삭제·재색이 **섞임** |
| 잔차 색 종류 | 2.5 | 소수 색 |
| 입력 객체 안 | 54% | 객체 연산도 격자 연산도 아님 — 반반 |
| 행/열 편중 | 0.13 | 줄 구조 아님 — **2차원 국소** |

**판정: 없는 것은 "국소 이웃을 보고 조건부로 생성·삭제·재색하는 규칙"이다.**
동결 어휘에는 전역 연산과 객체 속성 규칙은 있으나 이것이 없다.

### 시도 157: **패치 재작성 규칙**(생성적 기질) 구현 + 전이 게이트

- `arc_patch.rs`: 규칙 = `RULE(PATCH(3×3 이웃), 결과색)`. 훈련쌍의 **바뀐 자리**만
  기계적으로 추출(해석 없음) → LGG가 무엇이 조건인지 정하고 MDL이 채택 →
  **라이브러리에 축적**. 이 파일이 정하는 것은 규칙의 *문법*뿐, 내용은 경험이 채운다.
- **기존 시도와의 결정적 차이 = 전이**: EBM(141)·셀 역할 꿈(139)도 국소 문맥을
  썼지만 **과제마다 처음부터 배우고 버렸다**. 패치 규칙은 항이라 축적되고
  **다른 과제에 재사용**된다. 이 모듈의 존재 이유는 성능이 아니라 전이 가능성.
- **시험 3/3**: 추출은 바뀐 자리만 · **과제 A의 규칙이 과제 B의 훈련쌍을 재현**
  (code-free 전이의 정의) · 맞지 않으면 게이트가 막음(거짓 양성 방지).
- **패치 수면(앞 200과제)**: 규칙 경험 12,270건(135과제) → **새 규칙 4,049개**,
  압축률 2.35. 전이 시험 실행 중 — 게이트는 "다른 과제에서 배운 규칙이 이 과제의
  훈련쌍을 정확히 재현하는가".

### 시도 158: 전량 적용 실패(게이트 0건) → **증거 기반 규칙 선택**

- Day8: 규칙 4,049개 전량 적용 → **훈련 재현 통과 0건**. 원인은 명확 — 남의 과제
  규칙이 엉뚱한 자리에서 발화해 반드시 깨진다.
- **처방(원칙의 코드화)**: **기억은 가설일 뿐이다.** 이 과제의 훈련쌍으로 각 규칙을
  검증해 ①발화한 모든 자리에서 정답과 일치(반례 0) ②바뀌어야 하는 자리에서
  최소 1회 발화(쓸모)를 만족하는 것만 채택하고, 그 부분집합이 훈련쌍을 완전히
  재현할 때만 시험에 적용한다.
- 시험 4/4 — 새 시험이 이 상황을 그대로 재현: 유용 규칙 + 방해 규칙이 섞인
  라이브러리에서 **전량 적용은 실패, 증거 선택 후 성공**.

### ~~🏆 시도 159(Day 9): CODE-FREE LEARNING GAIN 최초 달성~~ **[시도 161에서 철회]**

> **철회 고지**: 아래 측정은 **오염된 조건**에서 나왔다. 패치 규칙을 앞 200과제에서
> 배우고 전이 시험을 **미해결 전체**(그 200과제 포함)에 돌렸으므로, 해결된 1건은
> 자기 훈련쌍에서 나온 규칙으로 자기를 푼 **순환**일 수 있었다. 엄격 홀드아웃
> (출처 과제 제외)에서 재측정한 결과 **0건**이었다 — 즉 그러했다. 상세는 시도 161.
> 어블레이션(106→105)은 "라이브러리가 없으면 못 푼다"만 말할 뿐, 그 라이브러리가
> **어디서 왔는지**는 구별하지 못한다. 이것이 이번 실험 설계의 결함이었다.

```
검증 1(패치 라이브러리 있음): 최종 해결 106/400 = 26.5% (동결 105 + MONAD_DERIVED 1)
검증 2(라이브러리 제거·어블레이션): 최종 해결 105/400 = 26.2% (동결 105 + MONAD_DERIVED 0)
```

**동일한 코드·동일한 바이너리가, 경험만 쌓인 뒤, 이전에 못 풀던 과제를 새로
풀었다. 그 능력은 MONAD_DERIVED 구조를 제거하면 사라진다.**

경로 전체가 조건을 만족한다:
1. 코드 동결(동결 솔버 미변경, HUMAN_DERIVED 그대로)
2. 경험 축적 — 135과제의 국소 변화 12,270건(기계적 추출, 해석 없음)
3. 수면 추상화 — LGG가 변수를 정하고 MDL이 채택 → 규칙 4,049개(MONAD_DERIVED)
4. **증거 기반 선택** — 이 과제의 훈련쌍이 어느 기억이 유효한지 판정(4,825개 채택)
5. 훈련 재현 게이트 통과 4건 → **시험까지 정확 1건**
6. **어블레이션: 그 1건이 사라진다**(106 → 105) — 인과 확정

**정직한 한계(반드시 함께 읽을 것)**:
- **n=1**. 재현 사례를 여러 건으로 늘리는 것이 M2-R 성공 기준이며 아직 미달.
- 게이트 통과 4건 중 3건은 **훈련에 맞고 시험에서 틀림**(과적합) — 선택 정밀도
  1/4. 다음 개선의 계량 근거.
- 규칙의 *문법*(3×3 이웃 → 색)은 사람이 정했다. *내용*은 전부 경험이 채웠다.
- 훈련셋 내 전이다(다른 훈련 과제 → 이 훈련 과제). 평가셋 일반화는 별개 시험.

### 시도 160~161: 일반화 압력(중립) → **자체 검증으로 시도 159 철회**

- **시도 160(일반화 압력)**: 규칙 선택에 오컴의 면도날(조건 상수가 적은 = 일반적인
  규칙 우선 + 지지도 하한 2). 선택 규칙 4,825 → 3,430으로 좁혔으나 **게이트 통과
  4건·해결 1건 동일** — 3건의 시험 실패는 "규칙이 과도하게 구체적"이어서가 아니다.
  (시험 6/6: 넓은 규칙 우선·단일 셀 암기 배제를 직접 검증.)
- **시도 161(오염 검증 — 스스로 의심하기)**: 규칙을 앞 200과제에서 배우고 전이
  시험을 **그 200과제를 포함한 미해결 전체**에 돌린 것이 설계 결함임을 발견.
  자기 훈련쌍에서 나온 규칙으로 자기를 푸는 것은 전이가 아니라 **순환**이다.
  - 조치: `arc-patchsleep`이 **출처 과제 목록**을 남기고(`monad-patchsource.txt`),
    `arc-real`이 **출처 과제에는 전이를 시도하지 않도록** 차단. 라이브러리를 지우고
    처음부터 재학습해 엄격 홀드아웃 재측정.
  - **결과: 증거선택 1,436개 · 훈련 재현 통과 0건 · 해결 0건 · 최종 105/400.**
  - **판정: 시도 159의 "code-free learning gain 1건"은 오염된 측정이었다. 철회.**
    게이트 통과 4건 전부가 자기 출처 과제였다.
- **어블레이션의 한계(교훈)**: 106→105는 "라이브러리가 없으면 못 푼다"만 말한다.
  그 라이브러리가 **어디서 왔는지**는 구별하지 못하므로, 전이 주장에는
  **출처 분리(provenance holdout)가 어블레이션과 함께 반드시 필요**하다.
  이 규율을 이후 모든 전이 실험에 적용한다.
- **현 확정 좌표: code-free learning gain = 0**(엄격 홀드아웃). 그대로 남는 것:
  탐색 152배 감소 · 스키마 39→95 · 부분 진전 138건(정밀도 0.97) · oracle 진단
  (탐색 병목 아님·개념 부재) · 전이 기계의 원리적 작동(단위 시험 6/6, 합성 사례).
- **다음**: 홀드아웃에서 게이트 통과가 0인 이유를 분해한다 — 규칙이 홀드아웃
  과제의 변화를 **덮지 못하는가**(경험 부족·패치 문법의 표현력 한계) vs
  **덮지만 오발화하는가**(선택 실패). 계량 없이 다음 설계를 하지 않는다.

---

### 시도 162~164: 전이 실패의 3중 분해 — **셀 국소 표현의 천장 확정(~17%)**

- **시도 162(덮개 진단기 `arc-coverage`)**: 홀드아웃 99건 전부 덮개 100%인데
  95건이 오발화(평균 341셀/과제)로 막혔다. 1차 판독은 "선택 실패" → 충돌 없는
  탐욕 덮개 선택(`select_cover`: 구체 규칙 우선 + 충돌 0만 채택, 시도 160의
  정렬 방향 오류도 정정)을 넣었으나 **게이트 통과 0 유지**.
- **진단의 결함을 스스로 교정**: "맞는 규칙이 존재"(덮개 100%)와 "**쓸 수 있는**
  맞는 규칙이 존재"는 다르다. **깨끗한 덮개**(그 과제에서 한 번도 오발화하지 않는
  맞는 규칙이 있는 셀)를 재정의해 측정 → **17.8%**. 선택 튜닝으로는 원리상 못 연다.
- **시도 163(문법 확장)**: 동결 기질이 이미 가진 객체 분해에서 나오는 특징 4종
  (객체 안/크기 순위/bbox 위치/색 종류 수)을 조건에 노출(9→13칸). 무엇이 조건인지는
  여전히 LGG·MDL이 결정. 결과: 깨끗한 덮개 17.8 → **18.9%**(+1.1%p) — 미미.
- **시도 164(적용 의미론 교정 — 이번 유닛의 핵심)**: 두 결함 발견.
  1. `patch_matches`가 변수를 **무조건 통과** — LGG가 발견한 "함께 변하는 자리는
     같은 변수"라는 구조를 적용 단계에서 버리고 있었다.
  2. 결과색을 같은 색끼리만 그룹핑해 일반화 — "결과 = 조건의 어느 자리 색"이라는
     **팔레트 독립 규칙이 원리상 생성 불가**였다. ARC 과제 간에는 리터럴 색이
     거의 공유되지 않으므로 과제 간 전이가 구조적으로 막혀 있었던 것.
  - 교정: 바인딩 일관 발화(`rule_fire`, subsumption 의미론 시행) + 결과색 교차
    일반화(전역 이웃쌍 LGG). **시험 15/15** — 핵심 시험: 팔레트 {3},{5} 경험 →
    LGG가 여덟 이웃과 결과를 같은 변수로 접음 → **본 적 없는 팔레트 {7} 과제
    재현**(상수 결과로는 불가능한 전이) + 이웃 섞이면 발화 안 함(바인딩 검증).
- **실데이터 재측정(규칙 4,417 → 5,608, 압축률 2.15)**: 깨끗한 덮개 **17.4%**.
  - 오르지 않았다. 그리고 이전 18.9%에는 **바인딩 불일치인데 우연히 맞은 발화**가
    포함돼 있었음이 드러났다(의미론을 정직하게 만들자 걷힘).
- **판정(3개 문법 변형에서 안정)**: 홀드아웃 바뀐 셀의 **~83%는 셀 국소 재작성
  표현으로 깨끗하게 기술 불가**. 원인은 규칙의 질이 아니라 **표현족 자체의 천장**
  — ARC 과제 간에 공유되는 것은 국소 패턴이 아니라 과제 수준 추상 구조다.
  셀 국소 재작성 전이 경로는 여기서 **측정으로 종결**한다(더 파지 않는다).
- **탐색 경로 소진 현황(모두 측정 종결)**: ①프로그램 재구체화(교사 포섭, 시도 150)
  ②합성 깊이 4(단조 소진 40/40, 시도 154) ③잔차 EBM(0/137, 시도 155)
  ④셀 국소 재작성 전이(천장 17%, 시도 162~164).
- **다음(운영자 지정 크리티컬 패스, 8단계)**: **표현 복수 가설**을 ARC에 배선한다.
  측정 우선 규율대로, 기계를 만들기 전에 **각 표현 후보의 기술 가능성 상한**부터
  잰다 — 셀 국소는 17%로 측정됐으니, 객체 수준 델타(재색/이동/삭제/출현)로
  홀드아웃 변화의 몇 %가 기술되는지를 같은 방식(집계 전용)으로 잰다. 표현 선택은
  사람이 아니라 `representation.rs`의 공통 점수가 한다.

### 시도 167~171: 객체 규칙 전이의 병목 사다리 — **홀드아웃 ④=2 최초 도달과 구성 확정**

**시도 167(부분 추출)**: 추출기가 "완전 기술 쌍"만 받아 이동·출현이 섞이면 확정된
재색·삭제 경험까지 버리고 있었다 — 시도 151의 원칙("교사가 버리는 정보를 회수")을
추출기가 다시 어긴 것. 부분 추출로 교정(+이동-삭제 오분류 가드). 경험 125건(10과제)
→ 623건(37과제), ②(일관 규칙 존재) 1/11 → 4/11.

**시도 168(과제 간 수면)**: `windows(2)` 이웃쌍은 대부분 같은 과제 안의 쌍 —
**전이 규칙의 원천인 과제 간 LGG가 사실상 막혀 있었다**. 서로 다른 과제의 같은
행동 경험쌍 전부를 접는 `sleep_obj_cross` 추가(105,878쌍 → 규칙 +5,494).
결과: ② 9/11 · ③(훈련 재현) 2 · **④(시험까지 정확) 2** — 출처 밖에서 배운
규칙만으로 미접촉 홀드아웃 2건이 시험까지 정확. 수면 결정론 확증(해시 동일).

**시도 169(이동 1급 델타)**: 시도 가능 풀 11건의 원인 = 행동 어휘(재색·삭제).
이동을 매칭(맨해튼 최근접)·추출(인코딩된 dx,dy)·일관성·적용(2패스: 지우기→그리기)
전 경로에 승격. 시험 21/21("오른쪽 2칸"의 과제 간 전이 포함).
경험 858건(58과제) · ① 11→17 · ② 11 · ④ 2 유지.

**시도 170(병목 해부 → 성질 18종 시도)**: 새 계량 — "성질 동일·행동 상이 쌍"
428개(10과제) = 12종 성질로 원리상 구분 불가, "일관 규칙 미발화 객체" 151/208.
x/y 순위·모양 클래스·구멍·색 유일·비율 6종 추가(18종) → 모호쌍 428→164로 62%
감소했으나 **④ 2→0 후퇴** — 성질 수에 비례해 쌍 LGG에서 우연히 상수로 굳는
슬롯이 늘어 전이 폭이 줄었다(일반화-특이성 트레이드오프의 실측).

**시도 171(스키마 정련 + 구성 확정)**: 일반화 사다리의 다음 칸(같은 행동 스키마끼리
재일반화, `sleep_obj_refine`) 1라운드로는 회복 불가(④=0 유지). **증거 우선으로
판정: ④=2를 실증한 12성질+이동 구성을 유지, 18종 확장은 측정으로 기각**(사다리가
다중 라운드로 강해진 뒤 재시도). 정련 패스는 무해하므로 수면 파이프라인에 존치.

**방법론 소득**: ①→②→③→④의 병목 사다리를 각 단계 계량으로 오르내린 것 자체가
M2-R의 작업 방식이다 — 어느 단계도 추측으로 넘지 않았고, 후퇴(v3)는 즉시 계량으로
기각했다. 다음: Day 12(v1+cross 구성 어블레이션·결정론) 완료 대기 중, v2+정련
확인 후 **성장 곡선**(경험 풀 25/50/100/200 · 고정 홀드아웃 인덱스 200+)으로
"경험 ↑ → 미접촉 해결 ↑" 그래프를 실측한다.

### 시도 172: **Code-Freeze 성장 곡선 실측** — 경험 ↑ → 미접촉 해결 ↑

운영자가 요구한 그래프(`experience ↑ → MONAD_DERIVED schemas ↑ → reuse ↑ →
unseen success ↑`)를 실측했다. 조건: **4스테이지 모두 동일 바이너리(코드 동결)** ·
**고정 홀드아웃 149건(로드 인덱스 200 이후, 스테이지 간 불변 확인)** · 출처 분리
(provenance holdout) 구조 보장.

| 경험 풀(과제) | 출처 | 규칙(MONAD_DERIVED) | 일관 규칙 존재 | **미접촉 해결** |
|---|---|---|---|---|
| 25 | 10 | 402 | 2/16 | 0 |
| 50 | 15 | 784 | 3/16 | 0 |
| 100 | 31 | 2,899 | 5/16 | 0 |
| 200 | 58 | 10,488 | **10/16** | **1** |

- 재사용 가능성(일관 규칙 존재)이 **단조 증가**(2→3→5→10), 능력(④)은 경험이
  충분해진 마지막 스테이지에서 발현. 코드는 한 줄도 변하지 않았다.
- 같은 구성의 실전 각성(Day12-A): **106/400 = 동결 105 + MONAD_DERIVED 1** —
  진단기(objcover)가 아니라 본 측정기(arc-real)의 게이트에서도 신규 1건 확정.
- 검증 현황: 수면 결정론(해시 동일) ✓ · 출처 분리 ✓ (구조 보장) ·
  어블레이션(Day12-B)·각성 결정론(Day12-C) 실행 중 — 완료 시 최종 판정.
- (부기) v2r 라이브러리 ④=1로 보인 앞선 측정은 Day12-A 종료로 solved가 106이
  된 시점의 홀드아웃 축소 효과 — 스냅샷 시점을 함께 기록해야 한다는 교훈.

### 🏆 시도 174: **CODE-FREE LEARNING GAIN — 5중 검증 통과 (오염 없음)**

시도 159의 철회를 낳은 결함(출처 분리 부재)을 포함해, 주장에 필요한 검증을 전부
자동화해 한 번에 돌렸다. **최종 코드 상태**(성질 12종 + 이동 1급 + 과제 간 수면 +
정련)로 라이브러리를 처음부터 다시 만들어 측정했다.

| 검증 | 결과 |
|---|---|
| ① 수면 결정론 | **PASS** — 두 번 재수면, SHA256 `ECC91A91AA8D5875` 동일 |
| ② 각성 A (라이브러리 ON) | **106/400** = 동결 105 + MONAD_DERIVED **1** |
| ③ 각성 B (어블레이션, OFF) | **105/400** = 동결 105 + MONAD_DERIVED 0 |
| ④ 각성 C (A 재실행) | **106/400**, A와 해결 목록 완전 동일 — **PASS** |
| ⑤ 출처 분리 | **PASS** — 획득 과제가 규칙 출처 목록에 없음 |
| 회귀 | **0건** |

**획득 과제 `aabf363d`의 좌표(가장 강한 형태)**:
- 규칙은 정렬 순서 앞 **200과제**에서만 학습(그중 57과제가 경험 생산).
- `aabf363d`는 **267번째** — 수면 패스가 **본 적조차 없다**. 출처 목록에서
  빠진 것이 아니라 **구조적으로 미접촉**이다.
- 어블레이션 해결 목록에 없음 = 동결 솔버 단독으로는 못 푼다.

**따라서 다음 문장이 성립한다**:

> 코드는 한 줄도 바뀌지 않았고 바이너리도 동일한데, MONAD가 경험을 쌓고 수면을
> 거친 뒤 **어제 못 풀던, 본 적도 없는 문제를 스스로 풀었다.** 그 능력은
> MONAD_DERIVED 구조를 제거하면 사라진다.

**능력의 계보(전부 계량이 정한 것, 사람이 고르지 않음)**:
잔차 해부 → 셀 국소 표현 천장 17.4% 측정 → 표현 경쟁에서 객체 델타 72.9% 승리 →
객체 성질 조건 규칙 문법 → 부분 추출(교사가 버린 정보 회수) → 과제 간 수면(전이
규칙의 원천) → 이동 1급 델타 → 증거 기반 선택.

**정직한 한계(반드시 함께 읽을 것)**:
- **n=1**. M2-R 성공 기준은 **여러 사례의 재현**이며 아직 미달이다.
- 훈련셋 내부 홀드아웃이다(앞 200 학습 → 267번 해결). 평가셋은 봉인 상태 그대로.
- 규칙의 *문법*(성질 12칸·행동 3종)은 HUMAN_DERIVED 기질이다. *내용*(어느 성질이
  조건이고 어떤 행동을 부르는지)은 전부 MONAD_DERIVED다.
- 성장 곡선상 이 능력은 경험 200과제 규모에서 처음 발현했다(25/50/100에서는 0).

**다음(시도 175, 작성 완료·측정 대기)**: ②(일관 규칙 존재 10/16) → ③(훈련 재현 2)
간극의 원인은 "바뀐 객체 208개 중 151개에 어떤 규칙도 발화하지 않음"이다. 선택된
규칙은 정의상 전부 일관적이므로 순서 문제가 아니라 **일반성 부족**이다. 처방은
**일반화 사다리를 고정점까지 올리는 것**(`sleep_obj_refine_rounds`) — 과일반화는
사람이 막지 않는다. MDL 이득이 음수가 되는 순간(두 스키마의 상이 슬롯 ≥8) 사다리가
스스로 멈춘다.

### 시도 175~180: 세 처방의 기각과 복제 도입 — **객체 성질 표현의 천장 계량**

시도 174의 검증된 이득(n=1) 이후, 성공 기준인 **여러 사례 재현**을 위해 네 가지를
차례로 시도했다. 셋은 측정으로 기각했고, 하나는 상한을 열었으나 전환되지 않았다.

| 시도 | 처방 | 결과 | 판정 |
|---|---|---|---|
| 175 | 일반화 사다리(0/1/3/6 라운드) | 4라운드 고정점, 규칙 +808 · **④ 2 불변, 미발화 151 불변** | 무효 |
| 177 | 결정 목록 선택(예외 우선+가림) | 훈련 재현 2(동일) · 시험 **1**(더 나쁨) · **한계 기여 0건** | 기각 |
| 170 | 성질 18종 | 모호쌍 428→164 · **④ 2→0** | 기각(시도 171) |
| 180 | **복제(ACT_COPY) 1급 델타** | ① 17→**23** · ② 11→**16** · ③④ 2 불변 | 상한만 열림 |

**시도 176~178의 진단(무엇을 만들지 정한 계량)**:
- 미발화 바뀐 객체 151개의 원인 분해: **정답 규칙 부재 64개**(경험 구멍) ·
  **있었으나 필터 탈락 87개**(성질 판별력). 과반이 후자 → 선택 규율을 의심해
  결정 목록을 만들었으나 한계 기여 0.
- ① 상한의 정체: 홀드아웃 254건 중 237건 탈락 — 크기 불일치 97 · 짝 없는 출력
  78 · 양쪽 58 · 짝 없는 입력 4. 짝 없는 출력 객체 2,382개를 갈라 보니
  같은 모양·색 원본 있음 912(복제) · 같은 모양만 347 · **원본 없음 1,123(진짜 출현)**.
  → 복제만으로 기술 가능한 과제 21건으로 계량, 그래서 복제를 만들었다.

**시도 180의 설계 요점**: 복제는 유지·재색·이동·삭제와 달리 **가산적**이다(한 객체가
여러 사본을 낳는다). 배타 행동은 첫 발화 승, 복제는 발화한 규칙을 **전부** 실행한다.
시험 24/24(사본 2개 추출 · 완전 기술 인정 · 색·위치 다른 과제로 전이).

**🧱 결정적 관측 — 표현의 천장**:

복제 도입으로 경험 858→1,982건(58→79과제), 규칙 10,534→**22,948개**(2.2배)로
늘었는데 **미발화 원인 분해가 64+87=151로 완전히 동일**했다(바뀐 객체는 208→212).

> **같은 종류의 경험을 아무리 더 쌓아도 이 간극은 닫히지 않는다.**
> 12종 성질이 그 자리들을 구분하지 못하기 때문이며, 이는 셀 국소 표현의
> 천장(17.4%)과 같은 성질의 한계다. 객체 델타 표현의 덮개는 61/212 = **29%**에서
> 평평해진다.

**다음(시도 181, 계량 우선)**: 현 성질 12종은 전부 **내재적**(색·크기·모양쌍둥이·
테두리)이거나 **전역적**(다수색·객체 수)이다. 없는 것은 **관계적** 성질 —
포함·인접·정렬·최근접·유일성이다. 다만 성질을 늘리면 과잉 구체화로 전이가
죽는다는 것을 시도 170에서 이미 겪었다. 그러므로 **먼저 재고 나중에 만든다**:
후보 관계 성질 각각이 **모호쌍 428개를 몇 개나 가르는지**를 홀드아웃에서 계량해,
정보가 있는 것만 채택한다. 어느 성질이 좋은지는 사람이 고르지 않는다.

### 시도 181~183: 성질 확장의 **법칙 확정**과 일반화 연산자의 교체

**시도 181(만들기 전에 재기)**: 시도 170에서 성질을 추측으로 늘렸다 전이가 죽은
교훈에 따라, 후보 관계 성질 11종의 **모호쌍 분리율**을 먼저 쟀다(`arc-propscan`).

```
quadrant 71.7% · touch_count 28.0% · nn_rank 23.6% · row/col_aligned 6.1/5.8%
contains_other 0.7% · contained_by·shape_unique·color_unique·
same_color_count·same_shape_count 모두 0.0%
```

위치가 지배적이고 6종은 정보가 **0**이다 — 시도 170에서 넣었던 것 상당수가 여기
해당하며, 왜 실패했는지가 사후에 설명된다.

**A/B 판정(같은 바이너리·같은 경험 풀, 슬롯만 환경변수로 변경)**:

| 구성 | ② | ③ | ④ | 규칙 부재 | 필터 탈락 |
|---|---|---|---|---|---|
| **A 없음(12종)** | 17 | **2** | **2** | 53 | 94 |
| B quadrant | 18 | 1 | 1 | 67 | 66 |
| C touch | 17 | 1 | 1 | 70 | 78 |
| D 둘 다 | 17 | 2 | 1 | 80 | 58 |

정보량 계량은 **옳았다** — quadrant가 필터 탈락을 94→66으로 30% 줄였다. 그러나
규칙 부재가 53→67로 올라 총합은 그대로고 ④는 떨어진다.

> **세 번 측정된 법칙**: 성질을 늘리면 판별력은 오르고 덮개는 내린다.
> 이 표현에서 그 교환은 항상 손해다. (18종·quadrant·touch 모두 동일 양상)

**원인은 성질이 아니라 일반화 연산자다**: 쌍 LGG는 두 경험이 **우연히 다른**
슬롯만 변수로 만든다 — "이 슬롯은 무관하다"를 알아낼 수단이 없다. 슬롯이 늘수록
우연한 불일치가 늘어 MDL 이득이 음수가 되고 규칙이 통째로 기각된다(덮개 붕괴의 기전).

**시도 182(반례 기반 조건 탈락)**: LGG는 긍정 쌍만 본다. 경험을 **반례 집합**으로
쓰면 무관한 슬롯을 *의도적으로* 떨어뜨릴 수 있다 — 구체 경험에서 출발해 슬롯을
하나씩 변수로 바꾸되 **반례가 없을 때만** 채택. 특수→일반 방향의 version-space
탐색이며 LGG와 상보적이다. 팔레트 독립(param↔슬롯 공유 변수)도 같은 검사로 시도.
시험 25/25 — 신규 시험이 LGG의 실패 지점을 재현한다(두 경험이 우연히 같은
객체 수=2를 LGG는 상수로 굳혀 3개짜리 과제에서 발화 못 하지만, 반례 검사는
그 슬롯을 떨어뜨려 발화한다).

**시도 183(반례 범위의 교정)**: 첫 구현은 79과제를 **하나의** 반례 집합으로 묶었다
→ 규칙 387개 추가에도 홀드아웃 지표가 **전부 불변**(53+94=147 동일). 당연한 결과다
— "최소 객체를 지운다"는 규칙은 그러지 *않는* 다른 78과제에서 전부 반례가 되므로
아무 슬롯도 못 떨어진다. **규칙의 타당성은 그것이 나온 에피소드 안에서 성립하고,
과제 간 타당성은 선택 게이트가 새 과제의 증거로 판정한다.** 반례 검사를 과제 내로
한정했다.

### 시도 184: 관계 조건 도입 → **보존 법칙 147의 확정(여덟 번째 무효)**

**가설**: 지금까지의 조건은 전부 **절대적**이었다("내 색이 3"). 절대 조건은
팔레트·배치가 다른 과제에서 성립하지 않으므로 구체화가 곧 덮개 손실이고,
그래서 부재+필터의 합이 네 번 보존됐다. **관계 조건**("내 색 == 최대 객체의 색")은
팔레트 불변이라 덮개를 잃지 않고 판별할 것이다.

**구현**(두 번 스스로 틀리고 두 번 고침):
1. 등식을 먼저 시도 → 등식은 *제약*이라 떨어뜨릴 수 있는 슬롯까지 묶어 규칙이
   더 좁아짐(시험이 잡음).
2. 떨어뜨린 뒤 등식 → 전역 성질(최대 객체의 색)은 과제 내 모든 객체에 같은 값이라
   반례 없이 떨어지고 **관계가 파괴됨**.
3. 해법: 순서를 고르지 않고 **두 가설을 다 남긴다**(떨어뜨리기만 한 것 / 등식 먼저).
   여러 일반화 수준이 공존하고 과제의 증거가 고른다 — 이 설계의 일관된 규율.

**단위 시험 26/26** — 신규 시험이 성질을 직접 검증: 팔레트 (3,5)에서 배운 규칙이
완전히 다른 (7,2)를 재현하고 (9,1)에서 시험까지 정확(절대 색 조건으로는 원리상 불가).

**실데이터**: 탈락 규칙 353 → **559개**(+58%, 관계 조건이 실제로 만들어짐).
그러나 ①23 ②17 ③2 ④2 **완전 동일**, 부재 50 + 필터 97 = **147**.

**🧱 보존 법칙의 확정 — 여덟 번의 독립 개입**

| # | 개입 | 부재+필터 | ④ |
|---|---|---|---|
| 1 | 경험 2.2배(858→1,982건) | 151 | 2 |
| 2 | 일반화 사다리(고정점 4라운드) | 151 | 2 |
| 3 | 결정 목록 선택 | 151 | 2 (한계 기여 0) |
| 4 | 성질 quadrant | 129~133 | 1 |
| 5 | 성질 touch | 148 | 1 |
| 6 | 반례 탈락(전역) | 147 | 2 |
| 7 | 반례 탈락(과제 내) | 147 | 2 |
| 8 | **관계 등식** | **147** | 2 |

> **판정**: "객체 속성 벡터 → 행동" 표현족 안에서 만들 수 있는 모든 기전을
> 소진했다. 남는 147개는 기전의 문제가 아니라 **표현이 담지 못하는 정보**다.
> 특히 관계 등식(집계된 전역 성질과의 동일성)조차 무효라는 것은, 필요한 정보가
> **특정 객체 쌍 사이의 관계**(어느 상자 안에 있는가, 어느 화살표가 가리키는가,
> 어느 열쇠와 모양이 같은가)임을 시사한다 — 이는 속성 벡터가 아니라 **일차
> 관계 논리**(대상 변수에 대한 양화)를 요구한다.

**다음(측정 우선 규율 유지)**: 일차 관계로 넘어가기 전에, 그것이 실제로 147을
가르는지부터 잰다. 모호쌍에 대해 "어떤 객체 O가 존재해 관계 R(self,O)가 다르다"가
성립하는지를 구체 관계 목록으로 계량한다. 가르면 학습기를 만들고, 못 가르면
천장이 더 깊다는 뜻이므로 그대로 기록한다.

### 시도 185: 일차 관계 계량 + 관계 슬롯 → **보존 법칙은 조건 언어와 무관**(아홉 번째)

**측정 우선(`arc-relscan`)**: 여덟 번의 무효 뒤, "필요한 정보가 특정 객체 쌍
사이의 관계"라는 가설을 **만들기 전에** 쟀다. 모호쌍 428개에 대해 "어떤 객체 O가
존재해 R(self,O)가 다른가"를 관계 11종으로 계량:

```
above 57.2% · adjacent_to 48.6% · left_of 45.1% · same_row_as 31.8% ·
same_col_as 7.5% · bigger_than 1.9% · same_shape_as 0.5% ·
same_color_as 0.0% · inside_of 0.0%
★ 하나라도 가르는 모호쌍: 373/428 = 87.1%
```

여덟 번의 무효 뒤 처음으로 **"정보가 있다"**는 측정이었고, 상위가 전부 기하
관계이며 의미 관계가 0%라는 것이 방향을 좁혔다.

**관계 슬롯 A/B**(상대의 *정체*가 아니라 **역할**을 담아 배치 불변으로 설계):

| 구성 | 부재 | 필터 | 합 | ④ |
|---|---|---|---|---|
| 없음 | 50 | 97 | **147** | **2** |
| above | 60 | 81 | 141 | 1 |
| adjacent | 64 | 84 | 148 | 2 |
| 둘 다 | 69 | 73 | 142 | 1 |

계량은 또 옳았다 — 필터 탈락이 97→73으로 25% 감소. 그러나 부재가 50→69로 정확히
그만큼 상승. **보존 법칙은 절대/관계 구분과 무관하다.**

**🧱 보존 법칙의 기전(이제 설명된다)**

> 덮개 ≈ (경험 지점 수) × (평균 일반성)이고, **일반성과 판별력은 1:1로 교환된다.**
> 조건 언어를 무엇으로 바꾸든 이 곱은 **경험 지점 수에 묶여** 있다.
> 한 경험 지점은 자기와 "충분히 닮은" 객체에만 규칙을 인가할 수 있고, 슬롯을
> 바꾸는 것은 *무엇이 닮음인가*를 바꿀 뿐 **인가 지점의 수를 늘리지 못한다**.

이것이 아홉 번의 개입이 모두 147 근처를 남긴 이유이며, 성장 곡선에서 **경험량만이
유일하게 부재를 줄인 것**(34→18→16)과 정확히 일치한다.

**M2-R의 정직한 좌표**:
- ✅ Code-free learning gain **1건**, 5중 검증(수면 결정론·어블레이션·각성 결정론·
  출처 분리·회귀 0), 획득 과제는 수면이 본 적 없는 267번째. 태그 `COGNITIVE_FREEZE_GEN1`.
- ✅ 표현 자기선택(계량이 객체 델타 72.9% > 셀 국소 17.4%를 골랐다)
- ✅ 스키마 자기발견 25,919개 · 탐색 152배 감소 · 어블레이션 인과 확정
- ❌ **여러 사례 재현**: n=1에서 정체. 아홉 개 기전으로 열리지 않음이 계량됨.
- **다음 단계의 근거**: 조건 언어가 아니라 **인가 지점의 수**를 늘려야 한다 —
  즉 (a) 경험 자체를 늘리거나(성장 곡선이 지지), (b) 한 규칙이 서로 닮지 않은
  객체들을 동시에 옳게 덮을 수 있는 **행동 언어**(계산된 행동)로 가야 한다.
  후자는 지금까지 한 번도 변주하지 않은 축이다.

### 시도 186~188: **원리상 상한의 발견 — 아홉 번의 판정이 잘못된 지표 위에 있었다**

**시도 186(인가 지점 3.5배)**: 보존 법칙의 기전으로 "덮개 ≈ 인가 지점 × 일반성"을
세우고, 반례 탈락이 완전 기술 과제 26개만 쓰던 것을 부분 관측 지점으로 확장
(씨앗 558→**1,982**, 과제 26→79, 탈락 규칙 559→1,658). **결과: 부재 50→46, 합
147 그대로.** 내가 세운 기전 설명이 **내 측정으로 기각**됐다.

**시도 187(원리상 상한)**: 조건 언어 9가지에도 인가 지점 3.5배에도 불변인 147이면
라이브러리가 아니라 **표적 과제의 성질**이다. 객체 X가 원리상 덮이려면 같은 과제
안에 성질 동일·행동 상이 객체가 없어야 한다 — 그것을 직접 셌다.

```
바뀐 객체 212개
├ 원리상 덮기 불가 122개 (58%)   <- 라이브러리와 무관한 상한
└ 원리상 덮기 가능  90개
   ├ 실제로 덮음    65개 (72%)   <- 학습 기계는 이론 최대의 72%를 달성 중
   └ 못 덮음        25개
```

**아홉 번의 무효가 설명된다**: 나는 25개짜리 구멍을 놓고 기전을 아홉 번 바꿨고,
진짜 벽인 122는 상수였다. 학습 기계 자체는 이미 이론 최대의 72%를 내고 있었다.

**시도 188(천장 스캔) — 판정의 정정**:

| 구성 | 원리상 차단 | **천장** | 실제 덮음 |
|---|---|---|---|
| 없음(12종) | 122 | 90 | **65** (72%) |
| quadrant | 80 | 132 | — |
| touch | 106 | 106 | — |
| **quadrant+touch** | **69** | **143** | **78** (55%) |
| above | 89 | 123 | — |
| above+adjacent | 82 | 130 | — |

**성질 확장은 천장을 90→143(+59%) 올리고 절대 덮개도 65→78(+20%) 올린다.**
그런데 나는 아홉 번 내내 **④ 하나만 보고** "성질 확장은 손해"라고 판정했다.
④는 ③(과제당 100% 덮개)에 걸린 전부-아니면-전무 게이트라, 집계 개선이
"어느 과제가 100%에 도달하는가"로 전환되지 않는다. **잘못된 지표로 기각한 것이다.**

**최종 지표 A/B**(같은 바이너리·같은 경험 풀, 성질만 변경):
- 없음: 규칙 27,116 · 게이트 통과 2 · **최종 106/400**
- quadrant+touch: 규칙 63,385 · 게이트 통과 2 · **최종 106/400**

성질 확장은 최종 지표를 **올리지도 내리지도 않는다**. 천장과 집계 덮개는 올랐지만
과제당 100% 게이트를 넘지 못했다.

**③ 게이트는 임의 기준이 아니다**: 규칙이 못 덮는 객체는 그대로 두는 것이 예측이므로,
훈련쌍을 정확히 재현하지 못하면 시험 예측의 근거 자체가 없다. 즉 부분 덮개에
부분 점수를 줄 방법이 원리적으로 없다 — 이것이 집계 개선이 능력으로 전환되지
않는 구조적 이유다.

**회귀 검증(최종 코드 상태, 5중 전부 통과)**:
```
수면 결정론  PASS (SHA256 21495C1915F6FB6E x2)
각성 A(ON)   106/400 = 동결 105 + MONAD_DERIVED 1
각성 B(OFF)  105/400 = 동결 105 + MONAD_DERIVED 0    <- 어블레이션
각성 C(재실행) 106/400, A와 해결목록 완전 동일          <- 결정론
출처 분리    PASS (aabf363d) · 회귀 0건
```
복제·관계 등식·부분 관측 지점·14슬롯 리팩터를 모두 거친 뒤에도 n=1이 유지된다.

---

## M2-R 중간 총괄 (시도 156~188)

**달성**:
1. **Code-free learning gain n=1, 5중 검증** — 동일 코드·동일 바이너리가 경험만
   쌓은 뒤 **수면이 본 적조차 없는 267번째 과제**를 해결. MONAD_DERIVED 제거 시 소멸.
2. **표현 자기선택** — 계량이 객체 델타(72.9%)를 셀 국소(17.4%)보다 골랐다.
3. **스키마 자기발견** 27,116개 · **탐색 152배 감소** · 어블레이션 인과 확정.
4. **성장 곡선** — 경험 ↑ → 재사용 가능성 ↑(② 3→8→9), 규칙 부재 ↓(34→18→16).

**미달**: 여러 사례 재현(n=1 정체).

**미달의 원인 — 계량으로 확정**:
- 홀드아웃 바뀐 객체 212개 중 **122개(58%)가 원리상 차단**(같은 과제에 성질
  동일·행동 상이 객체 존재). 라이브러리로 열 수 없다.
- 학습 기계는 남은 90개 중 65개(**이론 최대의 72%**)를 이미 덮는다.
- 성질을 늘리면 천장이 90→143으로 오르고 절대 덮개도 65→78로 오르지만,
  **최종 지표는 106으로 불변** — ③이 과제당 100% 덮개를 요구하기 때문이며,
  그 요구는 임의가 아니라 예측의 전제다.

**열 번의 개입과 그 판정**(전부 기록):
경험 2.2배 · 일반화 사다리 · 결정 목록 · quadrant · touch · 반례탈락(전역) ·
반례탈락(과제내) · 관계 등식 · 관계 슬롯 · 부분 관측 지점 → 최종 지표 전부 106.

**다음 단계의 근거**: 이 표현족에서 남은 자유도는 소진됐다. 진짜 상한을 올리려면
"객체 성질 → 행동"이 아니라 **부분 덮개에 신용을 줄 수 있는 구조**(예: 객체 규칙이
잔차를 줄이고 다른 계층이 마무리하는 계층 간 합성)나, 과제당 100% 요구를 우회하는
**다른 예측 형식**이 필요하다.

### 시도 189~191: 계층 간 합성 · 부분 선택 · 반복 적용 — 세 번의 무효로 경로 종결

상한 분석(시도 187)이 지목한 유일한 출구는 "부분 덮개에 신용을 주는 구조"였다.
세 단계로 그것을 만들고 전부 계량했다.

| 시도 | 처방 | 결과 |
|---|---|---|
| 189 | **계층 간 합성** — 객체 규칙이 잔차를 줄이고 동결 계층(격자 연쇄·셀 규칙·EBM)이 마무리 | 잔차 감소 **10건** → 마무리 **0** |
| 190 | **부분 선택** — 합성 경로가 완전 기술 과제만 받던 제약 해제("완전성은 게이트의 몫") | 잔차 감소 10→**23건** → 마무리 **0** |
| 191 | **반복 적용** — 규칙 적용 후 성질이 바뀌면 다른 규칙이 발화(최소 깊이·고정점·상한 4) | 깊이>1 통과 **0건** |

**동결 계층의 잔차 마무리 실패는 세 번 측정됐다**(시도 155에서 137→0, 189에서
10→0, 190에서 23→0). 네 번째 마무리기는 만들지 않는다.

**MONAD_DERIVED 규칙이 23개 과제의 잔차를 실제로 줄인다**는 것은 확인됐다
(어블레이션에서 0건 — 인과 확정). 다만 그 부분 진전이 완결로 전환되지 않는다.

---

## 🏁 M2-R 최종 총괄 (시도 156~191, 커밋 ee0c105+, 태그 COGNITIVE_FREEZE_GEN1/GEN2)

### 운영자가 지정한 8단계 — 전부 구현·실증

| 단계 | 구현 | 증거 |
|---|---|---|
| 1 episodic 기록 | 경험 저널(성공·부분진전·객체 델타) | 1,982건/79과제 |
| 2 sleep 공통구조 | LGG + MDL + 과제 간 수면 | 27,116 규칙 |
| 3 변수화 schema | 반례 기반 조건 탈락(version-space) | 관계 등식 포함 |
| 4 재구체화 | 증거 기반 선택 + 도메인 가드 | 시험 26/26 |
| 5 schema 조합 | anytime 합성(깊이·잔차·메모이제이션·정규형) | 단위 실증 |
| 6 library 축적 | 출처 태그·영속·사전분포 | 디스크 성장 |
| 7 탐색 prior | 학습된 사전분포 순회 | **152배 감소** |
| 8 표현 복수 가설 | representation.rs + 표현 경쟁 계량 | 객체 델타 72.9% > 셀 국소 17.4% |

### 핵심 증거

> **동일 코드·동일 바이너리가, 경험만 쌓은 뒤, 수면이 본 적조차 없는 267번째
> 과제를 스스로 풀었다. 그 능력은 MONAD_DERIVED 구조를 제거하면 사라진다.**

5중 검증(수면 결정론·어블레이션·각성 결정론·출처 분리·회귀 0) 전부 통과,
대규모 리팩터 후 재검증에서도 유지.

### 미달과 그 이유 — 추측이 아니라 계량

- **여러 사례 재현 미달(n=1)**.
- 홀드아웃 바뀐 객체 212개 중 **122개(58%)가 원리상 차단** — 같은 과제에 성질
  동일·행동 상이 객체가 있어 **어떤 규칙으로도** 구별 불가(라이브러리와 무관).
- 학습 기계는 남은 90개 중 65개, **이론 최대의 72%**를 이미 덮는다.
- ③의 100% 재현 요구는 임의가 아니라 **예측의 전제**이므로 부분 점수가 원리적으로
  불가능하다 — 집계 덮개를 20% 올려도(성질 확장) 최종 지표는 불변이었다.

### 열두 번의 개입과 판정(전부 기록)

경험 2.2배 · 일반화 사다리 · 결정 목록 · quadrant · touch · 반례탈락(전역) ·
반례탈락(과제내) · 관계 등식 · 관계 슬롯 · 부분 관측 지점 · 계층 간 합성 ·
반복 적용 → **최종 지표 전부 106**.

### 다음 세대(GEN3)를 위한 근거 있는 설계 지침

1. **표현의 교체가 필요하다**: 객체 속성 벡터는 58%를 원리상 구분하지 못한다.
   필요한 것은 특정 객체 쌍에 대한 **일차 관계 논리**(대상 변수 양화) — 계량이
   모호쌍의 87.1%를 일차 관계가 가른다고 말했다(상위는 전부 기하: above 57.2%,
   adjacent 48.6%, left_of 45.1%; 의미 관계는 0%).
   단, 그것을 **고정폭 슬롯으로 근사하면 보존 법칙에 걸린다**(시도 185에서 실증) —
   진짜 양화가 필요하다.
2. **크기 변환 과제(97/237)**는 이 표현 밖이다. 별도 표현이 필요하다.
3. **잔차 마무리는 세 번 실패했다**. 동결 계층에 기대지 말 것.

### 시도 192~193: **GEN3 — 존재 양화가 있는 일차 관계 계층**

상한 분석이 지목한 다음 표현을 구현했다. 기존 객체 계층(GEN2, 검증된 n=1)은
건드리지 않고 별도 모듈(`arc_relrule.rs`)로 세웠다.

**규칙 형태**: `∃Y ≠ X. R(X,Y) ∧ target_cond(Y)` — 그리고 결정적으로 **self와
target의 슬롯이 변수를 공유**할 수 있다("내 색과 같은 색의 객체가 내 바로 위에
있다"). 이것이 속성 벡터가 원리상 담지 못하는 정보이며, 시도 187이 측정한
122개(58%)를 여는 유일한 형태다. 관계 4종(above·adjacent·left_of·same_row)은
`arc-relscan`이 분리율로 고른 것이다(사람이 아니라).

**구현 중 세 번 틀리고 세 번 고침**:
1. 떨어뜨리기 우선 → self·target이 각각 자유 변수가 되어 등식을 맺을 상수가
   남지 않음 → 시도 184의 해법(두 순서를 다 남기고 증거가 고름) 적용.
2. `above`가 "위쪽 어딘가" → 열이 안 겹치는 객체까지 포함해 판별력 소멸
   (규칙은 만들어져도 전이 과제에서 발화 0회) → **직접 위/왼쪽**(열·행 겹침)으로 교정.
3. 성질 벡터로 객체를 되찾음 → 성질이 같은 객체들이 같은 상대 목록을 공유,
   **하필 그런 객체들이 이 계층의 존재 이유** → 객체 색인을 그대로 전달
   (`pair_deltas` 신설).

**단위 시험 28/28** — 신규 시험이 전제까지 검증한다: 두 대상의 성질 벡터가
**동일함을 먼저 assert**한 뒤(속성 규칙으로 원리상 구별 불가임을 확인), 존재 양화
규칙이 그것을 구별하고 다른 팔레트로 전이되는지 본다.

**실데이터 1차(시도 192)**: 관계 지점 5,435개(58과제) → 씨앗 4,634 → **규칙 2,335개**,
그러나 홀드아웃 게이트 통과 **0건**.

**진단(시도 193)**: 폭발 방지로 씨앗을 관계별 **첫 상대 하나**로 제한한 것이
표현력을 깎고 있었다 — 판별에 필요한 상대가 첫 번째가 아니면 그 규칙이 아예
만들어지지 않는다. 서로 다른 성질의 상대를 최대 4개까지 씨앗으로 쓰도록 완화.

### 시도 194~196: **GEN3의 실효 — 원리상 상한 122 → 21** + 두 계층 결합

**시도 194(관계 표현의 상한 측정)**: 관계 서명(상대들의 성질)까지 포함해도
구별되지 않는 객체를 셌다. 이것이 표현 교체의 실효를 속성 상한과 직접 비교하는
유일한 방법이다.

| | 속성 표현(GEN2) | **관계 표현(GEN3)** |
|---|---|---|
| 원리상 차단 | 122/208 (58%) | **21/208 (10%)** |
| 덮기 가능(천장) | 86 | **187** |
| 실제 덮음 | 65 | 50 |
| **달성률** | **76%** | **27%** |

`arc-relscan`이 예측한 87.1%가 실현됐다. **세션 전체에서 처음으로 병목이 표현이
아니라 학습이 됐다** — 열두 번의 개입이 무효였던 이유는 천장이 상수였기 때문인데,
이제 천장은 거의 사라지고 달성률이 문제다.

**시도 195(맨몸 존재 변형)**: 관계 규칙은 self 14 + target 14 슬롯을 모두 맞춰야
하고, 존재 양화라 발화가 잦아 반례에 더 걸려 슬롯이 덜 떨어진다. 그래서 가장
일반적인 형태("바로 위에 **뭔가 있기만 하면**")를 슬롯 단위 탈락으로는 도달할 수
없었다 — 통째로 시도해 반례가 없으면 채택하도록 추가. 규칙 4,582→4,210(중복 정리).

**GEN3 성장 곡선**(고정 홀드아웃 128객체, 같은 바이너리):

| 경험 풀 | 규칙 | 부재 | 필터 | 미발화 |
|---|---|---|---|---|
| 100과제 | 1,500 | 60 | 38 | 98 |
| 200과제 | 4,210 | 43 | 48 | 91 |
| 300과제 | 5,588 | **37** | 50 | **87** |

**부재가 단조 감소(60→43→37, −38%)** — GEN2에서 실증한 "경험이 규칙 부재를 닫는다"가
GEN3에서도 성립한다. 다만 관계 규칙 공간이 훨씬 커서 속도가 느리고, 400과제로는
100% 덮개(③의 요구)에 못 닿는다. 이 홀드아웃에서도 원리상 차단은 속성 61 → 관계 18.

**시도 196(두 계층 결합)**: GEN2와 GEN3는 서로 다른 객체를 덮으므로(성질로 결정되는
자리 vs 관계로 결정되는 자리), 합집합이 ③의 전부-아니면-전무 게이트에 유리할 수
있다. 속성 우선·관계 후속으로 결합 적용을 구현.
**결과: 같은 과제를 다른 경로로 풀었을 뿐 총계 106 불변**(객체 1→0, 결합 0→1).
새 사례는 없고 귀속만 바뀌었다. 어블레이션에서 결합 0건 — 인과는 확인.

### 시도 197~199: **Active Epistemic Curriculum — 세 번의 시도와 정직한 음성 결과**

운영자 지시로 "무엇을 다음에 볼지 스스로 고르는" 루프를 만들었다. 자기 과제
*생성*은 정답의 출처가 순환이 되므로, 정답이 실재하는 환경(훈련셋)에서 **선택**하는
형태로 설계했다. 결과는 **음성**이며, 그 과정에서 내 실험 설계의 결함 셋을 잡았다.

**1차(시도 197 이전 버전) — 겉보기 4배 이득, 철회**

| 예산 | 수동 부재 | 능동 부재 |
|---|---|---|
| 20 | 73 | 30 |
| 40 | 51 | 22 |
| 80 | 46 | 15 |

두 겹의 결함이었다:
1. **라벨 누출** — `uncertainty()`가 `task_props_partial(train)`로 델타를 계산해
   **선택 전에 정답을 봤다**. 운영자가 지적한 label-informed selection 그대로다.
2. **불공정 대조군** — "수동 40"은 스캔 과제 수 40(경험 생산 ~17)이고 능동은 40개
   생산이었다. 같은 예산이 아니었다.

교정: `task_uncertainty(rules, inputs: &[Grid])` — **시그니처가 입력만 받아 출력
접근이 타입 수준에서 불가능**하다. 규율을 주석이 아니라 타입이 보장한다.
시험도 박았다(같은 입력·다른 출력 → 같은 점수).

**2차 — 무작위 다중 시드가 뒤집음**: 라벨 차단 후 능동 부재 **57** vs 무작위
32.1±6.2(z=+4.04), **12시드 중 0개를 이김**. 진단: "침묵+경합 **개수**"는 정보가
아니라 **객체 수**를 고른다.

**3차 — 정규화가 퇴화를 부름**: ÷전체로 정규화하니 빈 라이브러리에서 모든 과제가
1.0 동점 → 첫 후보 선택 → **sequential과 완전히 동일한 숫자**(32/68/3).

**4차(시도 199) — 구별되는 패턴 수**: 정보는 반복이 아니라 구별되는 패턴이므로
서로 다른 성질 벡터만 센다. 퇴화는 사라졌으나 우위는 없다:

| 팔 | 부재 | z | ② | z |
|---|---|---|---|---|
| sequential | 32 | −0.01 | 3 | −1.87 |
| active-conflict | 35 | +0.47 | **7** | +1.72 |
| active-mixed | 37 | +0.80 | 6 | +0.82 |
| random ×12 | 32.1±6.2 | — | 5.1±1.1 | — |

부재는 무작위 평균보다 **나쁘고**, ②는 무작위 **최선과 동률**(넘지 못함).

**🔍 세 실패를 관통하는 하나의 원인**

> 경험의 정보량은 **"어느 객체가 바뀌는가"**가 지배하는데, 그것이 바로 **정답**이다.
> 대부분의 객체는 바뀌지 않으므로, 입력만 보는 불확실성은 규칙 생성과 약하게만
> 연결된다. 1차가 잘 나온 이유가 정확히 이것 — 바뀌는 객체를 알고 있었으니까.

즉 **이 과제족에서 라벨 없는 능동 선택은 신호가 희박하다.** 구현 결함이 아니라
문제의 성질이다.

**결론(정직한 음성)**: 라벨을 차단하고 무작위 다중 시드와 겨루자 불확실성 기반
능동 선택의 우위는 사라졌다. "4배 표본효율"은 **신호로도 주장하지 않는다** — 철회.
내부 불확실성 감소(367→5)는 **자기 만족도이지 외부 능력이 아님**이 함께 실증됐다.

**다음 방향(계량이 지목)**: 능동 선택이 신호를 얻으려면 라벨 없이 **"무엇이 바뀔지"를
예측**해야 한다. 그것은 선택기의 일이 아니라 **세계모델**의 일이다 — MONAD의 원래
설계(관찰→예측→오차→수정)로 돌아가는 지점이며, 선택은 그 예측의 불확실성 위에서만
의미를 갖는다.

**영구 규율(이번에 확립)**:
- 봉인을 **먼저** 정하고 커리큘럼을 시작한다(능동 학습기는 평가 대상을 먹어치운다 —
  실제로 `aabf363d`를 출처로 골랐다).
- 선택기는 **입력만** 본다(타입으로 강제).
- 대조군은 파일 순서가 아니라 **무작위 다중 시드**(순서 운 배제).

### 시도 200~201: 합집합 덮개 계량 + 천장 수치 자체 교정

**합집합 덮개(시도 200)** — 지금까지 결합의 *게이트 통과*만 봤고 **덮개 자체**는
잰 적이 없었다:

```
합집합 덮개 70/208
├ GEN2 단독  18
├ GEN3 단독   9    ← 관계 계층이 새로 여는 것은 9개뿐
└ 둘 다      43    ← 두 계층이 대부분 같은 객체를 덮는다
```

**진단**: GEN3 규칙은 `self 조건(속성 14슬롯) + 관계 + target 조건` 형태라 사실상
**속성 규칙의 특수화**다 — 속성이 이미 통하는 자리에서 발화할 뿐이다. 관계만이
가를 수 있는 자리에 닿으려면 **관계가 판별을 떠맡아야** 하고, 그러려면 self가
일반적이어야 한다.

**처방과 그 실패**: target을 통째로 비운 "맨몸 존재"의 거울상인 **"맨몸 self"**
변형을 추가(세 변형 공존: 탈락만 / 등식 우선 / self 통째 변수). 규칙 4,210 →
**4,942개(+17%)**. 그러나 **합집합 덮개 74 → 74, GEN3 단독 9 → 9로 완전 불변**이고
어블레이션에서 관계 라이브러리를 빼도 최종 106 — **GEN3의 최종 기여는 0**.

원인: 맨몸 self 규칙은 target 조건이 **출처 과제의 구체 성질 벡터**라 홀드아웃
과제에서 그 조건을 만족하는 상대를 찾기 어렵다. self를 비우면 target이 좁아지고,
target을 비우면 self가 좁아진다 — 어느 쪽이든 전이 가능한 조합이 남지 않는다.

**천장 수치의 자체 교정(시도 201)**: `targets` 비교가 `Vec` **순서**까지 봤다.
존재 양화는 상대의 **집합**만 보므로 순서는 의미가 없고, 정규화하지 않으면 같은
집합을 다른 순서로 가진 객체가 "구별 가능"으로 잘못 집계된다. 정렬·중복 제거 후
재측정: **원리상 차단 21 → 30**(30% 부풀려져 있었음).

정정 후에도 결론은 유지된다: **관계 표현이 차단을 122 → 30으로 75% 줄인다.**

---

## 🏁 M2-R 2차 총괄 (시도 156~201)

### 확정된 것

1. **Code-free learning gain n=1** — 5중 검증(수면 결정론·어블레이션·각성 결정론·
   출처 분리·회귀 0), 대규모 리팩터 후 재검증까지 통과. 획득 과제 `aabf363d`는
   수면이 본 적조차 없는 267번째.
2. **표현 교체의 실효** — 관계 표현이 원리상 차단을 122/208 → **30/208**로 감소
   (천장 86 → 178). 이것이 이번 국면의 최대 성과이며 아키텍처 수준의 개선이다.
3. **8단계 루프 전부 구현·실증** — episodic·sleep·변수화·재구체화·조합·축적·
   prior(탐색 152배 감소)·표현 복수 가설.
4. **성장 곡선** — 경험 ↑ → 규칙 부재 ↓ (GEN2: 34→18→16, GEN3: 60→43→37).

### 미달과 그 정확한 위치

- **여러 사례 재현(n>1) 미달.**
- 병목이 **표현 → 학습**으로 이동했다. 관계 표현은 178개를 덮을 수 있는데
  실제로는 53개, 그중 43개는 속성 계층이 이미 덮던 것 → **새로 여는 것은 9개**.
- 세 가지 규칙 형태(탈락만·등식 우선·맨몸 self)를 시도했으나 전부 동일.
  self를 비우면 target이 좁아지고 target을 비우면 self가 좁아진다.

### 음성 결과(동등하게 중요)

- **Active Epistemic Curriculum: 우위 없음.** 라벨 차단 + 무작위 12시드 대조에서
  능동은 무작위 평균보다 나쁘거나(부재) 최선과 동률(②)이었다. 겉보기 4배 이득은
  **라벨 누출 + 불공정 예산**의 산물이었고 철회했다.
- 근본 이유: 경험의 정보량은 "어느 객체가 바뀌는가"가 지배하는데 **그것이 정답**이다.
  라벨 없는 능동 선택은 이 과제족에서 신호가 희박하다.
- 열두 개 이상의 개입이 최종 지표 106에서 움직이지 않았다.

### 다음 세대의 근거 있는 방향

1. **세계모델이 먼저다** — 능동 선택이 신호를 얻으려면 라벨 없이 "무엇이 바뀔지"를
   예측해야 한다. 그것은 선택기가 아니라 예측기의 일이며, MONAD의 원래 설계
   (관찰→예측→오차→수정)로 돌아가는 지점이다.
2. **규칙 형태의 조합** — 단일 관계 리터럴로는 self·target 어느 쪽도 충분히
   일반화할 수 없다. 관계 **연접**(여러 리터럴의 AND)이 남은 형태다.
3. **크기 변환 과제 97/237**은 이 표현족 밖이다.

### 시도 202~204: ③까지의 거리 계량 + 상대 복제(무효) — 표적이 좁아졌으나 벽은 같다

**시도 202(③까지의 거리, 처음 측정)**: ②인 과제들이 100% 덮개에서 얼마나 떨어져
있는지 재니 분포가 극단적이었다:

```
② 과제별 덮개율: 100% ×7 · 81% · 75% · 56% · 50% · 19% · 18% · 14% · 11% · 6%
100% 도달 7건인데 ③(훈련 재현)은 2건 → 5~6건이 덮개 부족이 아닌 이유로 실패
```

**시도 203(원인 분해)**: 100% 덮개인데 재현 실패한 6건에서 **유지 객체 훼손 0건**,
총 오차 셀 303. 바뀐 객체는 전부 덮었고 유지 객체는 하나도 안 망가뜨렸는데 오차가
남는다 → 오차의 출처는 **덮개 지표가 제외한 사본 객체**(delta=None·copies 있음)다.

*정직 고지*: 보고에 쓴 "사본 필요 6건"은 "그 과제에 사본 객체가 **있다**"를 셌을 뿐
원인을 증명하지 않았다. 위 삼각측량(덮개 100% + 유지 훼손 0 + 오차 303)이 근거다.

**시도 204(상대 복제)**: 복제 오프셋이 절대 좌표라 색 상수처럼 과제를 넘지 못한다고
보고 **자기 크기 단위**(`ACT_COPY_REL`, "내 너비만큼 오른쪽")를 추가. 절대·상대가
공존하고 증거가 고르는 구조. 경험 1,982 → **2,822건(+42%)**, 규칙 27k → **36k**.

**결과: 무효.** ③ 2 → **1**(오히려 감소), 100% 덮개 실패 6건 그대로, 최종 106
(어블레이션 105로 n=1은 유지).

**구조적 이유(세션 내내 반복된 교환의 또 다른 얼굴)**: 복제 규칙은 성질로 발화하는데,
**같은 성질을 가졌지만 사본을 낳지 않는 객체가 과제 안에 하나라도 있으면** 일관성
검사가 그 규칙을 버린다. 사본은 드물어 거의 항상 그렇다. 이를 피하려면 조건이 극히
구체적이어야 하고, 그러면 전이되지 않는다 — **일반성↔판별력 교환**이 복제에서도
동일하게 성립한다.

### 시도 205~206: 크기 변환 슬라이스 계량 + **선택 계층**(무엇이 답인가)

**시도 205(가장 큰 미개척 슬라이스)**: 약 스무 번의 개입이 모두 ①(시도 가능 22건)
안에서만 움직였는데, 홀드아웃 254건 중 **231건이 기술 불가**이고 그중 **97건이
크기 불일치** — 시도 가능 풀의 4배다. 그 구조를 처음 계량했다:

```
고정 크기 출력 40(41%) · 배수 k 15(15%) · 객체 bbox 추출 13(13%)
약수 8(8%) · 배수 kx,ky 4(4%) · 기타 17(18%)
훈련쌍 전부 같은 부류: 68건(70%) — 규칙이 일관됨
선택 문제(출력 = 입력 어느 객체의 잘라내기): 7건(7%)
```

최대 부류인 고정 크기 출력 40건은 대부분 입력의 부분이 아니라 **합성된 내용**이라
이 표현족 밖이다.

**시도 206(선택 계층)**: 7건은 작지만 **구조적으로 다르다** —
게이트가 "모든 객체를 덮어라"가 아니라 **"어느 객체가 답인가"**다. 세션 내내 ③를
막아온 과제당 100% 덮개 요구를 **쌍당 결정 하나**로 우회한다. 그리고 새 표현이
아니라 기존 성질 어휘를 **새로운 질문**에 재사용한다.

`arc_select.rs`: `SELRULE(성질 12칸)` · 반례 기반 탈락 · 모호하면 침묵.
시험 31/31(색·배치 다른 과제로 전이 + 모호 시 침묵).

**학습 논리의 오류를 하나 잡았다**: 처음 구현은 한 쌍의 **구체 벡터**에서 출발하며
"모든 쌍의 답에서 발화"를 처음부터 요구해 **모든 씨앗이 즉시 기각**됐다(규칙 0개).
슬롯을 떨어뜨리면 발화는 **늘기만** 하므로(단조성), 학습 중에는 **거짓 양성만
금지**하고 마지막에 완전성을 검사해야 한다. 교정 후 규칙 0 → 2개.

**결과**: 규칙 2개 · 게이트 통과 0 · 해결 0. 출처 4과제에서 배운 규칙이 홀드아웃
7건으로 전이되지 않는다. 최종 106 불변(어블레이션 동일).

---

## 최종 좌표 (시도 156~206)

| 항목 | 값 |
|---|---|
| **Code-free learning gain** | **n=1**, 5중 검증 3회 재확인 |
| GEN2 원리상 차단 | 122/208 (58%) |
| **GEN3 원리상 차단** | **30/208 (10%)** — 표현 교체의 실효 |
| 두 계층 합집합 덮개 | 70/208 (GEN3 단독 기여 9) |
| 탐색 감소 | 152배 |
| 최종 벤치마크 | 106/400 = 26.5% |
| 시험 | 31/31 |

**약 스물세 번의 개입이 최종 지표를 움직이지 못했다.** 경험 확대 · 일반화 사다리 ·
결정 목록 · 성질 확장 3종 · 반례 탈락 2종 · 관계 등식 · 관계 슬롯 · 부분 관측 지점 ·
계층 합성 · 반복 적용 · GEN3 규칙 형태 3종 · 상대 복제 · 능동 커리큘럼 3종 ·
선택 계층.

**전부 하나의 교환으로 설명된다**: 일반성을 올리면 판별력이 떨어지고, 판별력을
올리면 덮개가 떨어진다. 복제에서도(사본을 안 낳는 동일 성질 객체가 하나만 있어도
규칙이 버려짐), 선택에서도(4과제 경험으로는 7건에 닿지 않음) 같은 형태로 나타났다.
